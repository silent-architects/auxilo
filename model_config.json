{
  "extraction": {
    "_note": "P2.1a \u00a76.3 \u2014 Autonomous extraction provider config. Model is a config value, never hardcoded.",
    "primary": {
      "provider": "anthropic",
      "model": "claude-haiku-4-5"
    },
    "fallbacks": [
      {
        "provider": "anthropic",
        "model": "claude-sonnet-4-5"
      }
    ],
    "sonnet_model": {
      "provider": "anthropic",
      "model": "claude-sonnet-5",
      "_note": "Interim Sonnet default for the openclaw memory daemon (server.js runOpenClawDaemon) and /pipeline/upload extraction. De-dated from the retired claude-sonnet-4-20250514 pin (404s post-retirement) to the current supported Sonnet; config-driven, never hardcoded (AGENTS.md). NOTE: BUILD-SPEC-P2.1a designates claude-haiku-4-5 (extraction.primary) as the extraction tier, with Sonnet as fallback insurance only — the Sonnet pin on these two paths is inherited drift, not a designed choice. A tracked follow-up will realign both onto primary (Haiku) via extractWithRetry (which also adds the primary+fallback chain they currently lack)."
    },
    "timeout_ms": 45000,
    "max_attempts_per_provider": 3,
    "cache_control": {
      "enabled": false,
      "blocks": [
        "system_prompt",
        "extraction_schema"
      ],
      "_note": "Disabled at launch; flagged for P2.1b optimization (10-15% cost reduction)"
    },
    "rate_limits": {
      "tiers": {
        "unverified": {
          "daily": 50,
          "hourly": 30,
          "burst_per_min": 5
        },
        "verified": {
          "daily": 200,
          "hourly": 60,
          "burst_per_min": 5
        },
        "trusted": {
          "daily": 1000,
          "hourly": 120,
          "burst_per_min": 5
        }
      },
      "global_circuit_breaker": {
        "soft_alert_usd": 25,
        "hard_throttle_usd": 50,
        "kill_switch_usd": 100
      }
    },
    "transcript_limits": {
      "min_chars": 1500,
      "max_chars": 30000,
      "max_body_bytes": 262144,
      "max_json_depth": 10,
      "max_keys_per_object": 200
    },
    "source_allowlist": [
      "claude-code",
      "openclaw"
    ],
    "retraction_window_days": 7,
    "idempotency_ttl_hours": 24,
    "consent_version": "2026-04-14"
  }
}
