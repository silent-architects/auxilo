# Auxilo — Master Punch List

> Everything that must happen before launch. One list. No duplicates.
> Source files: TASKS.md, SECURITY-AUDIT.md, ANTIGRAVITY-REVIEW.md, VISUAL_IDENTITY.md, AGENT-TEAM.md
> Last updated: 2026-07-20 (FINAL SWEEP — see the note under Counts; previous: 2026-06-10 Launch Wave §20: LW-1–LW-16. LW-16 sensitivity-gated seamless publishing now **two-layer** (regex + LLM semantic) — built, not deployed; live recall MEASURED 2026-06-11 = **98.7% combined / 0 seed false-positives** — deploy-cleared. Spec: specs/BUILD-SPEC-LAUNCH-WAVE.md §LW-16/§LW-16a.)

---

## Legend

- **Owner**: Role ID from AGENT-TEAM.md (e.g., BUILD-1 = Architect)
- **Status**: `OPEN` | `SPEC DONE` | `IN PROGRESS` | `DONE` | `BLOCKED`
- **Priority**: `P0` (blocks launch) | `P1` (blocks real money) | `P2` (blocks scale) | `P3` (polish)

---

## 1. Security — CRITICAL (P0)

These block real money. No exceptions. Source: SECURITY-AUDIT.md

| # | Finding | Owner | Status |
|---|---------|-------|--------|
| C1 | Withdrawal race condition (double-withdrawal exploit) — add per-wallet async mutex | BUILD-1 → BUILD-2 | DONE (A1) — deployed 86c77f2 |
| C2 | No viem nonce management — nonce collision on concurrent TXs | BUILD-1 → BUILD-2 | VERIFIED (A0) — AUDIT-13 proof: T-A0-EDGE-004 |
| C3 | Non-atomic dual write on unlock — crash between learnings/earnings writes | BUILD-1 → BUILD-2 | DONE (A2) — deployed 86c77f2 |
| C4 | Stuck settlement infinite loop — no max-retry, no auto-refund | BUILD-1 → BUILD-2 | DONE (A2) — deployed 86c77f2 |
| C5 | x402 payment validation entirely external — no local crypto verification | BUILD-1 → BUILD-2 | DONE (A4) — deployed 86c77f2 |
| C6 | Private key plaintext on disk — move to env var minimum | BUILD-2 | VERIFIED (A0) — T-A0-UNIT-001 + S3 grep |
| C7 | Challenge nonce reuse (replay attack) — consume before verify | BUILD-1 → BUILD-2 | VERIFIED (A3) |
| C8 | Pending balance deducted before broadcast — use reservation model | BUILD-1 → BUILD-2 | DONE (A1) — deployed 86c77f2 |

**Review**: GOV-3 (Security Auditor) + SPEC-1 (Crypto Auditor) sign off on every fix. Separate context from Builder.

---

## 1b. Security — Sensitivity Filter (SPEC-SF, P0)

GOV-3 audit found 2 CRITICAL, 3 HIGH, 6 MEDIUM, 4 LOW in the learning pipeline. Prerequisite to Build-2 A-series.

| # | Finding | Owner | Status |
|---|---------|-------|--------|
| SF-C1 | 6 critical pattern gaps (SSH, Slack, Stripe, Google, npm, PEM) | BUILD-2 | DONE — patterns added to `lib/sensitivity-filter.js` |
| SF-C2 | sk_live_ not caught by generic sk- prefix | BUILD-2 | DONE — dedicated `stripe_key` pattern |
| SF-H2 | redactMatch leaks too many chars (6+4=10) | BUILD-2 | DONE — changed to 3+2=5 chars |
| SF-H3 | No unlock_price ceiling — DoS vector | BUILD-2 | DONE — `MAX_UNLOCK_PRICE = 1.00` enforced |
| SF-M1 | scanLearning() not wrapped in try/catch — fail open | BUILD-2 | DONE — fail closed (returns 500) |
| SF-M2 | PATTERNS missing /g flag invariant assertion | BUILD-2 | DONE — assertion at module load |
| SF-M3 | aws_secret matches substrings of larger base64 strings | BUILD-2 | DONE — word-boundary lookahead/lookbehind |
| SF-M4 | password pattern false-positives on null/undefined/placeholders | BUILD-2 | DONE — negative lookahead |
| SF-M5 | Dedup re-hashes every existing learning on every POST | BUILD-2 | DONE — pre-computed `body_hash` on new learnings; legacy fallback |
| SF-M6 | 409 Conflict exposes existing_id + existing_title | BUILD-2 | DONE — stripped from response |
| SF-L3 | Docs out of sync with live patterns | BUILD-2 | DONE — guide, punch-list, tasks updated |

**Tests**: 46 new cases (T-SF-UNIT-001/018, T-SF-INT-001/012, T-SF-EDGE-001/016). All 72 tests pass.

**GOV-3 Sign-off required** before Build-2 executes A-series (A3→A0→A2→A1→A4).

## 2. Security — HIGH (P1)

Block production traffic. Source: SECURITY-AUDIT.md

| # | Finding | Owner | Status |
|---|---------|-------|--------|
| H1 | No EIP-712 structured signing — implement typed data with domain separator | BUILD-1 → BUILD-2 | VERIFIED (A3) |
| H2 | Timestamp reuse on withdrawal signatures — add server-generated nonce | BUILD-1 → BUILD-2 | VERIFIED (A3) |
| H3 | No rate limit on /wallet/challenge — max 5 per wallet per 10 min | BUILD-2 | VERIFIED (A3) |
| H4 | Facilitator DDoS = revenue disabled — local verification fallback | BUILD-1 → BUILD-2 | DONE (A4) — deployed 86c77f2 |
| H5 | Admin token — no expiry, no rotation, no scope limiting | BUILD-2 | DONE (A4) — deployed 86c77f2 |

---

## 3. Security — MEDIUM (P2)

Fix before scale. Source: SECURITY-AUDIT.md

| # | Finding | Owner | Status |
|---|---------|-------|--------|
| M-A | In-memory rate limit lost on restart | BUILD-2 | DONE — Sprint 08, persist to disk on write + restore on boot |
| M-B | Earnings migration sets pending_balance without validating against learnings | BUILD-2 | DONE — Sprint 08, cross-check validation on earnings load |
| M-C | Settlement JSONL append-only, never compacted | BUILD-2 | DONE — Sprint 08, compaction at threshold with atomic rewrite |
| M-D | Backup cleanup runs on every safeWrite() call | BUILD-2 | DONE — Sprint 08, throttled to once/hour max |
| M-E | Silent JSON parse failures at startup | BUILD-2 | DONE — Sprint 08, loadDataFile with critical vs non-critical distinction |
| M-F | No private key rotation mechanism | BUILD-2 | DONE — Sprint 08, /admin/stage-key endpoint |
| M-G | Concurrent backup cleanup race condition | BUILD-2 | DONE — Sprint 08, cleanupRunning mutex with finally block |
| M-H | Ghost settlement on-chain check — `resolveStuckSettlements` should verify on-chain like `resolveProcessingSettlements` | BUILD-2 | DONE — Sprint 01 Track 1 |
| M-I | Daemon overlap guard — `let running = false` flag to prevent concurrent daemon ticks | BUILD-2 | DONE — Sprint 01 Track 1 |
| M-J | Reservation orphan cross-reference — filter against pending/retry settlements before releasing reservations | BUILD-2 | DONE — Sprint 01 Track 1 |

---

## 4. Antigravity Review — Open Questions (P1)

Fixes M1–M7 + R1 are applied but review questions are unresolved. Source: ANTIGRAVITY-REVIEW.md

| # | Question | Owner | Status |
|---|----------|-------|--------|
| AR-1 | M1: Should rate limit window burn on failed (pre-broadcast) withdrawals? | BUILD-1 | SPEC DONE (A1) — Yes, all attempts burn |
| AR-2 | M2: Double-toFixed redundancy — clean up or leave? | BUILD-2 | DONE — code comment only, no spec needed |
| AR-3 | M3: Consistency check runs before resolveStuckSettlements — causes false-positive drift warnings. Move after? | BUILD-1 | SPEC DONE (A2) — documented explicitly |
| AR-4 | M4: Backup cleanup in safeWrite() hot path — throttle to once/hour? | BUILD-1 | SPEC DONE (A2) — documented, defer to P2 |
| AR-5 | M5: Number(bigint) precision on large USDC values — use viem utility? | BUILD-2 | VERIFIED (A3+A0) — parseFloat(formatUnits(...)) mandatory, confirmed in tx-manager.js |
| AR-6 | M6/M7: Should pre-broadcast failures return 400/503 instead of 500? | BUILD-1 | SPEC DONE (A2) — pre=503, post=202 |
| AR-7 | M6/M7: Should timeout internals be exposed to client in processing response? | BUILD-1 | SPEC DONE (A2) — no, client polls /settlement/:id |
| AR-8 | Cross-fix interactions: Do the 7 fixes create new edge cases when combined? | GOV-3 + SPEC-1 | DONE — Sprint 03, PASS WITH OBSERVATIONS (3M + 2L, no blockers) |

---

## 5. Payment Infrastructure — Phase 0 (P0)

Without dual-rail payments, agents can't buy learnings. Source: TASKS.md

| # | Task | Owner | Status | Depends On |
|---|------|-------|--------|------------|
| 0.1 | Account system (email + magic link, API key gen, dashboard endpoint) | BUILD-1 → BUILD-2 | DONE — 74/74 tests, deployed to the legacy VM host, 12/12 live checks | — |
| 0.2 | Dual auth middleware (x402 OR API key, 401 with both options) | BUILD-1 → BUILD-2 | DONE — Sprint 02, deployed to the legacy VM host, 10/10 live checks | 0.1 |
| 0.3 | Credit system (free tier 50 queries + 10 unlocks/mo, balance tracking) | BUILD-1 → BUILD-2 | DONE — Sprint 03, 49/49 tests, deployed to the legacy VM host, 5/5 live checks | 0.1, 0.2 |
| 0.4 | Stripe integration (credit packs $5/$20/$50, webhook to credit account) | BUILD-1 → BUILD-2 | DONE — Sprint 04, 13/13 live checks, deployed 2026-03-08 | 0.1, 0.3 |
| 0.5 | Updated earnings system (track by account ID, wallet-less start, backward compat) | BUILD-1 → BUILD-2 | DONE — Sprint 02, deployed to the legacy VM host, 8/8 live checks | 0.1 |

**Review**: GOV-2 (Compliance) reviews Stripe terms for crypto-adjacent products. SPEC-3 (Builder UX) reviews account + earnings flows. SPEC-2 (Agent UX) reviews dual-auth developer experience.

---

## 6. Autonomous Publishing — Phase 1 (P0)

The product. If agents can't publish learnings, no supply. Source: TASKS.md

| # | Task | Owner | Status | Depends On |
|---|------|-------|--------|------------|
| 1.1 | System prompt template (learning detection, quality gate, dedup, structured output) | BUILD-3 + SPEC-2 | DONE — Sprint 03, 48/48 tests, deployed to the legacy VM host, 3/3 live checks | — |
| 1.2 | Learning extractor (transcript → structured learnings, confidence scoring, dedup) | BUILD-1 → BUILD-2 | DONE — Sprint 05, 35/35 tests, deployed 2026-03-08, 15/15 live checks | 1.1 |
| 1.3 | OpenClaw adapter (read .md memory files, feed to extractor, heartbeat daemon) | BUILD-1 → BUILD-2 | DONE — Sprint 06, 31/31 tests, deployed 2026-03-09, 18/18 live checks | 1.2 |

**Review**: SPEC-2 (Agent UX) validates agent-side flows. BUILD-4 (QA) tests across Claude Code + OpenClaw minimum.

---

## 7. Brand & Narrative (P1)

Not before launch, but before public visibility. Source: VISUAL_IDENTITY.md

| # | Deliverable | Owner | Status |
|---|-------------|-------|--------|
| B-1 | BRAND_GUIDELINES.md — production-ready usage reference (strips exploration from VISUAL_IDENTITY.md) | BUILD-3 | DONE — Sprint 13, 159 lines, 6 sections |
| B-2 | Geometric pattern — quieter variant (scale down 40%, spacing 2x, opacity -30%) | BUILD-3 | DONE |
| B-3 | Growth flywheel — rebuild in Figma/SVG with brand typeface, fix copy ("upload learnings" not "publish skills") | BUILD-3 | DONE |
| B-4 | Section divider — SVG mark + CSS gradient implementation | BUILD-3 | DONE |
| B-5 | Category icons — map from Phosphor Light or Lucide libraries | BUILD-3 | DONE |
| B-6 | OG image template — tagline-swappable variants | BUILD-3 | DONE |
| B-7 | Favicon — verify Angular A at 16px, simplify if stroke intersections mud up | BUILD-3 | DONE |
| B-8 | Landing page copy (AFTER product works — strategic decision #7) | BUILD-3 | DONE — Sprint 13, 194 lines, 7 sections, all prices verified against live API |
| B-9 | Builder onboarding copy | BUILD-3 | DONE |
| B-10 | LinkedIn launch content | BUILD-3 | ON HOLD — GTM/marketing paused until marketplace fully functional (Tyler, 2026-03-30) |

---

## 8. Infrastructure & Deployment (P1)

| # | Task | Owner | Status |
|---|------|-------|--------|
| I-1 | Deploy security fixes to the legacy VM host after review sign-off | BUILD-2 + BUILD-4 | DONE — 86c77f2 live |
| I-2 | Validate live deployment matches local behavior | BUILD-4 | DONE — T-DEPLOY 5/5 + x402v2 7/7 |
| I-3 | Environment variable setup for private key (C6 minimum fix) | BUILD-2 | DONE — start.sh manages WALLET_PRIVATE_KEY + SESSION_SECRET |
| I-4 | Legacy VM host production hardening (process restart, log rotation, monitoring) | BUILD-2 | DONE — Sprint 08, PM2 + health monitoring + graceful shutdown |
| I-5 | npm package update (auxilo-mcp) after security fixes | BUILD-2 | DONE — Sprint 08 v0.6.0; Sprint 12 v0.7.0 (8 new tools); v0.7.0 published to npm 2026-03-14 |
| I-6 | Deployment documentation (DEPLOY-GUIDE.md) | BUILD-2 | DONE — the legacy host API, chunked uploads, env vars, troubleshooting |

---

## 9. Testing & QA (P0)

| # | Task | Owner | Status |
|---|------|-------|--------|
| T-1 | Write test cases for C1–C8 + H1–H5 fixes from SPEC-A0/A1/A2/A3/A4 BEFORE implementation begins | BUILD-4 | DONE — `specs/TEST-A-SERIES.md` (159 tests) |
| T-2 | Write test cases for Phase 0 features BEFORE implementation begins | BUILD-4 | DONE — 0.1 (37), 0.2 (37), 0.3 (49), 0.5 (34), 0.4 (25) done; Phase 0 complete |
| T-3 | Write test cases for Phase 1 features BEFORE implementation begins | BUILD-4 | DONE — 1.1 (48), 1.2 (35), 1.3 (31); Phase 1 complete |
| T-4 | Full regression: v0.1.0 endpoints (GET /, /health, /categories, /stats, POST /discover, GET /skill/:id, 4 MCP tools) | BUILD-4 | DONE — Sprint 07, 26/26 tests pass |
| T-5 | Full regression: v0.2.0 endpoints (POST /learn, POST /knowledge, GET /knowledge/:id, POST /knowledge/:id/rate, GET /knowledge/stats, GET /contributor/:wallet, openapi.json, agent.json, 5 MCP tools) | BUILD-4 | DONE — Sprint 07, 30/30 tests pass |
| T-6 | Full regression: v0.3.0 settlement (challenge/verify, withdrawal, settlement lifecycle, earnings migration, stuck resolution, admin) | BUILD-4 | DONE — covered by 108 A-series tests |
| T-7 | Edge case sweep: concurrent withdrawals, malformed JSON on every POST, empty states, 10K+ char strings, unicode, duplicate submissions | BUILD-4 | DONE — Sprint 07, 58/58 tests pass |
| T-8 | Live validation on the legacy VM host after each deployment | BUILD-4 | DONE — T-DEPLOY 5/5 + x402v2 7/7 + Sprint 02 18/18 + Sprint 03 8/8 + Sprint 04 13/13 + Sprint 05 15/15 + Sprint 06 18/18 + Sprint 07 157/157 live regression + Sprint 10 19/19 |

---

## 10. Governance & Compliance (P1)

| # | Task | Owner | Status |
|---|------|-------|--------|
| G-1 | Stripe terms review for crypto-adjacent products (before 0.4) | GOV-2 | DONE |
| G-2 | Money transmission analysis (are we operating as an MSB?) | GOV-2 | DONE |
| G-3 | Tax reporting obligations for builder payouts (1099 thresholds) | GOV-2 | DONE |
| G-4 | Terms of service draft | GOV-2 | DONE |
| G-5 | Privacy policy (what data we store, GDPR if applicable) | GOV-2 | DONE |
| G-6 | Risk register — document and track all identified risks | GOV-2 | DONE |

---

## 11. Agent Team Operationalization (P1)

| # | Task | Owner | Status |
|---|------|-------|--------|
| A-1 | AGENT-TEAM.md created | GOV-1 | DONE |
| A-2 | Per-agent CLAUDE.md files (10 roles) | GOV-1 | DONE |
| A-3 | PUNCH-LIST.md created (this file) | GOV-1 | DONE |
| A-4 | TASKS.md updated with team ownership | GOV-1 | DONE |
| A-5 | Sprint Orchestrator pattern (ORCHESTRATOR.md + 4 prompt templates) | BUILD-1 | DONE — punchlist→sprint→agents→deploy pipeline |

---

## Execution Sequence

```
Phase 0 (parallel tracks):

  Track A: Security Fixes
  C1-C8 → Review (GOV-3 + SPEC-1) → H1-H5 → Review → Deploy → Live QA

  Track B: Payment Infrastructure
  0.1 Account System → 0.2 Dual Auth → 0.3 Credits → 0.4 Stripe → 0.5 Earnings
  (GOV-2 reviews Stripe terms in parallel with 0.1-0.3)

  Track C: Autonomous Publishing (can start during Track B)
  1.1 System Prompt Template → 1.2 Learning Extractor → 1.3 OpenClaw Adapter

  Track D: Brand (runs parallel, blocked items unblock as product ships)
  B-1 Brand Guidelines → B-2 through B-7 (assets) → B-8/B-9/B-10 (copy, post-product)

Gate: All P0 items DONE + all review sign-offs → Launch
```

---

## 12. Post-Sprint 08 Audit Findings (P1/P2)

Source: AUDIT-POST-S8-RESULTS.md (GOV-3 audit, 2026-03-11). 28 checklist items, 18 findings.

| # | Finding | Severity | Owner | Status |
|---|---------|----------|-------|--------|
| AU-1 | OpenClaw endpoints unauthenticated (S-3) | HIGH | BUILD-2 | DONE — Sprint 09, adminAuth() added to all 4 endpoints |
| AU-2 | Unbounded in-memory Maps leak memory (E-7) | HIGH | BUILD-2 | DONE — Sprint 09, 10-min sweep timer for 3 stores |
| AU-3 | Reservations + compaction writes skip fsync (S-10) | MEDIUM | BUILD-2 | DONE — Sprint 09, writeAndSync() for 3 call sites |
| AU-4 | processing_unresolved is dead-end state (F-9) | MEDIUM | BUILD-2 | DONE — Sprint 09, daemon resolution + 48h forced-fail |
| AU-5 | /extract endpoint broken auth + rate limiter (S-1/F-2) | LOW | BUILD-2 | DONE — Sprint 09, adminAuth() + rate limiter fix |
| AU-6 | Startup parallelization (E-8) | HIGH (eff) | BUILD-2 | DONE |
| AU-7 | Cache accounts + settlements in memory (E-2) | MEDIUM | BUILD-2 | DONE |
| AU-8 | Per-API-key rate limit on paid endpoints (S-5) | MEDIUM | BUILD-2 | DONE |
| AU-9 | Root endpoint missing 20+ routes in docs (F-1) | MEDIUM | BUILD-2 | DONE — Sprint 10, 11 routes added (32 total documented) |
| AU-10 | MCP missing web-reader + stats tools (F-7) | MEDIUM | BUILD-2 | DONE — Sprint 11, 8 tools added (the six web-reader tools, get_stats, get_knowledge_stats) |
| AU-11 | CORS legacy-host URL documentation (S-7) | LOW | BUILD-2 | DONE — Sprint 11, comment added documenting URL format, current origins, update instructions |
| AU-12 | err.message in signature error responses (S-8) | LOW | BUILD-2 | DONE — Sprint 11, stripped details field from /wallet/verify + /withdraw catch blocks; kept in console.error |
| AU-13 | .catch(console.error) on startup (F-4) | LOW | BUILD-2 | DONE — Sprint 11, replaced with contextual handlers logging subsystem + stack; non-critical (continues with warning) |

---

---

## 13. Post-Audit Cleanup & Next Features (P1/P2)

Source: Audit remediation session 2026-03-20. Items identified during 13-track deploy + documentation review.

### Cleanup (P1)

| # | Task | Owner | Status |
|---|------|-------|--------|
| CL-1 | Remove orphaned `public/terms.html` (no route serves it — `/terms` uses `docs/TERMS-OF-SERVICE.md`) | BUILD-2 | DONE — deleted 2026-03-20 |
| CL-2 | Remove orphaned `public/privacy.html` (no route serves it — `/privacy` uses `docs/PRIVACY-POLICY.md`) | BUILD-2 | DONE — deleted 2026-03-20 |
| CL-3 | Update "Last Updated" date in `docs/TERMS-OF-SERVICE.md` from March 17 → March 20 | BUILD-2 | DONE — 2026-03-20 |
| CL-4 | Update "Last Updated" date in `docs/PRIVACY-POLICY.md` from March 17 → March 20 | BUILD-2 | DONE — 2026-03-20 |
| CL-5 | Add `favicon.ico` to `public/` (explicit route exists at line 3031 but file is missing — returns 404) | BUILD-3 | DONE — favicon.svg created + routes updated (W1-F1, verified 2026-03-30) |
| CL-6 | Deploy `docs/SITE-ARCHITECTURE.md` to the legacy VM host | BUILD-2 | DONE — Wave 1 full deploy 2026-03-30 (22 files, 11/12 live checks, 1 test bug fixed) |

### Next Features (P2 — designed, not built)

| # | Task | Owner | Status |
|---|------|-------|--------|
| NF-1 | Multi-environment onboarding — per-LLM setup instructions for builders with multiple agents | SPEC-3 + BUILD-2 | DONE — 5 templates in `prompts/` (W1-D1, verified 2026-03-30) |
| NF-2 | MCP `instructions` field — proactive Auxilo checking prompt in tool metadata | SPEC-2 + BUILD-2 | DONE — already implemented in mcp-server.js `instructions` field |
| NF-3 | `npx auxilo-mcp init` / `add-environment` CLI — builder onboarding tool | BUILD-2 | ✅ **BUILT 2026-07-19 (Wave 3.4, `wave34-scoped-keys`)** — `npx auxilo init` (+ `auxilo-mcp init` delegation): zero-prompt scoped-key minting for CI / second machines via the device flow (`--scope read\|earnings-read\|contribute` default contribute, `--label` default init-\<hostname\>, `--env-file` 0600 idempotent writer, `--save` opt-in credentials write, `--json`); warns on pre-3.4-server scope downgrade (status echo). Spec `~/.auxilo/handoffs/BUILD-SPEC-WAVE34-2026-07-19.md`. CLI half is POST-0.9.3 (ship as npm **0.9.4** — no version bumped yet); server half (device-flow scope/label/cap/echo) rides the next deploy and degrades gracefully until then. NOT merged/deployed. |
| NF-4 | Scoped API keys per environment — one account, multiple keys with labels | BUILD-1 → BUILD-2 | DONE — multi-key with labels, 3 endpoints (W1-D2, verified 2026-03-30). **D2 TIGHTENED 2026-07-19 (Wave 3.4)**: taxonomy now read < **earnings-read** < contribute < admin (single-sourced lib/accounts.js; `GET /account/earnings` moved read→earnings-read); **admin never issuable** via API or device flow (403 ADMIN_SCOPE_NOT_ISSUABLE); pre-D2 keys grandfathered via `effective_scope` (legacy read ⇒ earnings-read = exact prior capability set — existing keys keep working); **live bug fixed**: dualAuthDynamic's exact-match scope check 403'd the default contribute setup key on the API-key/credit unlock path (now rank-based) — **regressed 3d365e1 / 2026-04-18 (P2.1a)**: exact-match scope check + 'read' requiredScope + contribute device keys landed in the same commit; API-key credit unlocks dead on prod until this wave (Gate-A reviewer CONFIRMED live by booting both servers); rotation added (`POST /account/api-keys/rotate`, capability-preserving, session-by-label or key-SELF-only) + key-caller self-revoke + session-or-key listing; full route→scope audit table in spec §3. The lost `specs/WAVE1-BUILD-D2-SCOPED-KEYS.md` is superseded by `~/.auxilo/handoffs/BUILD-SPEC-WAVE34-2026-07-19.md` (design of record). NOT merged/deployed. |
| D2-F2 | **Key-store growth + rotation abuse (Wave 3.4 Gate-A reviewer F2)** — (a) the labeled-creation 10-key cap counts INACTIVE entries (`account.api_keys.length`) while rotation permanently appends dead deactivated entries → 10 rotations bricks labeled minting for the account; (b) `POST /account/api-keys/rotate` has no cap and no rate limit → a stolen key looping rotate grows accounts.json without bound (DoS amplifier — the file is parsed on every authenticated request). Fix direction: count ACTIVE keys only at the labeled cap (device-authorize path already does), compact-or-cap rotated-out entries (e.g. keep last N per label or prune inactive beyond a ceiling), rate-limit rotate (per-account, reuse the AU-8 limiter idiom). | P1 fast-follow | GOV-3 + BUILD-1 | ✅ **BUILT 2026-07-19 (Wave-5B, `wave5b-review-closures`; spec `~/.auxilo/handoffs/BUILD-SPEC-WAVE5B-2026-07-19.md` §3)** — (a) labeled cap now counts ACTIVE keys only AND the fully-uncapped legacy name-path gained the same cap; (b) `compactRotatedKeys` (lib/accounts.js): rotation keeps the newest 3 rotated-out entries per label, revocation audit rows untouched, replacement entry stays LAST (caller index math holds), rotate route calls `rebuildKeyIndex()` whenever compaction shifted positions (stale `key_index` would stamp last_used_at on the wrong entry); (c) `POST /account/api-keys/rotate` per-account limit 10/hour (in-memory sliding window, isLearnRateLimited idiom; deliberately NOT in the M-A persisted payload — compaction is the durable control, restart-reset acceptable). 12-rotation loop test pins total store size ≤ 1 live + 3 debris. Tests in `test/wave5b-review-closures.test.js`. **Gate-A F5:** the create→revoke loop still grew the store (revocation debris was permanent and LABELS are attacker-chosen, so per-label ceilings bound nothing) — `capInactiveDebris`: GLOBAL newest-15 ceiling on ALL inactive entries per account (label-agnostic; chosen over a creation rate limit, which only slows the same unbounded growth), revoke route stamps `revoked_at` + caps + `rebuildKeyIndex()` on shift; rotation path applies both ceilings. Hard bound: ≤10 active + 15 dead per account. 30-unique-label churn test + mutation-verified. **Gate-A F6 (adjacent, same commit):** /learn's own sensitivity call had no mutation pin (stubbing the classifier left the whole suite green) — loud pins added on the /learn slice: classifier invoked, fail-closed fallback, verdict gates hold reason AND seamless predicate, flagged verdict persisted; reviewer's stub mutation now kills. |
| NF-5 | Dynamic pricing engine activation — `lib/pricing.js` exists but not wired to live pricing | BUILD-2 | DONE — pricing wired to 3 routes + cron + 2 analytics endpoints (W1-E1/E2, verified 2026-03-30) |

---

## 14. Wave 1 — Documentation Architecture + Agent Adoption (P1/P2)

Source: Documentation audit session 2026-03-20. All specs in `specs/WAVE1-*.md`.

### Documentation — Keystone Docs (P1)

| # | Task | Spec File | Owner | Status |
|---|------|-----------|-------|--------|
| W1-A1 | `docs/PRODUCT.md` — Product Requirements Document | `specs/WAVE1-DOC-A1-PRODUCT.md` | Opus | DONE (verified 2026-03-30) |
| W1-A2 | `docs/MARKETPLACE.md` — Marketplace mechanics reference | `specs/WAVE1-DOC-A2-MARKETPLACE.md` | Opus | DONE (verified 2026-03-30) |
| W1-A3 | `docs/GTM.md` — Go-to-market plan | `specs/WAVE1-DOC-A3-GTM.md` | Opus | DONE (verified 2026-03-30) |
| W1-A4 | `docs/FINANCIAL-PLAN.md` — Consolidated financial model | `specs/WAVE1-DOC-A4-FINANCIAL.md` | Opus | DONE (verified 2026-03-30) |

### Documentation — Reference Docs (P1)

| # | Task | Spec File | Owner | Status |
|---|------|-----------|-------|--------|
| W1-B1 | `docs/UX-FLOWS.md` — User journey maps | `specs/WAVE1-DOC-B1-UX-FLOWS.md` | Opus | DONE (verified 2026-03-30) |
| W1-B2 | `docs/DEVELOPER-GUIDE.md` — API quickstart | `specs/WAVE1-DOC-B2-DEVELOPER-GUIDE.md` | Sonnet | DONE (verified 2026-03-30) |
| W1-B3 | `docs/RUNBOOK.md` — Operations runbook | `specs/WAVE1-DOC-B3-RUNBOOK.md` | Opus | DONE (verified 2026-03-30) |
| W1-B4 | `docs/INDEX.md` — Master documentation map | `specs/WAVE1-DOC-B4-INDEX.md` | Opus | DONE (verified 2026-03-30) |

### Fixes — Stale Content + File Moves (P1)

| # | Task | Spec File | Owner | Status |
|---|------|-----------|-------|--------|
| W1-C1 | Fix stale pricing in ONBOARDING-COPY.md, mcp-server.js, llms.txt + version normalization | `specs/WAVE1-FIX-C1-STALE-CONTENT.md` | Sonnet | DONE (verified 2026-03-30) |
| W1-C2 | Move competitive analysis to `docs/COMPETITIVE.md` + mark 4 superseded docs | `specs/WAVE1-FIX-C2-FILE-MOVES.md` | Sonnet | DONE (verified 2026-03-30) |

### Build — Agent Adoption (P1)

| # | Task | Spec File | Owner | Status |
|---|------|-----------|-------|--------|
| W1-D1 | Per-LLM instruction templates (Cursor, Windsurf, Cline, OpenAI, Claude Desktop) | `specs/WAVE1-BUILD-D1-LLM-TEMPLATES.md` | Sonnet | DONE (verified 2026-03-30) |
| W1-D2 | Scoped API keys with labels (one account, multiple keys) | `specs/WAVE1-BUILD-D2-SCOPED-KEYS.md` | Sonnet | DONE (verified 2026-03-30) |

### Build — Pricing Engine (P2)

| # | Task | Spec File | Owner | Status |
|---|------|-----------|-------|--------|
| W1-E1 | Wire `lib/pricing.js` to live routes (POST /learn, POST /knowledge, GET /knowledge/:id) | `specs/WAVE1-BUILD-E1-PRICING-ENGINE.md` | Sonnet | DONE (verified 2026-03-30) |
| W1-E2 | Search impression tracking + daily pricing cron + GET /pricing/categories + GET /contributor/:wallet/pricing-insights | `specs/WAVE1-BUILD-E2-IMPRESSIONS-CRON.md` | Sonnet | DONE (verified 2026-03-30) |

### Build — Cleanup (P1)

| # | Task | Spec File | Owner | Status |
|---|------|-----------|-------|--------|
| W1-F1 | Create `public/favicon.svg` + version normalization in server.js + openapi.json | `specs/WAVE1-BUILD-F1-FAVICON-VERSION.md` | Sonnet | DONE (verified 2026-03-30) |

---

## 15. Governance: Documentation Rules

**These rules apply to ALL future work. No exceptions.**

### Rule 1: No Undocumented Features
Every new feature, endpoint, pricing change, policy change, or UX flow MUST be reflected in its domain's source-of-truth document BEFORE the PR/deploy is considered complete. The build spec must list which docs to update.

### Rule 2: INDEX.md Is the Map
`docs/INDEX.md` maps every document to its domain and declares source-of-truth status. New documents MUST be registered in INDEX.md. If a document is superseded, it gets a banner AND an INDEX.md update.

### Rule 3: One Source of Truth Per Domain
Each domain (product, marketplace, pricing, GTM, etc.) has exactly ONE source-of-truth document as designated in INDEX.md. Business decisions live in domain docs, NOT in MEMORY.md. MEMORY.md stores operational context only (credentials, VM IDs, session state).

### Rule 4: Build Specs Reference Docs
Every build spec (`specs/WAVE1-*.md`, `specs/BUILD-SPEC-*.md`) must include a "Documentation Impact" section listing which docs need updating. Reviewers check this before sign-off.

### Rule 5: Stale Detection
Any document with "Last updated" older than 30 days gets flagged in the next sprint planning. The sprint must include a documentation review item.

### Rule 6: Punch List Tracks Everything
Every planned enhancement, optimization, or addon gets a line item in PUNCH-LIST.md with: task description, owner, status, and spec file reference (if applicable). Nothing lives only in conversation history or memory bullets.

---

## 16. Review & Inspection Gates

**Reviews are gate-based, not calendar-based. Every change triggers the relevant gates before merge/deploy. No exceptions.**

### Gate A: Every Deploy/Merge (mandatory)

| Gate | Owner | Pass Criteria |
|------|-------|---------------|
| **Engineering Review** | BUILD-1 (Architect) | Spec matches architecture, no unintended side effects, fallback chains intact |
| **Code Inspection** | BUILD-4 (QA) | `node --check server.js` passes, all tests green, no regressions, scope check (only listed files modified) |
| **Security Review** | GOV-3 (Security Auditor) | Auth correct, input validated, no secrets in code, OWASP top 10 checked, no new attack surface |
| **Documentation Review** | GOV-1 (PM) | §15 Rule 1 enforced — domain docs updated, INDEX.md current, PUNCH-LIST.md updated |

### Gate B: Scope-Triggered (only when change touches that domain)

| Gate | Trigger | Owner | Pass Criteria |
|------|---------|-------|---------------|
| **Financial Integrity** | Any file in outputs/, financial model, investor deck, pricing assumptions, revenue projections | GOV-4 (CFO) | Every number traces to FOUNDATION.md, FOUNDATION.lock hash valid, no stale assumptions, cross-file sync verified |
| **Crypto/Payment** | Wallets, x402, pricing, payouts | SPEC-1 (Crypto Auditor) | Payment dedup intact, price bounds enforced, no fund loss paths |
| **Agent UX** | API responses, MCP tools, discovery, search | SPEC-2 (Agent UX) | Backward-compatible, agent can discover/query/unlock without breaking, error messages actionable |
| **Builder UX** | Onboarding, earnings, dashboard, API keys | SPEC-3 (Builder UX) | Flow completable end-to-end, earnings visible, no dead ends |
| **Legal/Compliance** | Terms, privacy, data handling, public claims, payments | GOV-2 (Compliance) | ToS/Privacy consistent, GENIUS Act compliant, no unapproved data collection |
| **Brand/Narrative** | Copy, messaging, external-facing text, landing page | BUILD-3 (Brand) | Voice consistent with BRAND_GUIDELINES.md, no jargon drift, ICP messaging intact |
| **SEO** | Page content, meta tags, URLs, sitemap | BUILD-3 (Brand) | Title/description tags present, structured data valid, canonical URLs, no broken links |
| **GEO (Generative Engine Optimization)** | llms.txt, agent.json, MCP descriptions, OpenAPI spec, instructions field | SPEC-2 (Agent UX) | Agent-discoverable, tool descriptions accurate, OpenAPI paths match live routes, llms.txt current |

### Gate C: Milestone Deep Dives (at major releases or when entering operate mode)

| Review | Trigger | Owner | Pass Criteria |
|--------|---------|-------|---------------|
| **Competitive** | Major release or quarterly | GOV-1 (PM) | Landscape scanned, moat intact, pricing competitive, no new threats unaddressed |
| **Catalog Health** | Major release or quarterly | GOV-1 (PM) | No stale learnings (>90d unrated), pricing distribution healthy, category gaps identified |
| **Infrastructure/Dependency** | Major release or quarterly | BUILD-4 (QA) | `npm audit` clean, prod VM healthy, backups verified, uptime >99% |
| **Financial** | Major release or quarterly | GOV-2 (Compliance) | Revenue tracking accurate, payout math verified, tax reporting current |
| **Full UX Audit** | Major release | SPEC-2 + SPEC-3 | All user journeys tested, mobile responsive, accessibility basics met |

### How It Works in Practice

1. Build spec lists files to modify → that determines which Gate B reviews activate
2. Builder (BUILD-2) implements the spec
3. Gate A reviews run (all four, every time)
4. Gate B reviews run (only the ones triggered by scope)
5. All reviewers sign "SHIP" or "SHIP WITH FIXES" → fixes applied → re-review
6. GOV-1 (PM) gives final sign-off
7. Deploy to the prod VM
8. Post-deploy smoke test (BUILD-4)

**The agent who builds is never the only agent who reviews.**

---

## 17. GTM & Acquisition

| # | Finding | Priority | Owner | Status |
|---|---------|----------|-------|--------|
| GTM-1 | **History upload pipeline non-functional** — `requireSession` middleware undefined in server.js, `accounts` data structure unverified. Every POST /pipeline/upload request fails. Blocks onboarding flow (new builders can't upload history → can't earn from day 1 → flywheel stalls). | **P0** | BUILD-1 → BUILD-2 | **DONE** — `requireSession = requireAuth` alias confirmed at server.js:645 (bc2b4650); `accounts[accountId]` confirmed at server.js:5436 (81907963). `specs/BUILD-SPEC-GTM.md` documents full flow. |
| GTM-2 | **Pricing floor discrepancy** — `lib/pricing.js` exports `MIN_UNLOCK_PRICE = $0.005`. Approved floor is `$0.05`. 10x gap. Fix code to match approved pricing. | **P0** | BUILD-2 | **DONE** — server.js:1969 changed 0.005→0.05. `tests/test-sensitivity-filter.js` T-SF-INT-002 updated. `node --check` clean. 114/114 tests pass. `specs/BUILD-SPEC-GTM.md`. |
| GTM-3 | **TLDR AI newsletter placement** — $10K budget, 1M+ AI practitioner subscribers. Schedule post-launch when catalog has 200+ learnings and earning proof exists. | P1 | EXEC-1 | ON HOLD — marketplace must be fully functional first (Tyler, 2026-03-30) |
| GTM-4 | **Product Hunt launch** — $0 CPA. Prep launch page, assets, first-day push. Sequence after core platform stable + 3-5 builder testimonials. | P1 | BUILD-3 | ON HOLD — marketplace must be fully functional first (Tyler, 2026-03-30) |
| GTM-5 | **Hacker News launch post** — $0 CPA. Write "Show HN" post. Sequence same day or within 48h of Product Hunt for momentum. | P1 | BUILD-3 | ON HOLD — marketplace must be fully functional first (Tyler, 2026-03-30) |
| GTM-6 | **MCP marketplace listings** — List auxilo-mcp in Anthropic MCP directory, OpenAI plugin registry, and any future agent tool registries. $0 CPA, ongoing discovery. | P1 | SPEC-2 | ON HOLD — marketplace must be fully functional first (Tyler, 2026-03-30) |
| GTM-7 | **Agent-to-agent viral loop** — Agents that use Auxilo recommend it to other agents. Speculative. Research mechanism design when agent-to-agent communication patterns emerge. | P2 | SPEC-2 | ON HOLD — marketplace must be fully functional first (Tyler, 2026-03-30) |
| GTM-8 | **Earnings-copy remediation (FTC + CA DFAL §3103)** — kill $21k/$210k worked math + 10-yr projection tables; "Build Once. Keep Earning." positioning; royalty framing; concrete 70/60 split; live real-earnings module on /earnings fed by `/knowledge/stats`. Two-seat review (marketplace-ip-counsel × growth-advocate). Reinstatement trigger + full policy in `docs/GTM.md` §1–2. | **P0** | GROWTH-1 + GOV-2 | **DONE 2026-07-03** — all public pages + README + mcp-server.js (npm copy rides next republish) |
| GTM-9 | **Real-earnings module v2** — trailing-30-day unlock volume + top-earning learning on /earnings; needs timestamped unlock events (current `/knowledge/stats` is all-time only). | P2 | SPEC-3 | ✅ **GROUNDWORK BUILT 2026-07-19** (branch `wave5a-money-closures`, spec `~/.auxilo/handoffs/BUILD-SPEC-WAVE5A-2026-07-19.md` §4): `data/unlock-events.jsonl` — per-unlock rows (ts, learning_id, amount_paid_usd = accrual basis, funding_source, contributor identity, settled_onchain; **NO buyer PII**), written at the WAL-protected unlock commit as step `unlock_event_appended`; `replayUnlock` re-emits after a crash with a duplicate scan (event id == WAL id = reader dedupe key; ts stored in the WAL payload so replay is byte-identical). Deliberately a SEPARATE file from settlements.jsonl (that file is a state machine, folded to latest-status-per-id and compacted at startup — it would misfold immutable facts); never compacted. Tests: `test/wave5a-money-closures.test.js` U-series. Remaining scope (small, now unblocked): the /earnings trailing-30d render itself. NOT merged/deployed. |
| GTM-10 | **Projection reinstatement** — numeric earnings projections may return at ≥25 real external earning builders + publishable median, built from actuals with adjacent disclosure. Trigger definition: `docs/GTM.md` §2. | P2 | GROWTH-1 + GOV-2 | GATED (waiting on real-builder threshold) |

> **P0 LAUNCH GATE: CLEARED (2026-03-25)** — GTM-1 confirmed resolved (retroactive), GTM-2 floor fix applied. All P0 items DONE.

---

## 18. Post-Deploy Findings (2026-03-30 SPEC-2 Audit)

Source: SPEC-2 full lifecycle audit against live API after Wave 1 deploy.

| # | Finding | Priority | Owner | Status |
|---|---------|----------|-------|--------|
| PD-1 | **`total_contributors: 0` in /knowledge/stats** despite 29 learnings from one wallet. Contributor counting logic broken. | P1 | BUILD-2 | DONE — root cause: counted earnings entries not contributor wallets. Now uses Set on contributor_wallet from learnings array. |
| PD-2 | **agent.json version stale (0.3.0 vs 0.7.0)** — `.well-known/agent.json` shows old version. Must match server. | P1 | BUILD-2 | DONE — version bumped to 0.7.0, stale pricing min also fixed ($0.005→$0.05). |
| PD-3 | **agent.json `base_url` points to `api.auxilo.io`** — may not resolve to the legacy VM host. Verify DNS/proxy or update URL. | P1 | BUILD-2 | DONE — `api.auxilo.io` resolves correctly via Cloudflare (172.67.153.104), /health returns 200. No change needed. |
| PD-4 | **GET /discover returns 404** — endpoint is POST-only but agents expect GET. Return 405 with `Allow: POST` header. | P2 | BUILD-2 | DONE — GET handler returns 405 with Allow header and helpful JSON message. |
| PD-5 | **OFAC SDN list refresh failing** — startup logs show 5 consecutive 403s on SDN download. Stale sanctions list >48h. | P1 | GOV-3 | DONE — URLs updated to new Treasury endpoint, User-Agent header added, multi-hop redirect support (up to 5). Shared `_fetchWithRedirects()` helper. |
| PD-6 | **Platform wallet ETH balance: 0.000000** — on-chain payouts will fail. Fund wallet or disable on-chain withdrawal. | P1 | EXEC-1 | DEFERRED — SPEC-1 audit: wallet has 40.69 USDC, 0 ETH. Fails gracefully (503, no fund loss). Stripe withdrawals work. Fund 0.01 ETH (~$0.03) when on-chain payouts needed. |

> **Lifecycle assessment**: Full agent lifecycle (search → preview → unlock → rate → contribute) is FUNCTIONAL. No blockers in the core chain.

### 18b. Wave-3 client Gate-A reviewer findings (N-series, 2026-07-19) — closed by Wave 5C

Source: wave-3 client Gate-A adversarial review (`wave3-client-funnel`, merged f6282fd). Rows recorded here at closure — spec/design of record: `~/.auxilo/handoffs/BUILD-SPEC-WAVE5C-2026-07-19.md` (branch `wave5c-client-closures`).

| # | Finding | Priority | Owner | Status |
|---|---------|----------|-------|--------|
| N1 | **Adapter read-size cap missing** — `discoverSessions()` returns `bytes` per the §4.1 contract but `readSession()` read unbounded (`fs.readFileSync`; 54MB proven in the wild) — a multi-GB transcript OOMs a constrained sweeper node, all to feed a pipeline that truncates to 30k chars. | P1 | BUILD-2 | ✅ **BUILT 2026-07-19** (`wave5c-client-closures`, npm-0.9.4 rider). Cap lives on the BASE adapter path — concrete `TranscriptSource.readSessionCapped()` (scripts/sources/source.interface.js): fresh `statSync` size (else `sessionRef.bytes`), over-cap throws `SessionTooLargeError` (`code SESSION_TOO_LARGE`) BEFORE any byte is read; ALL adapters incl. generic-jsonl fallback + future UC-3 self-registered ones inherit it (no per-adapter copies; no-shadowing + no-bare-`readSession`-call-site closure guards test-pinned). Runner (both call sites: sweep + `--transcript`): oversize = counted SKIP with stderr line (`oversize_skipped=N`) + ledger-marked `'oversize'` (probe-refused idiom), never a failure; single-file mode short-circuits BEFORE the raw readFileSync fallback (which would have been the same OOM). **Cap = 64MB default** (54MB proven-in-the-wild extracted successfully — a lower cap silently regresses real coverage; 64MB bounds worst-case RSS at low-hundreds-MB, multi-GB structurally refused), env-tunable `AUXILO_MAX_SESSION_BYTES` (positive int; garbage → default). Garbage-fixture + sweep-e2e + env-resolution + closure-guard tests: `test/wave5c-client-closures.test.js` N1-1…N1-5. |
| N3 | **Consent-gate had NO executable coverage** — reviewer proved flipping `extractionArmed = false → true` (bin/auxilo-cli.js cmdSetup ~:231) AND deleting the UC-1a else-cleanup branch each shipped silently: prior coverage was a string-position check + removal-helper unit tests; nothing executed the branch. Consent is the legal spine (UC-1a/GOV-3; agent-cannot-enroll doctrine). | P0 | BUILD-2 | ✅ **BUILT 2026-07-19** (`wave5c-client-closures`). Integration test drives the REAL CLI as a subprocess (`test/wave5c-client-closures.test.js` N3): fixture HOME (claude-code + cursor detected, pre-authed creds), scripted PROMPT DRIVER (answers written only when each prompt appears — up-front piped answers get dropped by readline, documented in the harness), local HTTP server playing `POST /extract/consent` in three modes: **(a)** 200 → sentinel + SessionEnd hook + cursor shim exist, exactly 1 grant call with the right key; **(b)** 500 → loud stderr, NOT armed, zero capture artifacts, pre-seeded STALE hook/shim artifacts REMOVED; **(c)** decline → ZERO consent calls + same cleanup asserts. MCP registration + SessionStart notice pinned consent-independent in all three. **Mutation-verified (apply→fail→revert→prove): the `extractionArmed` flip kills (b)+(c) (10 tests: 7 pass/2 fail, exit 1); deleting the else-cleanup branch kills (b)+(c) (7/2, exit 1); reverted tree green (exit 0).** |
| N4 | **UC-5 spec pre-R2 gate items** — (a) `auxilo_accept_terms` was "Qualified yes" for remote (in-chat assent unresolved); (b) OAuth dynamic client registration had no abuse limits (anonymous store-growth DoS + phishing-client mint; D2-F2 class). | P1 | BUILD-2 + GOV-2/GOV-3 | ✅ **RESOLVED BY AMENDMENT 2026-07-19** — `~/.auxilo/handoffs/SPEC-UC5-REMOTE-MCP-2026-07-19.md` amended in place (banner + amendment record): **A1** `auxilo_accept_terms` FIRMLY link-out — an agent tool-call must never constitute clickwrap assent (SPEC3-C1 agent-cannot-enroll + GOV-3 'evidentiary not preventive' doctrine bind a fortiori remotely); tool returns terms status + dashboard accept URL only, R2 tool table must expose no assent-capable parameter; former open decision §8.1 CLOSED. **A2** R3 acceptance now requires DCR abuse limits (§6.7): per-IP + global rate limit, client-store cap + never-authorized TTL eviction, DCR records inert until a real account authorizes, strict redirect-URI validation. Both marked pre-R2 gate items resolved. |

> Only N1/N3/N4 were assigned to Wave 5C; any other N-series rows from that review are tracked by the orchestrator, not here.

---

## 19. Post-Deploy 5-Agent Audit (2026-03-30)

Source: 5-agent audit against live API after Wave 1 deploy. Top 5 findings addressed; remaining deferred to backlog.

| # | Finding | Priority | Owner | Status |
|---|---------|----------|-------|--------|
| BUILD-2a | **JSON 404 catch-all + security headers** — JSON 404 at line 5937, security headers middleware at line 84 of server.js. | P1 | BUILD-2 | DONE — deployed and verified live. |
| BUILD-2b | **Unauthenticated IP rate limiting on /knowledge and /discover** — `isSearchUnauthRateLimited()` at line 3544, applied at line 2344. 30 req/min per IP for unauth callers. | P1 | BUILD-2 | DONE — deployed and verified live. |
| BUILD-2c | **Batch validation on POST /learn** — `validationErrors` array at lines 3596-3623, returns all errors + `expected_fields` schema in single response. | P1 | BUILD-2 | DONE — deployed and verified live (POST /learn with `{}` returns errors array). |
| BUILD-2d | **Template tool names + URLs** — Fixed `auxilo_search_knowledge` → `auxilo_knowledge`, `auxilo_submit_learning` → `auxilo_contribute`, `auxilo_list_categories` → `auxilo_categories`. Replaced `auxilo.example.com` → `api.auxilo.io` across all 6 template files. | P1 | BUILD-2 | DONE — deployed to VM. |
| H-3 | **OFAC health visibility** — add monitoring/alerting for OFAC list fetch failures (currently fails silently). | P1 | GOV-3 | ✅ **BUILT 2026-07-19** on `wave2b-ops-hardening` (spec `~/.auxilo/handoffs/BUILD-SPEC-WAVE2B-2026-07-19.md` §1). `refreshOFACList()`'s failure catch now fires `sendOpsAlert` (category `ofac`, never-throws) on EVERY failed refresh — cadence is 24h so one failure already = list >24h stale (WARNING subject), ≥2 consecutive = >48h (CRITICAL subject, the AML-PROGRAM line); body carries error, failure count, last-success timestamp, list size, and names the fail-closed 503 state when the list has never loaded (boot failure). NOT deployed — rides the Wave-2b merge. |

> Remaining audit items not in top 5: deferred to backlog. Will be triaged in next sprint planning.

---

## 20. Launch Wave — Readiness Blockers (2026-06-10)

Source: Launch-readiness review 2026-06-10. Spec: `specs/BUILD-SPEC-LAUNCH-WAVE.md` (per-item scope, pre-written test cases, Gate B triggers, doc impact, sequencing).

| # | Finding | Priority | Owner | Status |
|---|---------|----------|-------|--------|
> **2026-07-19 reconcile:** every LW row below whose status says built-but-"NOT deployed"/"NOT published" (LW-2, LW-6, LW-13, LW-14, LW-15, LW-16) HAS long been deployed and published — prod == main tip via the v34–v38 chain and npm auxilo-mcp@0.9.2. Those inline statuses are historical build-time notes.

| LW-1 | **Magic-link email delivery** — lib/accounts.js:485 only console-logs the link; onboarding broken in prod. Wire Resend (lib/email.js, RESEND_API_KEY secret, neutral-response failure handling, no token in prod logs). | **P0** | BUILD-1 → BUILD-2 | ✅ **DONE — CLOSED end-to-end (verified 2026-07-20)**: code long-deployed via the v34+ prod==main chain, and `RESEND_API_KEY` + `OPS_ALERT_EMAIL` confirmed present in prod Fly secrets (the stale "Tyler must provision" clause is cured — ops-alert emails ride the same key). |
| LW-2 | **Content moderation default-on** — server.js:151 defaults OFF and var unset in ecosystem.config.js/fly.toml/Dockerfile. Flip to opt-out (`!== 'false'`), set explicitly in all deploy configs, verify prod env post-deploy. | **P0** | BUILD-2 | **DONE (built 2026-06-10, tests pass — NOT yet deployed; post-deploy env verification T-LW2-06/07 pending)** |
| LW-3 | **Prompt-injection defense for served learnings** — GET /knowledge/:id returns contributor content verbatim. (a) Untrusted-content envelope (`content` + `content_advisory`) in unlock responses + openapi.json + MCP + guides; (b) injection-pattern screening (`lib/injection-screen.js`, flag-don't-block) in moderation pipeline. Depends on LW-2. | **P0** | BUILD-1 → BUILD-2 | ✅ **DONE** (part (b) via LW-13, 2026-06-10; part (a) envelope verified in code 2026-07-19 per AUD19-14: `UNTRUSTED_CONTENT_ADVISORY` const server.js:43 + `content_advisory` in the unlock response server.js:6751, MCP fenced BEGIN/END-UNTRUSTED wrap mcp-server.js:40-55, `content_advisory` in openapi `LearningFull`) |
| LW-4 | **Off-VM backups** — backups live on same VM disk + fragile Gist upload. rclone to Cloudflare R2 every 6h, 30d daily/12w weekly retention, local-machine weekly pull, mandatory restore drill, failure heartbeat → LW-5 alerting. | **P0** | BUILD-2 + GOV-3 | ✅ **DONE — LIVE-VERIFIED 2026-07-20**: merged + deployed via the wave chain; staged Tigris secrets ACTIVATED (AWS_*/BUCKET_NAME/BACKUP_ENCRYPTION_KEY confirmed in prod Fly secrets); proof of life = uptime.yml `backup-freshness` job green in scheduled runs today (marker <8h ⇒ the 6h on-box runner is writing encrypted backups to the off-VM bucket). Build record: **BUILT 2026-07-19** — Fly-native Tigris (not R2; no new vendor): org-level bucket `auxilo-backups` provisioned; on-box `scripts/backup-runner.js` (6h, streamed tar → AES-256-GCM → S3, HEAD-verified, 30d daily/12w weekly prune, freshness marker) launched crash-isolated from `docker-entrypoint.sh` via `backup-supervisor.sh`; creds + encryption key STAGED as Fly secrets (`--stage`, zero restart — activate at next deploy); scripted restore drill (`scripts/backup-restore-drill.js`) RUN against a real uploaded prod-data backup 2026-07-19: decrypt/extract/row-counts PASS (one pre-existing prod wart found: `settlements.jsonl` = literal `{ bad json` line, see RUNBOOK §9). Spec `~/.auxilo/handoffs/BUILD-SPEC-WAVE2A-backups-2026-07-19.md`; 23 unit tests in `test/backup-runner.test.js`. launchd laptop pull retained as secondary (RUNBOOK §4). |
| LW-5 | **External monitoring + alerting** — UptimeRobot on auxilo.io + api.auxilo.io /health, enable HEALTH_WEBHOOK_URL in ecosystem.config.js, healthchecks.io heartbeats for backup/pricing crons. Also closes H-3 (OFAC fetch alerting, §19). Urgency proven 2026-07-19: api.auxilo.io had NO TLS cert for an unknown period (cold `npx auxilo setup` hard-down — AUD19-1, §26) and nothing watched the host every published artifact hardcodes. | P1 | BUILD-2 + GOV-3 | ✅ **DONE 2026-07-20** — every real fragment closed: external uptime + TLS-cert monitor ACTIVE (uptime.yml, both hosts, 30-min); backup-freshness job ACTIVE and **green in live runs 2026-07-20** (proves the on-box Tigris backup runner is running against prod — also closes LW-4's activation question); H-3 OFAC alerting live (Wave 2b); **pricing-cron heartbeat closed 2026-07-20** (main `c076021`): cron failure now fires `sendOpsAlert` category `pricing-cron` (24h cadence = one failure is already a >24h-stale shelf), RESEND_API_KEY + OPS_ALERT_EMAIL confirmed provisioned on prod. HEALTH_WEBHOOK_URL fragment ruled STALE: identifier appears nowhere in the codebase (PM2/ecosystem-era scope; ecosystem.config.js superseded by Fly) — superseded by uptime.yml + sendOpsAlert, no build owed. |
| LW-6 | **npm audit fix** — 1 high (fast-uri), 6 moderate (ws via viem). Fix via audit fix/overrides; SPEC-1 sign-off if viem moves; full regression + live smoke. Run FIRST among code items (clean baseline). | P1 | BUILD-2 + BUILD-4 | **DONE (built 2026-06-10: 0 vulnerabilities post-fix, lockfile-only, viem untouched, full suite green — NOT yet deployed; live smoke T-LW6-04 pending)** |
| LW-7 | **Rating endpoint hardening** — POST /knowledge/:id/rate (server.js:5066) unauthenticated. Require auth + proof of prior unlock (purchaser-side unlocks ledger), keep cooldowns keyed by account. Ships WITH LW-3 as one breaking-change window. | P1 | BUILD-1 → BUILD-2 | ✅ **BUILT 2026-07-19** on `wave1-money-closures` (spec `~/.auxilo/handoffs/BUILD-SPEC-WAVE1-2026-07-19.md` §1.3). Route now `requireSessionOrApiKey('read')` + proof-of-purchase gate (403 `UNLOCK_REQUIRED_TO_RATE`); eligibility store = NEW durable `lib/purchase-ledger.js` (`data/purchase-ledger.json`, never pruned — deliberately separate from the 30-day-pruned accrual-cap store), written at delivery success on the main + capped-repeat unlock paths, NEVER on self-unlock (contributors cannot rate their own learnings) and after the WAL commit (a refunded AUD19-10 failure mints no rating rights). Cooldown re-keyed IP→account; ratings JSONL gains `rater_account_id`. Breaking change accepted (anonymous x402 buyers can't rate; pre-ledger unlocks not grandfathered — zero real external users; LW-3's window already shipped). MCP `auxilo_rate` already sends the API key — description updated to state the contract (**rides npm 0.9.3**). openapi/ERROR-CODES surface lands with the next AUD19-13 propagate. Tests in `test/wave1-money-closures.test.js`; NOT deployed. |
| LW-8 | **Copy/doc reconciliation** — (a) $1.00 ceiling in superseded root AGENT-LEARNING-GUIDE.md:228 vs canonical $50.00 (MARKETPLACE.md:63, lib/pricing.js:21) → fix to $50, add `maximum: 50` to openapi unlock_price; (b) "no minimums" copy vs $0.50 Stripe withdrawal minimum (MARKETPLACE.md:248, STRIPE-TERMS-REVIEW.md) → qualify copy ("no minimum to earn; $0.50 Stripe payout minimum; on-chain no minimum"), resolve P-038. | P1 | BUILD-3 + GOV-2 → CFO-1 sign-off | **PARTIAL (2026-07-19)** — (a) root AGENT-LEARNING-GUIDE ceiling fixed $1.00→$50.00 + floor/default line corrected; (b) live copy already rewritten earn-side ('no minimum before you start earning' — accurate); withdrawal-minimum disclosure lands with unpause copy (Marketing copy thread) |
| LW-9 | **Legal/ops paperwork** (non-code): (a) legal contact info in ToS/Privacy, (b) DMCA agent registration per docs/DMCA-REGISTRATION-GUIDE.md, (c) adopt incident-response plan from OPS-READINESS-REVIEW into RUNBOOK, (d) live support channel wired + linked. | P1 | GOV-2 + GOV-1 + SPEC-3 + Tyler | **PARTIAL — LEGAL-ONLY remainder** — (b) DMCA CLOSED 2026-07-01; **(c) CLOSED 2026-07-20**: incident-response plan adopted as RUNBOOK §11 (severity tiers, first-15-min checklist, kill-switch lever table, data-incident branch carrying the 2026-07-01 leak lessons) + §12 changelog; OPS-READINESS-REVIEW §2 banner-marked adopted; (d) support channel live on /status (support@ + security@ + response targets). Remaining: **(a) legal contact info in ToS/Privacy — counsel/Tyler (LEGAL bucket)**. |
| LW-12 | **Turnkey installer (`npx auxilo setup`)** — onboarding required repo clone + manual hook install + hand-POSTed consent (~0% real-user completion). New `auxilo` bin (`setup`/`status`/`disable`, bin/auxilo-cli.js) + testable core `lib/installer.js` (explicit homeDir, injectable fetch): client detection + MCP registration (Claude Code, Claude Desktop, Cursor, OpenClaw), device-code auth → `~/.auxilo/credentials.json` 0600, runner stack packaged into npm tarball + installed to `~/.auxilo/bin` (P1-13 layout), SessionEnd hook generated + registered (replaces legacy `~/.claude/hooks/` entry), consent default-No (sentinel only on grant). Package 0.7.0 → 0.8.0; legacy `auxilo-mcp setup/login` print pointers. Also fixed: generated hook must NOT set AUXILO_EXTRACTING=1 on spawn (trips runner's recursion guard → silent no-op; same class as P1-13 plist bug). Spec: BUILD-SPEC-LAUNCH-WAVE §LW-12. | **P0** | BUILD-2 | **DONE (built 2026-06-10, full suite green incl. 31 new installer tests — NOT published to npm, NOT deployed; `npm publish` auxilo-mcp@0.8.0 is Tyler/release-owner action)** |
| LW-13 | **Extraction moderation parity + auto-approve valve** — POST /extract set `status='approved'` directly, bypassing moderation that POST /learn submissions go through (inverted). Now both paths respect `CONTENT_MODERATION_ENABLED`. Adds `MODERATION_AUTO_APPROVE` throughput valve (opt-in, default false; requires sensitivity+injection+dedup clean + quality score) with `moderation: 'auto'/'manual'` audit field; pulls forward LW-3(b) `lib/injection-screen.js` (flag-don't-block, fail-closed). | **P0** | BUILD-2 | **DONE (built 2026-06-10, full suite green — NOT deployed)** |
| LW-14 | **Near-duplicate detection at submission** — /learn deduped only on exact normalized body hash; /extract computed body_hash but never compared it. New `lib/similarity.js` (3-token shingle Jaccard, same-category, O(catalog)): ≥0.85 → 409 with existing_id; 0.60–0.85 → pending_review with `possible_duplicate_of`. Applied identically on both paths incl. /extract intra-batch. Scaling ceiling noted in spec (~5k entries → index/MinHash). | P1 | BUILD-2 | **DONE (built 2026-06-10, full suite green — NOT deployed)** |
| LW-15 | **Contributor self-review queue** — the durable fix for the incident where private learnings auto-published. After LW-13, extraction lands `pending_review`, but the only way to clear it was the platform-admin queue (`/admin/moderation/*`, wrong trust boundary). Three account-scoped, API-key-authenticated endpoints (`GET /account/pending`, `POST /account/pending/:id/approve|reject` in server.js) let the CONTRIBUTOR review/approve/reject ONLY their OWN pending learnings — every read/mutation guarded by `contributor_account_id === caller's accountId` (pure ownership-isolation core in `lib/self-review.js`; ownership precedes pending-status so a non-owned id always returns 403, no cross-account state leak). New `auxilo review` CLI subcommand (bin/auxilo-cli.js + `lib/review.js`): interactive approve/reject/skip/quit per candidate (full body + injection/near-dup safety flags shown), plus `--list` and `--all-reject [--yes]`. Makes background extraction safe to re-enable: `extract → pending_review → auxilo review → approve safe ones`. Package 0.8.0 → 0.8.1 (adds `lib/review.js` to published `files`). Spec: BUILD-SPEC-LAUNCH-WAVE §LW-15. **Attribution fix (2026-06-10, blocks LW-16 deploy)**: POST /learn with a valid API key + `contributor_wallet` created the learning with `contributor_account_id = null` (wallet-only attribution) → held items were INVISIBLE in the contributor's own `GET /account/pending` and stuck in `pending_review` (22 live learnings hit this; cleared by direct data-file edit). /learn now ALWAYS binds `contributor_account_id` from the validated key's account — wallet is payout attribution, account is ownership; they are independent and both set. JWT session fills ownership only when no valid key is presented. Side effect: API-key-only submissions (no wallet, no JWT) now pass the identity gate. Regression tests T-LW15-17/18. Possible follow-up (not built): `GET /account/pending` fallback match on the account's linked wallet, for pre-fix wallet-only orphans. | **P0** | BUILD-1 → BUILD-2 | **DONE (built 2026-06-10, full suite green incl. 23 new tests; ownership isolation tested T-LW15-06/09 — NOT published to npm, NOT deployed)** |
| LW-16 | **Sensitivity-gated seamless publishing (two-layer)** — inverts the LW-13 "everything → pending_review" default. With moderation on, a fully-clean submission now publishes `approved` immediately (seamless); review is the exception triggered by a flag. **Layer 1 — regex** `lib/content-sensitivity.js` (`classifySensitivity`) catches confidential CONTENT the credential/injection/dedup screens miss — person/client/brand names, emails, private home paths, account/wallet ids, first-person proprietary context. **Measured regex recall on the 374 retracted-confidential learnings = 77.5% (misses 82** "proprietary-context phrased generically" items with no literal token). **Layer 2 — LLM** `lib/content-sensitivity-llm.js` (`classifySensitivityLLM`, `claude-haiku-4-5`, 6s hard timeout, fail-closed on timeout/error/missing-key) is a strict confidentiality classifier that catches the misses regex can't see. Combined as a **UNION** (`regex.sensitive \|\| llm.sensitive`) via `evaluateContentSensitivity` in server.js, awaited on /learn + /extract; **short-circuit** skips the LLM when regex flags, but regex-clean MUST consult the LLM before auto-approve. `sensitivity_source` (`regex`/`llm`/`both`) recorded alongside `sensitivity_signals`; both stripped from buyer projections. New `LLM_SENSITIVITY_ENABLED` toggle (default true; `false` → regex-only, NOT leak-safe). Curated allowlist keeps all 22 `outputs/SEED-VERIFIED.json` seeds seamless. /extract honors extractor verdict as a raise-only UNION. `FORCE_ALL_REVIEW` kill switch (opt-in, default false). `MODERATION_AUTO_APPROVE` retired (no-op). Recall harness `test/scripts/lw16-recall-eval.js` (refuses to fabricate; `--self-test` verifies wiring + 374/290/82 reconciliation without a key). Package 0.8.1 → 0.8.2. Spec: BUILD-SPEC-LAUNCH-WAVE §LW-16 / §LW-16a. | **P0** | BUILD-1/BUILD-2 + GOV-3 | **BUILT (2026-06-10): two-layer gate + combiner + toggle + fail-closed wired; 21 new mocked-LLM unit tests pass; full suite green except 4 pre-existing plist env failures; all 22 seeds clean under combined classifier (self-test). NOT published, NOT deployed. ✅ LIVE recall MEASURED 2026-06-11 on the prod box (live `ANTHROPIC_API_KEY` Fly secret, 374 real retracted-confidential bodies): **combined recall 98.7% (369/374), 0/22 seed false-positives, LLM caught 79/82 regex-misses**. The 2–3 residual are generic techniques over-labeled confidential by session-origin (one is a published seed) — true leak rate effectively near-zero. DEPLOY-CLEARED; recommend deploying with the security-audit fixes as one wave. Evidence: `outputs/LW16-recall-combined-20260611.md`.** |
| LW-10 | **Legacy tests/ cleanup** — test-mcp-regression.js dead legacy-host BASE_URL + stale v0.5.0 assertion → localhost default + version from package.json; fix or formally retire hanging test-a0-unit.js / test-eip712-negative.js; add tests/README.md. Runs LAST (after LW-3/LW-7 contract changes). | P1 | BUILD-4 | ✅ **DONE 2026-07-19** (`wave3-client-funnel`) — reality reconciled: test-mcp-regression.js and test-eip712-negative.js NO LONGER EXIST (lost in the 2026-07-01 repo reconciliation; their named fixes are moot). Every surviving tests/ file measured under a watchdog: test-a0-unit.js confirmed HANGING (after T-A0-UNIT-007, proxyquire viem mock) → RETIRED in place with banner; a1/a2-adversarial/a4 carry stale source-grep assertions; a3-integration/a3-sec need a live server; rest pass. Full inventory + run contract in tests/README.md. Nothing in tests/ is wired into `npm test`. |
| LW-17 | **Install-path verification fixes (end-to-end "install once and earn" proof)** — 2026-06-11 verification of `npx auxilo setup` → autonomous extraction → attributed learning found and fixed FIVE breaks: (1) **server** `POST /extract/consent` was session-JWT-only (`requireAuth`) — the CLI's API-key grant always 401'd, so NO installer user could ever enable extraction; new `requireSessionOrApiKey(minScope)` middleware (session OR API key, scope-gated per GOV-3 review: consent needs `contribute`, settings-read needs `read`; suspended accounts rejected). (2) **server** `GET /account/settings` did not exist though `auxilo status` has queried it since 0.8.1 (mode always "unknown"). (3) **installer** registered the SessionEnd hook as a bare STRING — Claude Code silently ignores string entries (verified empirically: string never fired, structured form fired immediately); also only removed string legacy entries, missing the pre-LW-12 structured `~/.claude/hooks/` entry. Now writes structured form + removes all legacy shapes preserving co-grouped non-Auxilo commands. (4) **prod-killer**: `model_config.json` shipped in the image as 0600 root:root (Docker COPY preserves local modes; server runs de-privileged as `node`) → startup config read EACCES'd, silently degraded to `{}`, every /extract 200'd `published=0` while all chunks threw `undefined (reading 'provider')`. chmod 644 + loud loader logging + Dockerfile `chmod a+r /app/*.json` guard. (5) **CLI** per-question readline interfaces swallowed buffered piped stdin (consent step hung on EOF in scripted runs) → one shared interface. Plus: `npx auxilo-mcp setup|status|review|disable` now delegates to the turnkey CLI (npx resolves PACKAGE names, not bin aliases — documented `npx auxilo setup` 404s until the `auxilo` npm name is claimed), and the MCP server's credential-less default base URL was a dead legacy sandbox → auxilo.io. **E2E PROVEN 2026-06-11**: real `claude -p` session end → hook → runner → POST /extract → 10 learnings extracted, all held `pending_review` by LW-16 (signals: unknown_proper_noun/person_name/private_path/llm_semantic), `contributor_account_id=acc_647b846b`, `extraction_id` matches runner log, zero manual steps. Digest LaunchAgent re-enabled TCC-safely (decision: RUN — daily "is it firing" signal); retraction-sweeper formally retired. | **P0** | BUILD-2 + GOV-3 | **DONE (server fixes DEPLOYED to Fly 2026-06-11; auxilo-mcp@0.8.2 PUBLISHED + `auxilo` wrapper package PUBLISHED/name claimed 2026-06-11 via Tyler-approved 7-day token, token deleted after use; `npx auxilo setup` verified working cold from the public registry — site copy already correct)** |
| LW-18 | **In-session review surface (seamless pending-review)** — LW-15's CLI-only review queue is invisible (verified: 10 learnings sat unnoticed in Tyler's queue 2026-06-11); "user must run a command to discover a list" is a product fail. Three layers: (1) **agent-assisted in-chat review** — MCP tools `auxilo_pending_list` (untrusted-content envelope per LW-3a, full body + safety flags) + `auxilo_pending_decide` (explicit per-item human confirmation REQUIRED; refuses approve on injection-flagged items without human override) riding the existing LW-15 endpoints, plus a SessionStart hook notice ("N learnings held for your review") installed by `auxilo setup` — structured entry per LW-17, fail-silent, 4h suppression; (2) **local macOS notification** from the runner when extraction holds items (count only, no content); (3) digest email line + magic-link web review page deferred to LW-18b. Core threat model: pending bodies are adversarial by assumption (some held BECAUSE injection-flagged) — surfacing them to a live agent creates a confused-deputy path to self-approval; envelope + human-confirmation contract + server-side flag refusal are all REQUIRED. Gate is never weakened (no auto-approve/auto-drain). Spec: BUILD-SPEC-LAUNCH-WAVE §LW-18 (11 pre-written test cases). | P1 | BUILD-2 + GOV-3 + SPEC-2/SPEC-3 | **LARGELY SHIPPED (2026-07-19 reconcile)** — layer-1 in-chat review EXISTS as the confirm-gated `auxilo_review` MCP tool (mcp-server.js:392, published in 0.9.2; approve_clean requires explicit operator confirmation + expected_count — gate never weakened). Shipped shape (single tool) supersedes the spec's pending_list/decide pair. Remaining layers ✅ **BUILT 2026-07-19** (`wave3-client-funnel`, rides npm 0.9.3): SessionStart held-count notice — scripts/review-notice.js + installer registerClaudeCodeSessionStartNotice (structured LW-17 entry, fail-silent, ≤1 notice/4h, COUNT-ONLY per the adversarial-pending-bodies threat model) wired into `auxilo setup`; local macOS notification — runner.js notifyHeld() (osascript, count-only, fail-silent, fires when a run holds items). Still open: LW-18b digest email line is now covered by the A2 lane digest (P1-13b row); magic-link web review page remains deferred |

> **Launch gate**: LW-1–LW-4 DONE + Gate A/B sign-offs → launch unblocked. LW-7 and LW-3 must deploy together (single breaking API change to /knowledge/:id + MCP client release).

> **INCIDENT 2026-06-10 — stash sweep of untracked docs (RESOLVED)**: a `git stash -u` swept all untracked files (205, including most of `docs/` per repo convention); the stash was later dropped, then recovered from dangling commit `511b3435` and pinned as tag **`recovered-stash-20260610`**. 192 missing files were restored verbatim from the tag. 13 files already existed in the working tree and were not overwritten; GOV-1 reconciled the 10 that differed: originals restored from the tag with this session's LW-12/LW-13/LW-14 content re-applied and "recreated" banners removed (`CHANGELOG.md`, `docs/DEVELOPER-GUIDE.md`, `docs/UX-FLOWS.md`, `docs/SUPPORTED-CLIENTS.md`); newer working versions kept (`docs/MARKETPLACE.md` = original + LW-13/14 moderation table; `bin/auxilo-cli.js`, `lib/installer.js`, `scripts/hooks/auxilo-extract.sh`, `test/lw12-installer.test.js`, `test/similarity.test.js` = post-snapshot LW-12/14 commits). Load-bearing docs verified complete: `docs/TERMS-OF-SERVICE.md` and `docs/PRIVACY-POLICY.md` byte-identical to originals; `docs/RUNBOOK.md` intact including the LW-1 Resend section (RESEND_API_KEY/EMAIL_FROM, DNS TXT/DKIM steps, referenced from DEPLOY-GUIDE.md). Full regression suite re-run post-reconciliation. Keep the recovery tag until the next off-repo backup cycle confirms.

---


## 21. Universal Client Support (UC) — every LLM / vibe-coding app / agent (2026-06-12)

Source: Tyler directive 2026-06-11 ("ensure our system works in every other LLM and vibe coding app, even agents like OpenClaw") + 3-agent research pass (repo baseline, IDE landscape, CLI/agent landscape — all web-verified June 2026). Spec: `specs/BUILD-SPEC-UNIVERSAL-CLIENTS.md` (capture-strategy classes H/G/P/R/T, client→class matrix, per-phase tests, consent requirements).

Key research findings: MCP is now universal (Linux Foundation; every surveyed client supports stdio MCP — registration is a config-writer problem). Claude Code's hook contract became the de-facto industry standard (Codex CLI, Gemini CLI, Cursor IDE+CLI, VS Code Copilot, Windsurf, Antigravity, Factory droid all ship session-end-class JSON-over-stdin hooks, several handing transcript_path directly) — our existing integration ports with shims, not rewrites. No transcript interchange standard exists (OTel GenAI = pre-stable telemetry schema; ACP = wire protocol, strategic later bet). Current reality: only Claude Code (hook) + OpenClaw (poll, no MCP) have extraction; Claude Desktop/Cursor are MCP-only; SUPPORTED-CLIENTS.md overpromises.

| # | Finding | Priority | Owner | Status |
|---|---------|----------|-------|--------|
| UC-0 | **Truth + universal fallback** — rewrite docs/SUPPORTED-CLIENTS.md to the verified matrix (kills the hook-extraction overpromise); rules-file "agent-prompted contribution" snippets (opt-in per file) → every MCP client gets probabilistic capture day one; MCP config-writers for all Tier-1/2 clients (JSON + Codex TOML); server `agent-prompted` provenance tag. | P1 | BUILD-2 + GOV-1 | **DONE (auxilo-mcp@0.9.0 published 2026-06-12; 16 clients detected, MCP writers for 15)** |
| UC-1 | **Generic hook engine** — extract Claude Code hook into shared `auxilo-capture` core; per-client hook-config writers (Cursor, Windsurf, VS Code Copilot, Antigravity, Codex, Gemini, Factory — structured entries per LW-17); per-client transcript normalizers as TranscriptSource subclasses; consent screen names wired clients (GOV-3); server source_allowlist growth. Covers ~70% of agent-IDE share with one abstraction. | P1 | BUILD-1 → BUILD-2 + GOV-3 | **DONE (0.9.0; capture hooks for 8 clients incl. Claude Code; GOV-3 M1-M4 fixed; Antigravity e2e PROVEN on Tyler's machine — attributed learning in pending_review)** |
| UC-2 | **Platform plugins** — OpenClaw plugin on `agent_end` (sessions are long-lived; pruning risk) published via clawhub registry (also GTM channel — GROWTH-1) + mcporter MCP bridge (closes OpenClaw MCP gap), visibly consent-gated; opencode npm plugin (`session.idle`); Goose extension iff hook verification passes (currently secondary-source only). | P1 | BUILD-2 + GOV-3 + GROWTH-1 | OPEN |
| UC-3 | **Poll adapters** — Cline, Roo, Continue, OpenHands, Copilot CLI, Aider, Crush as TranscriptSource adapters; dynamic SOURCES registry; re-enable sweeper as P-class engine; every adapter version-pinned format probe + fail-silent degradation (paths are reverse-engineered). Publicly labeled "best-effort". | P2 | BUILD-2 | **PARTIAL 2026-07-19** (`wave3-client-funnel`, rides npm 0.9.3) — BUILT: Cline (`cline`), Roo Code (`roo-code`), Continue (`continue`) adapters (strict format probe + fail-silent per UC §5; NOT locally verified — no installs on the build machine, documented in each header, gemini-cli precedent) + dynamic SOURCES registry (runner.js loadSources() enumerates scripts/sources/*.js; adapters self-register; broken adapter = silent skip) + manifest closure made structural (RUNNER_STACK and sweeperManifest() both DERIVE sources rows by dir enumeration — the d8c7099 bug class is impossible; closure test in test/wave3-client-funnel.test.js). SKIPPED (not locally verifiable): OpenHands, Aider (markdown history — different parse class), Copilot CLI (generic-jsonl fallback already covers hook mode), Crush. Sweeper re-enable as P-class engine still open. |
| UC-4 | **Strategic bets** — OTel GenAI collector exporter for API-built agents (LangGraph/CrewAI/etc., version-pin experimental semconv); ACP observer/proxy when registry matures (Zed/JetBrains/Kiro); Devin API poller on demand. | P2 | BUILD-1 | OPEN |
| UC-5 | **Web LLM support (remote MCP connector)** — Tyler input 2026-06-12: most of his LLM use is web (claude.ai, ChatGPT, Gemini web), where NO local capture class applies (no filesystem/hooks/poll). Only viable path: host the Auxilo MCP server as a REMOTE connector (HTTP/SSE transport at e.g. mcp.auxilo.io, account-auth via API key or OAuth) — Claude.ai and ChatGPT both support custom MCP connectors as of 2026. Gets agent-prompted contribution (R-class) in the browser: the assistant calls auxilo_contribute when it solves something; attribution to the one account. Honest framing: contribution-grade, not extraction-grade (no transcript sweep on web); consumer-chat content is lower learning-density + more personal-sensitive (LW-16 gate unchanged). Scope: remote transport wrapper around mcp-server.js tool definitions, hosted on the Fly app or sibling, per-account auth, connector setup docs per web client. | P2 | BUILD-1 → BUILD-2 + SPEC-2 + GOV-2/GOV-3 | **SPEC DELIVERED 2026-07-19** — `~/.auxilo/handoffs/SPEC-UC5-REMOTE-MCP-2026-07-19.md`: OAuth 2.1+PKCE over the existing account system (device-code reused as approve-page, NOT as connector handshake), remote tool subset (no wallet/withdraw v1), same-Fly-app `/mcp` route, honest capture story (R-class only, never claim extraction-grade), confused-deputy invariants carried to remote, 5 sized slices (R1–R5). Build not started. |
| UC-1a | **GOV-3 follow-ups (UC-1 capture surface)** — L1: capture hooks are written into 7 third-party client configs in setup step 4, BEFORE the consent prompt (step 5); inert without the sentinel but a boundary smell + uninstall surprise — write capture hooks only after consent, OR state in consent copy that dormant hooks are placed. L2: renderCaptureShim interpolates homeDir into a double-quoted exec without quote-escaping (self-targeted, breaks only on exotic home paths). Both LOW, deferred from the 0.9.0 ship. | P2 | BUILD-2 + GOV-3 | ✅ **FIXED 2026-07-19** (`wave3-client-funnel`, rides npm 0.9.3) — L1: setup step 4 no longer writes ANY capture hook; registerClaudeCodeHook + installCaptureHooks moved inside the consent=Yes branch (after recordConsent succeeds + sentinel); consent=No (or failed record) runs new removeClaudeCodeHook + removeCaptureHooks so no hook artifact remains, including pre-UC-1a leftovers; consent copy states the No-path guarantee. L2: renderCaptureShim (and the new notice shim) single-quote embedded paths with '\'' escaping. Marker-based rules-snippet discipline + idempotency preserved (test/wave3-client-funnel.test.js). `auxilo disable` semantics deliberately unchanged (sentinel stays the fast kill-switch). |
| UC-6 | **Codex Desktop capture — sweep adapter over the shared `~/.codex` rollout store** — the ChatGPT desktop app (Codex merge 2026-07-09) embeds the Codex CLI and writes full CLI-format rollout JSONL to `~/.codex/sessions/YYYY/MM/DD/` + `~/.codex/archived_sessions/` (verified on operator machine 2026-07-26: 247 rollouts, all `originator:"Codex Desktop"`, 37 in Auxilo build venues) but does NOT execute hooks.json on current builds (open upstream regression openai/codex#21639; empirically zero hook executions across all 247 rollouts) — so the builder venue is captured nowhere for any user. Build: (a) `scripts/sources/` poll adapter per UC-3 self-registration (dedicated rollout→transcript normalizer — generic-jsonl's probe false-accepts rollout lines; default filter `session_meta.thread_source=="user"`; mtime-quiescence end-detection ~30 min matching app-server unload; reuse sourceId `codex-cli`, record `originator` in metadata; capped read already guards multi-GB sessions, openai/codex#24948; normalizer EXCLUDES `world_state`/`base_instructions`/`reasoning` — privacy); (b) lift the claude-code-only gate in `scripts/extract-local.js` for the source id (client-LLM extraction unchanged — zero platform-side inference); (c) PM-owned later: gated re-test of the already-shipped codex Stop/SessionEnd hook path when #21639 resolves (SessionEnd landed CLI 0.145.0, 2026-07-21) — promotes Codex to hook-tier with no new code; (d) doc impact: SUPPORTED-CLIENTS.md Codex row → CLI + Desktop coverage (poll tier for Desktop) + close the cline/roo-code/continue doc gap; AGENT-LEARNING-GUIDE if client-listed. Capture only — sentinel → scrub → F2 → `submission_channel:'extraction'` → review lanes unchanged per §21 footer. D1 RESOLVED (Tyler 2026-07-27): "go with whatever needs to be done in order to do what we want to do - collect learnings from Codex for all users" — organic sweep is the path for all users; operator-machine organic-pipeline-only ruling stands (sweep IS the organic path for Codex); MCP auxilo_contribute stays the documented direct path for end users' agents, no operator exception. Research: `~/.auxilo/handoffs/RESEARCH-CODEX-DESKTOP-CAPTURE-2026-07-26.md`. | P1 | BUILD-1 → BUILD-2 + GOV-3 + SPEC-2 | **SPEC DELIVERED 2026-07-27** — `~/.auxilo/handoffs/BUILD-SPEC-UC6-CODEX-DESKTOP-CAPTURE-2026-07-27.md` (branch `codex/UC-6` + tests-first suite; rides npm 0.9.8). **QUEUED behind SPEC3-G1** (builder correctly stopped at the AGENTS.md one-build-at-a-time gate 2026-07-27; pre-flight live validation PASSED — 251 rollouts, first record always `session_meta`, spec §2 holds, unknown variants → skip-silently; base re-pinned to fresh `origin/main`-containing-`5bde21e` + rulings in spec §8 addendum incl. UC6-R1 tracked-docs clearance). Kickoff re-issues when the shared checkout is clean. **RECONCILED + FIRED 2026-07-28 (acting PM): §9 addendum re-pins base to the exact sha `c6d0750` (contains 5bde21e, containment-proof ritual + environment proof), moves the version bump BUILDER-side (0.9.9, five surfaces one commit — the CI-RED-0726 partial-bump lesson), corrects the Slice B anchor (extract-local.js:565 post-P2), guide canon (docs/ copy only), and the pin figure (1445); adapter ships automatically via the `scripts/sources/` dir entry in package.json files (verified). THE ACTIVE BUILD — handed for relay.** **Baseline STOP (builder, correct) RULED 2026-07-28: 2 failures at the pinned base (spec3-f1-account-vocab-rider + spec3-f1-neardup-runtime, both "server not listening in ~20s") = the ENVIRONMENT class, not a repo defect — PM re-ran both files directly at `c6d0750` same-machine: 2/2 + 3/3, 0 fail, ~2.5s each; CI green on the tree; third sighting of the class in 48h → tracked as FLAKE-0728. Baseline ACCEPTED via PM verification on record; build proceeds.** **Scope STOP #2 RULED 2026-07-28: `test/wave3-client-funnel.test.js` AUTHORIZED as an additional scoped file — its :594 seven-adapter registry hardcode is exactly the spec §4 T1 anticipated bump; the file was omitted from §6.4/§9.6 (spec authors' miss, both of us). Change limited to the expected-list addition + seven→eight wording. PM sweep confirms it is the ONLY adapter-count hardcode in the suite (packaging closure is dynamic). Interim verification noted: UC-6 suite 8/8, closure 6/6.** **DELIVERED (`6510bb0`) but GATE-A = REWORK 2026-07-28: the spec-mandated live operator probe (§6.2) caught what synthetic fixtures could not — of 265 real rollouts, 19 pass the user-thread probe and 18 of those THROW in the normalizer (`oneLine`, codex-cli.js:81): `JSON.stringify(undefined)` returns the VALUE undefined (no exception → catch never fires) → `.replace` on undefined; trigger = tool-call records missing `arguments`/text fields, i.e. exactly the tool-heavy build sessions UC-6 exists to capture (only 1/19 user threads survives). Violates the §3 fail-silent never-throw contract on real data. Secondary: per-file refusal logging (246 lines/sweep, direct-stderr). Rework: defensive stringifyValue (undefined/null → ''), top-level readSession try/catch → null per contract, synthetic regression fixture for the missing-arguments variant, refusal log → single summary count. PM re-probe gate: 0 throws across all 265, ≥15 user threads readable, leak grep clean on the largest.** **REWORK DELIVERED (`d4806e8`) + GATE-A SHIP + MERGED (`10318b0`) + DEPLOYED 2026-07-28:** re-probe PASSED — 0 throws/265, 19/19 user threads readable, leak verdict MATCH (transcript Mandate occurrences == legitimate user-message occurrences exactly; world_state ×45 AND the spec-unknown `compacted` type ×43 fully excluded — the §3 unknown-skip rule validated in the wild); refusal logging = one summary line; pin 1455; FLAKE-0728 pair recurred in builder env only, quarantine held. Deploy verified: /stats + agent.json 0.9.9, catalog 123 unchanged. **PUBLISHED + VENDORED 2026-07-28: auxilo-mcp@0.9.9 (Tyler; registry dist.shasum == reviewed tarball `dd1e22e879`, latest confirmed); vendored via installRunner() from the published package — hook byte-identical (env line restored), runner/extract-local/codex-cli adapter/similarity all byte-match published, status healthy. Codex Desktop capture is LIVE on the operator machine: next sweep processes the ~19 historical user threads organically (default kept — no ledger tuning, per organic-pipeline-only), then ongoing sessions. UC-6 CLOSED.** **FIRST PRODUCTION CODEX SWEEP 2026-07-28 (operator-invoked, ~10.5 min): 274 discovered across sources, 16 processed → 7 held for review; codex-cli refusal summary fired exactly once (244 refused — F-B verified in production); the 5 "failed" = scrub FAIL-CLOSED refusals (second-pass patterns persisted — content stayed local, the safety posture working); 6 oversize skips incl. 222MB/233MB rollouts (the anticipated multi-GB class, cap held); F2 CROSS-SOURCE dedup observed live — Codex-session re-statements of already-captured Claude-Code lessons dropped via prompt memory (incl. the A3 piped-stdin lesson and two FLAKE-0728-class sandbox lessons). Queue 18 pending post-sweep; ledger high-water set (future sweeps incremental).** **G1 PHASE 3 RUNBOOK DRAFTED 2026-07-28 (RUNBOOK §14, local docs): drain = batched `--flush-pending` with `AUXILO_CAPTURE_VISIBILITY=private` (mechanism verified in 0.9.9 source — postExtract is fully local: extractLocally + F2 stack + /learn; success deletes queue file = natural resume ledger; pacing via holding-dir batches, ~25/run; heavy F2 drops expected, survivors land private-destined for counted keep-private). SPEC3-G1 program: ALL deliverables complete.** (superseded note: publish/vendor — NOTE: first sweep post-vendor processes the ~19 historical Codex user threads (extraction on operator subscription, F2-deduped, pending_review as always).** **INDEPENDENT POST-MERGE REVIEW PASS 2026-08-02 (research-session PM, fresh eyes — builder never sole reviewer): UC-6 suite re-run 10/10 green, full pin check 1455 discovered/0 fail matches pin; five-surface 0.9.9 bump verified present + one-commit; §6.4+§9.6 allowlist clean (runner.js touch = the sanctioned F-B refusal-summary rework; wave3 touch = the ruled T1 bump); high-water/quiescence interplay proven structurally safe (marked mtimes are ≥quiescence-old, active sessions always newer — no leapfrog class); live ledger corroborates the record (13 captured shas / 256 refused / 6 oversize, daily sweeps running, high-water 2026-08-01); zero-leak independently re-confirmed (Mandate-marker grep over extracted-index + pending queue = 0 hits). NO REOPEN. Enhancement notes (not defects): (a) `mcp_tool_call_end` records not normalized — MCP-tool context absent from transcripts; (b) MAX_CHARS head-truncation drops session tails where resolutions often live (pipeline-wide, pre-dates UC-6); (c) the 64MB oversize class is a real coverage gap (6 skips incl. 222/233MB rollouts — likely the densest build sessions).** |
| FLAKE-0728 | **Local server-boot listen-timeout flake class** (3 sightings in 48h, all local, never CI): P2 builder sandbox ("could not open loopback mock servers"), PM shell run-1 during P2 Gate-A (exit 1, unreproduced x2), UC-6 builder baseline (2 files, boot >20s, assertion-free). Signature: server-boot tests time out reaching listening state under constrained/parallel environments; pass in seconds standalone. Candidate causes: server.js boot cost under parallel test load / sandbox CPU limits vs fixed ~20s listen timeout. Investigate F7c-style (root cause, not timeout-bump-by-reflex) AFTER UC-6 lands; do not touch test infra mid-build. Sighting log lives in this row — append future sightings here. Also noted: skip-count varies by env (5 vs 6 skipped at the same tree) — likely env-conditional skip; fold into the same investigation. | P2 | BUILD-4 | **RCA COMPLETE 2026-07-28 (BUILD-4 agent + PM source/repro verification). Verdict = TWO environmental classes + ONE masked deterministic defect: (1) CONTENTION (sightings 2-4, HIGH confidence, reproduced): ~1s synchronous server boot starved past the fixed 20s stdout-parse deadline under CPU oversubscription (measured 10-40s at 3-5x load, super-linear + high variance — why immediate re-runs pass); NOT an IPC race, NOT port collision (EADDRINUSE-retry works). The two sighting files are simply the only 2 of 5 boot-leg files that FAIL on boot loss (rider :234, neardup :324 assert) — the other 3 (ci5 :818, pricing-visibility :287, spec3-b1 :792/:864) SKIP the identical event, which is also the 5-vs-6 skip-count wobble (reproduced: 16 CPU hogs → ci5 boot leg skipped at 20.1s). (2) SANDBOX DENIAL (sighting 1, mechanism HIGH/attribution MEDIUM): sandbox denies loopback bind → fast listen EPERM (reproduced under Seatbelt deny-network; distinguishable shape: fast+EPERM vs ~20s+partial-log starvation). (3) DEAD LEG (deterministic, PM-reproduced on current tree): `test/pricing-visibility.test.js` behavioral boot leg DEAD since account-vocab landed 2026-07-25 — staging symlinks only lib/public/prompts, omitting `config/`, so staged boot dies MODULE_NOT_FOUND on server.js:33 `require('./config/account-vocab.json')` and downgrades to a routine-looking skip; pass→skip is pin-invisible (check-test-count.sh counts skips in the total) and CI-green. Partial mitigation on record: the file's structural-guard leg still enforces the predicate. Baseline skip composition explained: 5 = 4 retraction-plist-absent + 1 dead leg. REMEDIATION QUEUED (own small build, test-infra — behind the active build per never-mid-build; the GOV2-DEL queue-point was corrected 2026-08-27 — that build never started, see its row): (a) add `config` to pricing-visibility staging (+model_config.json parity) — restores the dead leg; (b) crash-exit ≠ environmental: non-EADDRINUSE/non-EPERM boot crashes FAIL loudly in all five files, never skip (behavioral pin: staged server missing a required file turns the suite red); (c) consolidate the five drifted stageServer/bootWithRetry copies into one shared helper (drift caused both the dead leg and the fail/skip asymmetry); (d) reservePort pattern (spec3-f2's) everywhere — five files currently mutually exclude on :3000; (e) listen deadline 20s→~60s WITH (b)'s fast-fail (evidence-backed per measurements, not reflex; it blocks 90-180s budgets less than two wasted builder cycles did). Class-B disposition: EPERM-shaped boot loss = skip-with-reason "sandbox denies loopback bind" + builder-runbook triage note (fast+EPERM=environmental / ~20s+partial=starvation / else=real). Housekeeping: RCA left the gitignore-sanctioned node_modules symlink in the PM worktree (boot legs need it). Open attribution residue: builder-sandbox CPU quota unmeasurable from here; a preserved raw builder failure output (UC-6 capture now live) would settle it. |

> Cross-cutting (every UC phase): per-client opt-in consent + `auxilo status` lists wired clients; ALL classes feed the unchanged scrub → LW-16 gate → pending_review pipeline (new clients change capture, never publishing policy); recursion-guard class generalizes (never export AUXILO_EXTRACTING; rules text must exclude Auxilo-extraction sessions); fragility tiers in public docs: hook=supported, poll=best-effort, rules=probabilistic.


## 22. Compliance Program — Engineering Requirements (2026-07-03)

Source: written AML program (private docs tree, `docs/AML-PROGRAM.md` in the Documents workspace — deliberately not in this repo). Items below are the code-side requirements only.

| # | Finding | Priority | Owner | Status |
|---|---------|----------|-------|--------|
| CP-1 | **Wallet re-screening on sanctions-list refresh** — on each successful 24h OFAC refresh, re-run `checkOFAC` over all linked + verified wallets; on a hit, freeze payout eligibility + `logOFACBlock(wallet, 'rescreen')`. Hook into the refresh timer (server.js ~1873). Closes the continuing-obligation gap where a wallet linked clean is never re-checked. Trivial at current wallet count. | P1 | BUILD-2 + GOV-3 | ✅ **DONE 2026-07-20 — DEPLOYED** (main `c076021`). Found half-shipped: the LINKED-wallet sweep already existed (`rescreenLinkedWallets`, commit `2f95fbc` 2026-07-11, freezes via `account.disabled_at`, pinned by r01-launch-blockers §9) — left untouched. Built the missing half: `lib/ofac-rescreen.js` `rescreenVerifiedWallets()` (deps-injected, never-throws, per-wallet try/catch, PLATFORM_WALLETS exempt, dedupes against linked set) stamps `ofac_hit_at` on the verified-wallets record + `logOFACBlock('rescreen')` + ops alert, batched into the refresh success path in its own try/catch. Freeze semantics: verified-only wallets are already live-checked by `checkOFAC` at every reachable money/submit route — the stamp is the proactive compliance record + re-alert suppressor, not a new gate. 12 tests (test/cp1-ofac-rescreen.test.js). Gate-A verified the polymorphic store (`true` vs `{verified, ofac_hit_at}`) safe across ALL six consumers incl. boot loader round-trip; LOW-1 recorded (a sanctioned wallet actively re-verifying resets the stamp → duplicate alert next refresh, zero auth/money impact — accepted; strict idempotency = preserve stamp on re-verify, SPEC-1/GOV-3 optional). |
| CP-2 | **Identity + W-9/W-8 capture at wallet-link** — add legal name + country of residence (self-attested) + W-9/W-8 collection to `POST /account/link-wallet` (per red-team P1-7: atomic settlement removes the payout chokepoint, so identity gates at wallet-link). Country is the single gating input the state-by-state analysis lacks today. New PII store: access-controlled; reconcile PRIVACY-POLICY.md + SUBPROCESSORS.md if a KYC vendor is added. | P1 | BUILD-1 → BUILD-2 + GOV-2 | ✅ **CODE BUILT DARK 2026-07-19** on `wave2b-ops-hardening` (spec `~/.auxilo/handoffs/BUILD-SPEC-WAVE2B-2026-07-19.md` §4; GOV-3 storage argument in-spec + `lib/identity-vault.js` header). Behind `CP2_IDENTITY_CAPTURE_ENABLED` (default OFF, absent = off — flag-off purity test-pinned: zero behavior change). When on: link-wallet step-2 requires self-attested `{legal_name, country, tax_form_type: W-9|W-8BEN}` (400 `IDENTITY_REQUIRED` with machine-readable field contract; step-1 challenge advertises it); stored in SEPARATE `data/identity.json`, AES-256-GCM per record (fresh IV, AAD = accountId so rows can't be transplanted, key = `CP2_DATA_KEY` 64-hex Fly secret ONLY — no passphrase derivation), file 0600 tmp+rename; flag-on-but-key-missing REFUSES the link (503 `IDENTITY_CAPTURE_UNAVAILABLE`, fail closed, never plaintext); refusals sit before nonce consumption (challenge survives); snapshot/restore compensation means no identity row can exist for a failed link and a re-link 409 can't destroy a prior record. Never decrypted on any route (readIdentity = ops-shell/tests only), never logged, never in responses beyond `identity_captured: true`. 2026-07-01-leak lesson applied: even a full repo/volume/backup leak exposes ciphertext only. **Activation stays counsel-gated (GOV-2): flag stays OFF until the policy questions (which countries, W-8BEN-E, retention) are answered; PRIVACY-POLICY.md reconcile due at flag-flip, not at this dark ship.** NOT deployed. |
| CP-3 | **OFAC screen in the router settler path** — when settlement migrates to AuxiloSplitRouter via `lib/x402-router.js`, screen BOTH the buyer (EIP-3009 `from`) AND the resolved contributor wallet before broadcasting either settle path; refuse settlement where no buyer address is extractable (closes the unparseable-header fallback gap). Rises to P1 the moment the router rail goes live. | P1 (at router launch) | BUILD-2 + GOV-3 | ✅ DONE (2026-07-03) — shipped in `lib/x402-router.js` `settleWithRouter` (dual OFAC fail-closed both paths, deps-injected) + `_verifyPayment` router mode fails closed on unextractable buyer/unparseable header. Deployed inert; active at flag-enable. |
| CP-4 | **Geo/IP embargo screen** — no geo/country handling exists anywhere in server.js; add embargoed-jurisdiction IP screening at money-movement endpoints per the program's tiered trigger schedule. | P2 | BUILD-2 + GOV-3 | ✅ **DONE (verified 2026-07-19 reconcile)** — embargo screening implemented in server.js (14 refs) + `test/geo-embargo.test.js` green (47/47 in Gate-A runs); shipped with the r01 line, live in prod |
| CP-5 | **Clickwrap assent capture (red-team P0-3)** — the payee-agency appointment (ToS §5.10) does not bind without affirmative, captured assent. Built: `CURRENT_TOS_VERSION` + consent artifact (`tos_version`/`accepted_at`/`accepted_ip`/`accepted_ua` + append-only log) in `lib/accounts.js`; `POST /account/accept-terms` + `GET /account/terms-status` (session OR API key); **blocking 403 `TERMS_NOT_ACCEPTED` gate at `POST /account/link-wallet` + both withdrawal rails**; web clickwrap (unchecked box + Accept) in `public/dashboard.html`; MCP handshake `auxilo_accept_terms` (V-3, explicit `agree`) in `mcp-server.js`; durable hash-chained record `lib/tos-acceptance-log.js` (parallel reuse session, wired in — write-side evidence only, never a gate). Tests: `test/tos-clickwrap-assent.test.js` (**27/27** after the L-2 affirmation-token hardening; suite w/ the 7 pre-existing extract-pipeline fails). Docs: UX-FLOWS §3 STEP 6.5 + flow 7 V-3, ONBOARDING-COPY §3.5. **L-2 affirmation token DONE (2026-07-04):** `POST /account/accept-terms` now requires `agree===true` (else 400 `AFFIRMATION_REQUIRED`) and stamps `affirmed` into the account artifact + the tamper-evident durable row; both clients (`mcp-server.js`, `public/dashboard.html`) forward `agree:true`. **FINAL GATE-B (2026-07-04, 3-seat panel: contracts-counsel + money-transmission-counsel + legal-red-team) = SHIP-WITH-CONDITIONS** → `~/.auxilo/handoffs/GATE-B-FINAL-VERDICT-payee-agency-2026-07-04.md` + apply-ready diff `~/.auxilo/handoffs/DEPLOY-READY-TOS-DIFF-payee-agency-2026-07-04.md`. L-1 resolved (connect-stripe-ungated is correct; agency triggers on receipt, not destination-setup). Third-agency-clause check CLEARED (only §4.4 + §20.5; both carved out). Version→document orphan (red-team P0) fixed in the diff (publish `2026-07-04-payee-agency-a1` identifier + collapse 3 dates). V-3b (MCP principal-binding) still counsel-reserved. | P0 | BUILD-1 + GOV-3 + BUILD-4 + SPEC-2/3 + GOV-2 | ✅ CODE + TESTS DONE, GATE-B FINAL = SHIP-WITH-CONDITIONS (2026-07-04) — **DEPLOY-GATED: ships all-or-nothing with the §5.10 ToS text (CHANGES 0–6 of the deploy-ready diff) + the npm republish (MCP `agree:true`). `CURRENT_TOS_VERSION` = published amendment id `2026-07-04-payee-agency-a1`. Hold for Tyler's ship word + Condition A decision (CP-6).** Uncommitted; tree is a commingled multi-session snapshot — reconcile/attribute at commit. |
| CP-6 | **Accrual-time acceptance gate (Gate-B Condition A, red-team P0-1 + money-transmission L-1)** — the payee-agency defense turns on *receipt* of the Builder Share, but accrual is ungated: `GET /knowledge/:id` credits `pending_balance` at `server.js:6380` (+ WAL replay `server.js:1057`) with no builder-ToS check, so a builder can have funds received on their behalf before assenting. The 3 live gates are all outbound (link-wallet + 2 withdraw rails). Draft Part B.1 Cohort 1 already specifies acceptance "before the account can submit a Learning or link a wallet" — the build implemented the wallet/withdraw gates but NOT the submission/accrual gate. Options: (1) gate accrual at the credit sites (hold/suspend or route via receive-path if `!hasAcceptedCurrentTos(contributor)`); (2) gate Learning-submission (`POST /knowledge`/`/learn`, currently `optionalAuth` → needs auth-first); (3) honesty-only (draft B.3 corrected — accrued-but-unassented is received outside the agency, cured prospectively). **Launch-tolerable TODAY** (0 real external builders, $3.15 synthetic/self, custodial withdraw paused) — prospective gap; MUST close before a real external builder can accrue. | P0 (prospective) → do before real-builder onboarding | BUILD-1 + GOV-2 + BUILD-4 | ⏳ **SPEC TEED UP (Tyler chose "tee this up" 2026-07-04)** — build-ready spec `~/.auxilo/handoffs/BUILD-SPEC-CP6-accrual-gate-2026-07-04.md`. Design resolved: **quarantine held share in `unassented_pending` when `!isPayeeAgencyInForce(contributor)`, convert→`pending_balance` on assent** (accrual sites `server.js:6406` + WAL `:~1057`; `getWithdrawableBalance` reads pending_balance only → held is non-withdrawable for free; convert wired into accept-terms). ~40–80 LOC + 10 tests, additive/reversible. Sequencing = **fast-follow**; GOV-2 confirms quarantine-vs-refuse mechanics at build-time Gate B (direction already confirmed). **✅ BUILT + GATE-A GREEN 2026-07-04 (Tyler "build CP-6").** `isPayeeAgencyInForce` (`lib/accounts.js`, gate on §5.10-bearing version not current-version so unrelated bumps don't re-quarantine); `unassented_pending` bucket + `convertUnassentedToPending` + migration carries (`lib/earnings.js`); accrual gate at custodial credit + WAL-payload `agency_in_force` (decided at unlock, replayed deterministically — supersedes the spec's cold-cache concern) + backward-compat `!== false` default; lock-free conversion in accept-terms (first-accept-only + withdrawals acceptance-gated → no race). Tests `test/cp6-accrual-gate.test.js` **14/14** + regressions clean (M-1 ledger 9/9, x402-router 37/37, assent 27/27, stripe-reconcile/eip3009/facilitator all green). Folded onto `r01-noncustodial-launch` (9e455ff). **⚠️ GATE-B GOV-2 REVIEW DONE 2026-07-05 (money-transmission-counsel + legal-red-team, convergent) — SCOPE CORRECTED: CP-6 gates DISBURSEMENT, not receipt.** On the custodial rail the buyer's USDC physically lands in the platform wallet at settle BEFORE the gate runs; the held-bucket split is bookkeeping over money already received — it cannot cure the physical receipt. CP-6's real value: no Builder Share is made WITHDRAWABLE outside the §5.10 agency. **The receipt cure = router-default-or-REFUSE (cross-workstream launch invariant).** Verdict: SHIP CP-6 as the disbursement bridge (P0 none; no bypass; launch-defensible at 0 real builders). **Two fixes applied 2026-07-05: P1-B (accept-then-link would STRAND held balance non-withdrawable → link-wallet now converts after `lazyMigrateOnWalletLink`); P2-A (`isPayeeAgencyInForce` keys on a fail-closed `PAYEE_AGENCY_VERSIONS` allow-list — a version that removes §5.10 can't silently open the gate). Suite 17/17.** P1-B+P2-A verified ON r01 tip (2026-07-19 reconciliation: `PAYEE_AGENCY_VERSIONS` lib/accounts.js:124, link-wallet convert server.js:4064) and **LIVE since v34 (2026-07-19 deploy of d4eb050)**. Remaining: accept-then-crash cure sweep + held-balance visibility (AUD19-8, §26) — ✅ built 2026-07-19 on `funnel-fixes-aud19` (`sweepHeldEarnings` under the shared earnings lock; see AUD19-8 row), pending deploy. |
| CP-7 | **Aged held-balance disposition rule (Gate-B GOV-2 P1-C — NEW exposure CP-6 introduces)** — indefinitely holding a never-accepting Builder's identified `unassented_pending` looks MORE like custody ("holding identified beneficiary funds they can't access") and imports **unclaimed-property/escheatment** obligations (dormancy 3–5yr, state-dependent). CP-6 trades an acute receipt problem for a chronic held-liability one; there is NO expiry/sweep today (`unassented_pending` is only ever incremented, carried, or converted-on-assent). Red-team's cleanest fix: **refund the Builder Share to the BUYER after a defined no-assent window** (reverses the pre-assent receipt → "we never kept unassented Builders' money"), moots escheat; else document + build the dormancy/report path. Also: never describe `unassented_pending` as money "held for" the Builder — describe it as an *undisbursable, refund-eligible* receipt. | P2 (prospective) → before custodial rail carries real external volume | **auxilo-aml-compliance-officer** + GOV-2 + RISK-REGISTER | ⏳ ROUTED to the AML program (task chip spawned 2026-07-05). Not a launch-day blocker at 0 real builders; policy + code (window/sweep or refund-to-buyer) before real volume. |


## 23. R-01 Router — Flag-Enable Conditions (2026-07-03)

Server rewire steps 1–3 are BUILT, Gate-A-reviewed, and deployed to prod INERT behind `X402_ROUTER_ADDRESS` (unset). Canonical detail: `contracts/README.md`. These items gate flipping the flag ON (testnet staging OK now; mainnet additionally gated on external contract audit + R-01 structure):

| # | Item | Priority | Owner | Status |
|---|------|----------|-------|--------|
| RT-1 | Rate-limit / per-resource-key the unauthenticated 402 hint mint (sec review L-1: hint-flood evicts live Receive-path hints → degrades buyers to the Transfer path; availability only, no fund risk) | P1 (at flag-enable) | SPEC-2 + BUILD-2 | ✅ DONE (2026-07-03) — hints now reuse one salt per (resource, contributor, bps, amount) within a 5-min window and are NOT consumed on success (EIP-3009 nonces are per-authorizer, so buyers share salts safely); live-hint population is bounded by catalog size, not request rate. Unit-tested. |
| RT-2 | Record exact on-chain micro-USDC (`floor(value*bps/1e4)`) alongside float USD in `onchain_settlements` (sec review L-2; reconciliation/tax fidelity) | P2 | CFO-1 + BUILD-2 | ✅ DONE (2026-07-03) — gross_micro/contributor_micro/platform_micro (= floor(gross*bps/1e4), dust to fee side, matching the contract) recorded at all 3 booking sites incl. WAL replay (settlement_bps added to the WAL payload). |
| RT-3 | Integration test: router-settled self-unlock books an `onchain_settlements` row (eng review MED-1 — code fix landed 2026-07-03, test owed) | P1 (at flag-enable) | BUILD-4 | ✅ DONE (2026-07-03) — test/x402-router-server.test.js (structural suite, repo convention): self-unlock booking + ordering, single guarded pending_balance site, WAL replay guard, router-mode control flow, RT-2 fields. 9/9 green. |
| RT-4 | Auxilo-aware client in `mcp-server.js`: read 402 `extra.router`, derive nonce `keccak256(abi.encode(contributor,bps,salt))`, sign ReceiveWithAuthorization, echo `{extra:{salt}}` in X-Payment. Until then all buyers settle via the generic Transfer path (works, settler-asserted split). Rides npm republish (2FA — Tyler). | P1 (at flag-enable) | SPEC-2 + BUILD-2 | ◐ PROTOCOL DOCS DONE (2026-07-03) — mcp-server.js is a pass-through (agents' own x402 libs sign), so the buildable half is the protocol contract: AGENT-LEARNING-GUIDE.md §4 now documents both paths (Transfer works unchanged; Receive = sign with extra.router.nonce + echo salt) and the auxilo_unlock x_payment schema description carries the same. REMAINING: Tyler's npm republish ships the schema text. |
| RT-5 | Delete/deprecate custodial `POST /withdraw` after dust migration (rewire step 4; rail stays paused behind `CUSTODIAL_WITHDRAW_ENABLED`) | P1 (at flag-enable) | BUILD-2 + GOV-3 | OPEN |


## 24. Contract Audit — AuxiloSplitRouter (simulated adversarial audit, 2026-07-04)

Full report: `~/.auxilo/handoffs/AUXILO-SPLITROUTER-AUDIT-2026-07-04.md` (PRIVATE — discusses settler-key/incident history; NOT in this public repo). 13 findings, verifier-adjudicated. **Verdict: NOT YET MAINNET-READY — 0 Critical / 0 High survived verification; on-chain logic sound; 1 confirmed must-fix (off-chain), 1 hard gate, plus accepted-risk conditions.** Simulated audit does NOT replace the two README gates (paid external audit + counsel R-01 sign-off).

| # | Finding | Sev | Owner | Status |
|---|---------|-----|-------|--------|
| AUD-F5 | Settler key silently fell back to the shared custodial payout key (`X402_SETTLER_PRIVATE_KEY \|\| WALLET_PRIVATE_KEY`) — an undisclosed amplifier of the disclosed theft vector, using a leak-history key class. | Med (confirmed) | BUILD-2 + GOV-3 | ✅ **FIXED 2026-07-04** — `lib/x402-router.js:307` now requires a dedicated `X402_SETTLER_PRIVATE_KEY`, fails closed (no fallback); `contracts/README.md:130` corrected to mandate isolation (HSM/KMS/multisig). Syntax-checked, module loads inert. Rotation-by-redeploy runbook now WRITTEN (`contracts/README.md` Flip-the-flag runbook → Settler rotation); the deploy-attest preflight also asserts settler ≠ platform custodial wallet. STILL OWED: provision the dedicated key in HSM/KMS before flag-enable. On the Receive-only MVP, F5 is liveness-only (settler cannot divert). |
| AUD-F2 | `splitStranded` delegates its safety to an off-chain recovery module that **does not exist** (no caller/ABI/dedup/OFAC in `lib/x402-router.js`). | Low + hard GATE | BUILD-2 | OPEN — **mainnet gate:** before flag-enable, either descope/remove `splitStranded` from deployed scope, OR build the recovery module mirroring `settleWithRouter` guards (persistent authNonce dedup, dual OFAC fail-closed, verified-wallet contributor resolution, amount reconciliation). |
| AUD-F1 | `splitStranded` settles against total router balance, no on-chain replay guard — a compromised settler / WAL double-fire could split the whole resident balance repeatedly. | Low + harden | BUILD-1 + BUILD-2 | OPEN (only if `splitStranded` ships) — add `mapping(bytes32=>bool) strandedSettled` single-use guard; bind `value` to a recorded deposit. Contract change → re-audit. |
| AUD-B5 | **NEW (verification-program L7, 2026-07-04):** `contributor == feeWallet` or `== address(this)` corrupts accounting / silently strands funds under a "Settled" event — and every proving layer was BLIND to it (Halmos `vm.assume`d it away, invariant handler filtered it, no unit test). Green results were green *because the harness excluded the failing inputs.* | Low + harden | BUILD-1 | ✅ **DONE in the ReceiveOnly revision** (verified 2026-07-19 per AUD19-14): `_validate` reverts `BadContributor` on `contributor == feeWallet \|\| contributor == address(this)` (contracts/AuxiloSplitRouterReceiveOnly.sol:202-205), unit-tested with `vm.expectRevert(BadContributor.selector)` (AuxiloSplitRouterReceiveOnly.t.sol:408,420). The stranded-funds surface is additionally **descoped by construction** in RO: `splitStranded`/`settleAndSplitTransfer` do not exist there (0 occurrences; they live only in the non-deployed full AuxiloSplitRouter.sol). |
| AUD-A3 | Deploy-time constructor (usdc/feeWallet/settler) is the entire immutable trust root and is uncovered. | — (gate) | BUILD-1 | ✅ **DONE 2026-07-04** — runnable `scripts/deploy-attest.js` (`preflight`: canonical-USDC assert + fresh non-blacklisted feeWallet + impl-pin + distinctness; `readback`: on-chain immutable read-back + **immutable-aware bytecode match**) + written `contracts/DEPLOY-ATTESTATION.md` checklist (fresh-wallet provisioning, Basescan `forge verify-contract`, sign-off gate). Live-rehearsed against Sepolia `0x149C…8145`: both phases all-✓; negative cases fail closed. Must be RUN at mainnet deploy (see Flip-the-flag runbook). |
| AUD-VERIF | **Verification program (no-paid-audit substitute) COMPLETE 2026-07-04** — `contracts/VERIFICATION.md`. Slither 0 / Aderyn 0-High / **50 Foundry tests** / **100% branch** / Halmos 5/6 exhaustive proofs / 4 invariants @512×256k / mainnet-fork; then L7 multi-agent evidence-audit (46 agents). **Honest verdict: CONTRACT-logic ~93% (→96-97% w/ F1+B5 guards), DEPLOYED-SYSTEM ~78% (dominated by settler-key custody + F2 + A3, not contract bugs), R-01/legal UNADJUDICATED (counsel-only).** Does NOT replace the paid-audit + counsel gates — shortens them. | — | BUILD-1 + GOV-2 | DONE (assessment); the gate items above remain OPEN |
| AUD-TESTS | Foundry coverage gaps vs the project's own stated plan. | — | BUILD-4 | OPEN — add: settler-compromise KAT asserting exact max-extractable-value (README already calls it REQUIRED); EIP-1271 smart-account buyer fork test (contract claims support, only ECDSA unit-tested); transfer-hook reentrancy mock; `balanceOf<value` stranded branch; feeWallet-blacklist brick; confirm the mainnet-fork Receive E2E exists. |
| AUD-F6 | Client instruction "sign nonce as given" invites blind signing → the Receive-path independent-control property is conditional on buyer re-deriving the nonce. | Low | SPEC-2 + GOV-2 | ✅ **DONE 2026-07-04 (code copy)** — `buildRouterExtra` instructions now direct the client to DERIVE the nonce and confirm it equals the advertised one before signing (not blind-sign). Counsel-disclosure of the conditionality still owed to GOV-2. |
| AUD-A1 | USDC is a Circle-upgradeable proxy — every "safe" verdict is contingent on the current impl (a future upgrade could add a transfer hook / fee-on-transfer / changed nonce semantics). | — (risk register) | GOV-3 + GOV-2 | ✅ **MITIGATION DONE 2026-07-04** — off-chain impl-hash monitor + fail-closed circuit-breaker built: `lib/usdc-impl-monitor.js` (pinned impls Base `0x2ce6…d779` / Sepolia `0xd74c…c5b5`, ZeppelinOS slot) wired into `settleWithRouter` (sticky fail-closed on impl change; 503) + standalone `scripts/usdc-impl-monitor.js` (cron one-shot / `--watch` / webhook). Unit-tested + live: OK both chains, trips on wrong pin, no funds move. The underlying accepted-risk (only Circle can trigger) is now DETECTED + fails closed rather than silent. |
| AUD-A4 | **Reorg / confirmation-depth (audit A4/B1):** the settler booked earnings / served content at depth 1 — a Base OP-stack reorg could un-mine a booked `Settled` (phantom settlement). | — (gate) | BUILD-2 | ✅ **DONE 2026-07-04** — `lib/x402-router.js` `_waitForFinality()` books as final only after a reorg-safe wait (OP-stack `safe`/`finalized` block-tag + numeric min-depth, configurable; receipt re-verified for un-mining). `reverted`/`reorged_before_final`/`confirm_timeout` all fail closed. Unit-tested (4 states) + live on Sepolia (settle booked only after a 60.4s `safe`-head wait, not depth 1). Async provisional-serve remains a future enhancement. |
| AUD-RO | **Receive-only MVP server wiring (F8 closure):** in router mode offer ONLY the Receive path; refuse generic Transfer. | — | BUILD-2 | ✅ **DONE 2026-07-04** — `X402_ROUTER_RECEIVE_ONLY` (default ON): 402 challenge advertises Receive-only (`extra.router.mode`), `settleWithRouter` refuses saltless Transfer fail-closed (`receive_only_requires_salt`) → closes AUD-F8 (buyer forcing the unbound Transfer path) by construction. Never invokes `settleAndSplitTransfer` (absent from the deployed RO bytecode). Unit-tested + live. |
| AUD-LEGAL | **Binding legal condition (F4/AR-1):** every "non-custodial" claim to counsel AND in public copy must be scoped to the **Receive path only** — never assert diversion-impossibility for Transfer/Stranded. Independently corroborates red-team P0-2. | — | GOV-2 + contracts-counsel | OPEN — feed the counsel brief + audit the public/legal copy. |

## 25. Cold-Start Findings (2026-07-18)

| # | Finding | Priority | Owner | Status |
|---|---------|----------|-------|--------|
| CS-1 | **Fresh-install seeding TDZ crash** — the startup migration/seed blocks call `safeWrite()` during module init, but its backup-cleanup state (`let cleanupRunning` / `let lastBackupCleanup`) was declared ~120 lines *below* the seed block. On a cold start (empty `data/`), the seed write threw `Cannot access 'cleanupRunning' before initialization` inside the seed try/catch — mislabeled as "Failed to load seed knowledge", success log swallowed. The seed data landed on disk only because the TDZ read happens to sit after the atomic rename (one refactor away from real data loss), and the same TDZ sat under all four top-level migration write sites (AC-1/M-1/M-B), where a mid-block throw skips the paired earnings write — a partial-write hazard on populated installs. Prod never hit it (data dir populated); every fresh install did. | P1 | BUILD-2 | ✅ **FIXED 2026-07-18** — declarations hoisted next to the `BACKUP_DIR` setup, above every top-level `safeWrite()` call site. Verified by real cold boot against an empty temp data dir (pre-fix: TDZ error reproduced verbatim; post-fix: `Seeded 22 initial learnings` + backup cleanup runs at init). Regression: `test/cold-start-seed.test.js` — structural source-order guard (CI-enforced) + behavioral cold boot (self-skips where server runtime deps aren't installed). Negative control: both guards fail against the pre-fix code. Full suite 572 pass / 0 fail. |

## 26. Critical-Areas Audit (2026-07-19)

Four-lane audit (agent onboarding/install, first contribution + first unlock, human web UX, money/trust foundations) verified against repo AND live prod; targeted money-path tests run 88/88 green. Full findings with evidence: `~/.auxilo/handoffs/AUXILO-CRITICAL-AREAS-AUDIT-2026-07-19.md` (PRIVATE — keep out of this repo).

| # | Finding | Priority | Owner | Status |
|---|---------|----------|-------|--------|
| AUD19-1 | **api.auxilo.io TLS cert absent** → cold `npx auxilo setup` hard-down (that host is the published installer's default base URL + every npm-README curl example). | P0 | BUILD-2 | ✅ **FIXED 2026-07-19** — `flyctl certs add api.auxilo.io` (Let's Encrypt issued; verified live: /health 200, /auth/device issues codes, /categories + /knowledge/stats 200). Follow-ups: LW-5 external uptime on this host; sweep `api.auxilo.io`→`auxilo.io` defaults at next npm republish. |
| AUD19-2 | **Credit-path revenue-share economics** — contributor share is computed from the learning's dynamic list price while credit-pack buyers pay the flat pack rate; no per-buyer/per-learning unlock throttle. Accrued obligations can exceed collected revenue. Latent only while withdrawals are paused. | **P0 — blocks withdrawal re-enable** | BUILD-1 + CFO-1 + GOV-3 | ✅ **BUILT 2026-07-19** on `econ-fix-aud19-2` (Tyler ratified option (a) — decision `~/.auxilo/handoffs/DECISION-AUD19-2-credit-economics-2026-07-19.md`; spec `BUILD-SPEC-AUD19-2-econ-2026-07-19.md`). Accrual basis = **min(list, credit unit price)**, forward-only (no clawback; pre-cutover WAL entries replay stored amounts unchanged). Credit LOTS (`lib/credits.js unlock_lots`): webhook lots packs at pro-rata price (starter $0.125, growth/pro $0.10 — pack price 100% to unlocks), referral grants are **$0-revenue lots accruing $0**, paid-lots-first consumption, legacy balances migrate lazily (purchase-history-derived, $0.10 fallback). **1-per-(buyer,learning)/30d accrual cap** (`lib/unlock-attribution.js`, persisted `data/unlock-attribution.json`): applied at the ACCRUAL decision only — content served, credit burns, demand counters NOT pumped; credit path only (x402 is revenue-backed at full list). WAL gains `amount_paid_usd`/`funding_source`/`purchaser_account_id`/`purchaser_ip_redacted`/`purchaser_ua` (log-now collusion layer); replay books the STORED basis. Gross now cash-true (books basis). **Bonus fix:** M-2 wash guard's account arm compared a PRE-auth (always-null) accountId on the API-key path — now post-auth (`buyerAccountId`), so the guard actually fires against credit-path self-unlocks. Copy cure shipped on llms.txt/pricing/for-builders/index (JSON-LD+visible parity) + MCP unlock description ("computed on the amount the buyer actually pays"). **ToS §5.4 definitional sentence = GOV-2/counsel-gated, routed separately (§17 notice).** Tests: `test/aud19-2-econ.test.js` (32) + full suite 758/0. Later wave (pre-growth, per decision §4): daily velocity cap + alert, cross-account batch report. NOT deployed. |
| AUD19-3 | **Wallet-only contributions orphan in `pending_review`** — `contributor_account_id: null` + account-scoped self-review queue = only an admin can ever approve; no alert fires on queue entry. Promised earnings undeliverable for the wallet-only cohort. | **P0 — first-contribution funnel** | BUILD-1 → BUILD-2 | ✅ **BUILT 2026-07-19** on `funnel-fixes-aud19` (spec `~/.auxilo/handoffs/BUILD-SPEC-AUD19-funnel-2026-07-19.md`). Design: **HOLD bound-to-wallet + guidance** (not reject — work preserved, funnel intact) cured by **ownership ADOPTION**: `adoptWalletOrphans` (lib/self-review.js) binds `contributor_account_id` for null-account learnings bearing the account's VERIFIED linked wallet (all statuses; stamps `ownership_adopted`), wired at link-wallet + lazily at GET /account/pending(+/summary) for pre-deploy orphans — implements LW-15's noted follow-up but stronger (a read-time fallback would list items the ownership gate still 403s). Wallet-only pending 201s now carry `how_to_review` naming the `npx auxilo setup` → verify+link-same-wallet cure. Queue-entry ops alert: `notePendingReviewEntries` → `sendOpsAlert`, batched ≤1/6h (env `PENDING_REVIEW_ALERT_INTERVAL_MS`), count restored on failed send; wired /learn + /extract + chat-pipeline. **Gate-A adversarial review (2026-07-19) hardening, same branch:** HIGH-1 — global `verifiedWallets` membership was account-blind, so any ToS-accepted account could link a victim's verified wallet and the adoption/migration/sweep hooks would hand it that wallet's learnings+earnings (409-uniqueness locking the owner out); `POST /account/link-wallet` now demands a FRESH account-bound EIP-712 link challenge (action `link:acc_…` inside the signed message, single-use nonce, 5-min TTL, `verifyLinkSignature` in lib/eip712.js) before any link — adoption fires only after proven control; existing links grandfathered (one-time link, never re-validated; pre-deploy cohort is platform-only accounts). MED-2 — `linkWallet` refuses PLATFORM_WALLETS (403; the boot-time auto-verified live platform wallet was linkable → drain vector). MED-3 — link response lists `adopted_learning_ids`. Clients updated in the same diff: MCP `auxilo_link_wallet` two-step (schema `signature` + challenge surfaced as next-step, **must ride npm 0.9.2**), dashboard no-wallet hint copy. NOT deployed — ships via Fly + npm 0.9.2. |
| AUD19-4 | **MCP `auxilo_contribute` omits `quality_self_assessment`** (absent from tool schema) → `qualityPresent` always false → EVERY MCP contribution lands `pending_review`; seamless publish is dead code on the primary path; the response never mentions the self-approval step (`auxilo review`/dashboard). Root cause under LW-18's stalled funnel. | **P0 — first-contribution funnel** | BUILD-2 + SPEC-2 | ✅ **BUILT 2026-07-19** on `funnel-fixes-aud19`. mcp-server.js: `quality_self_assessment` added to the tool inputSchema (4 dims + total, all required so a cold LLM sends the full shape; server still accepts absence → pends) + handler passthrough + description teaches the field, the floor, and the pending self-review path; `auxilo_account_earnings` description names `held_pending_assent`. server.js: every pending_review 201 from /learn now carries `how_to_review` (CLI `auxilo review` + dashboard queue + GET /account/pending; wallet-only variant per AUD19-3). **Ships as a pair: server half via Fly deploy; MCP half via npm `auxilo-mcp@0.9.2` republish (build 0.9.2 from the reconciled tree — npm 0.9.1 is ahead of repo at the same version string). Server must deploy WITH AUD19-6's floor before/with the republish.** |
| AUD19-5 | **Unpaid unlock returns 401, not 402-with-`accepts[]`** (dualAuth Path 3); the x402 descriptor only appears after an *invalid* X-Payment; MCP unlock's 402 branch unreachable cold; contradicts ERROR-CODES.md. | P1 | BUILD-2 + SPEC-2 | ✅ **BUILT 2026-07-19** on `payment-contract-aud19` (spec `~/.auxilo/handoffs/SPEC-AUD19-5-402-first-2026-07-19.md`, approved as specced — bug fix restoring the published openapi contract). server.js: `verifyPaymentOrReject` gains `authOptions` param; `dualAuthDynamic` Path 3 delegates into it (one source of truth mints every challenge) → cold unauth'd GET on a payable resource now answers **402** + `accepts[]` (router hint included in router mode — RT-4 prerequisite) MERGED additively with the options envelope (+`how_to_authenticate: 'npx auxilo setup'`, audit #8); credits-exhausted 402 now carries `accepts[]` too (§3.3-2, adopted per spec). `dualAuth` static path (§3.3-1) **deferred** to the docs-reconcile wave per spec. mcp-server.js: unlock handler handles BOTH 402 (new) and legacy 401-with-options via pure `unlockPaymentRequired` (+`how_to_pay`). openapi.json `/knowledge/{id}` + `PaymentRequired` schema updated; ERROR-CODES.md refreshed (local-only). Tests: test/aud19-payment-contract.test.js (challenge consistency, options-merge both arms, refuse-arm ordering, api-key path regression, both MCP branches). Ships as a pair: server half via Fly deploy (independently safe — live MCP 0.9.1 already handles 402); MCP half rides npm 0.9.2 (2FA — Tyler). |
| AUD19-6 | **14/20 quality gate is prompt-only** — server validates sum arithmetic, never `total>=14`/dim`>=3`; contributor self-approval has no quality check at all. Day-one catalog defenses are dedup + sensitivity screens only. | P1 | BUILD-2 + CAT-1 | ✅ **BUILT 2026-07-19** on `funnel-fixes-aud19` (same server change as AUD19-4 — the floor must exist the moment MCP starts sending assessments). `meetsQualityFloor` + `QUALITY_FLOOR_TOTAL=14`/`QUALITY_FLOOR_DIMENSION=3` (lib/self-review.js); /learn seamless predicate requires it; below-floor → **QUARANTINE** `pending_review` w/ review_reason `below_quality_floor` (never a hard 400 — a bounce would teach clients to omit the assessment). **Deliberate non-gate: contributor self-approval (`applySelfDecision`) keeps NO quality check** — the floor stops unattended auto-publish, not the human's explicit decision on their own content (documented in lib/self-review.js; review surfaces show score + flag). /extract path unchanged (separate CAT-1 call, not a silent rider). |
| AUD19-7 | **`auxilo_withdraw` MCP description ≠ server protocol** — describes a plain-string signature; server requires withdrawal-action challenge + EIP-712 typed data + server-computed net amount; no MCP tool can request a withdrawal challenge. Unfollowable the day the pause lifts. | **P0 at unpause — R-01 exit criterion** | SPEC-2 + SPEC-1 | ✅ **BUILT 2026-07-19** on `payment-contract-aud19` — Tyler approved **Option DASH/ROUTE** (spec `~/.auxilo/handoffs/SPEC-AUD19-7-withdraw-contract-2026-07-19.md` §4): `auxilo_withdraw` is now an honest **status-and-routing tool** — description/schema replaced verbatim (fictional string-signature protocol + dead `timestamp` param gone; no required inputs), handler reads `GET /account/earnings` and reports balances + the real path (pay-at-sale via link-wallet; legacy balance via dashboard Stripe rail; paused → status page). It NEVER calls `POST /withdraw`. Latent passthrough closed: `auxilo_verify_wallet` no longer forwards raw args (which let a caller mint an `action:'withdrawal'` challenge) — body whitelisted to `{wallet, signature}` (`verifyWalletRequestBody`). Companion copy (same npm cycle): `auxilo_account_earnings` / `auxilo_link_wallet` / `auxilo_settlements` descriptions carry the pay-at-sale story. Tests: test/aud19-payment-contract.test.js (routing shape, no-POST invariant, whitelist, description hygiene). **Entirely mcp-server.js — ships via npm 0.9.2 (2FA — Tyler); no server change.** Remains per spec §7: cutover-copy sync across the 5 surfaces (§5), counsel answers on §6 items 1/4/5, Option B (full custodial MCP) stays specced-not-built. |
| AUD19-8 | **CP-6 accept-then-crash strands `unassented_pending`** (conversion runs once inside first-accept only; re-accept idempotent; no sweep) + held balance surfaced in NO user-facing endpoint/dashboard/MCP field. | P1 — before real builders | BUILD-2 | ✅ **BUILT 2026-07-19** on `funnel-fixes-aud19`. (a) `sweepHeldEarnings(accountId)` (server.js): gates on `isPayeeAgencyInForce`, cheap no-lock held>0 pre-check, then converts **under the shared earnings lock** (the sweep runs post-assent when withdrawals are possible, so CP-6's original lock-free argument no longer holds; ordering account→earnings matches Stripe-withdraw). Wired: accept-terms **BOTH branches incl. `alreadyAccepted`** (the crash cure — re-accept now converts), GET /account/earnings (self-healing read), link-wallet (replaces migrated-only conversion; sweeps unconditionally). (b) `held_pending_assent` exposed in both branches of GET /account/earnings (`unassented_pending` kept as legacy alias); MCP `auxilo_account_earnings` passes it through (description names it — rides npm 0.9.2). (c) Dashboard held line reframed per CP-7: "Undisbursable receipts — released on Terms acceptance" + receipt-language explainer; never "money we're holding". NOT deployed — ships via Fly (+0.9.2 for the MCP description). |
| AUD19-9 | **Router finality wait (≤180s; 60.4s observed Sepolia) vs Fly ~60s proxy idle timeout** → buyer can pay on-chain (nonce consumed) and get no content, no retry; no reconciliation runbook for `confirm_timeout`/dropped-connection settles. | **P0 at router flag-enable** | BUILD-2 + SPEC-1 | OPEN — keepalive/async provisional-serve + RUNBOOK reconciliation entry (on-chain `Settled` events auditable). |
| AUD19-10 | **Credit burned before delivery** — `deductCredit` persists immediately; a post-deduction `safeWrite` failure 500s with no credit refund (WAL protects earnings, not credits). | P2 | BUILD-2 | ✅ **BUILT 2026-07-19** on `wave1-money-closures` (spec §1.2). Compensating transaction in-request: unlock delivery wrapped in try/catch; on a credit-path failure → `abortWal` FIRST (new lib/wal.js primitive — if the WAL survives, refund is REFUSED so replay can't double-pay; ops-alert instead), in-memory ledger rollback (quality/demand/learning.earnings/earnings-entry snapshots — no phantom accrual can flush later), then `refundCredit` (new lib/credits.js — restores the count AND the consumed lot at its recorded unit price, $0 grants come back at $0, basis accounting can't drift), then `unrecordAccrual` (new lib/unlock-attribution.js) gated on THIS request having armed the cap — the buyer's retry accrues normally, not $0. Buyer gets 500 `DELIVERY_FAILED_CREDIT_REFUNDED` with `credit_refunded: true`. x402/router failures rethrow (WAL replay is the designed recovery for settled money). Compensation path never throws; refund failure = log + `unlock-refund` ops-alert. Residual (documented, out of scope): process CRASH between deduct and catch still loses the credit (no credit-side WAL; Wave-2-class durability). NOT deployed. |
| AUD19-11 | **Live UX breaks**: /for-agents nav → 6 dead homepage anchors; dashboard mobile hamburger toggles `open` vs CSS `nav-open` (no nav at all on phones). | P1 | BUILD-3 | ✅ **FIXED 2026-07-19** (this commit — hrefs → `/pricing` `/earnings` `/api`; hamburger → `nav-open`). |
| AUD19-12 | **Foundry artifacts baked into prod image** — `contracts/out/` (137 files) + `contracts/cache/` gitignored but not dockerignored. | P2 | BUILD-2 | ✅ **FIXED 2026-07-19** (this commit — .dockerignore). |
| AUD19-13 | **Docs/contract reconcile wave** — openapi (repo 0.7.0, live 0.9.1) both missing `/auth/device*` + `/account/terms-status`; ERROR-CODES.md v0.7.0 stale (401-first unlock, missing `TERMS_NOT_ACCEPTED`/`AFFIRMATION_REQUIRED`/pause code, $0.005 vs $0.05 floor); tool counts 18/22 published vs 23 actual + duplicate `get_stats`/`get_knowledge_stats`; prompts teach $0.005–$0.01 pricing (10x below floor → 400s); UX-FLOWS + ONBOARDING-COPY describe the pre-R-01 product; consent-story contradiction (approve-first copy vs shipped seamless+retraction). | P1 | BUILD-3 + SPEC-2 + GOV-1 | 🟡 **MOSTLY DONE 2026-07-19 (`docs-reconcile-aud19` off r01 tip).** Ground truth re-measured on ffc9a0b first — several audit numbers were stale (measured on the retired wip branch): actual tool count here is **18** (renderly gone, `auxilo_review` added), and llms.txt / api.html / for-agents.html / README were ALREADY correct at ffc9a0b (18 tools, Bearer-JWT quickstart, `npx auxilo setup`, $0.05–$50). Done this wave: **openapi.json** 48→53 paths (adds `/auth/device` + `/status` + `/verify` + `/authorize`, `/account/terms-status`; fixes phantom `/admin/moderation/{id}`+`/resolve` → real `approve`/`reject`; machine-readable `TERMS_NOT_ACCEPTED` 403 schema on link-wallet + both withdraw rails; `/knowledge/{id}` documents the 401-first dualAuth reality + both 402 branches + 409 `CONTRIBUTOR_NOT_ONBOARDED` + 70/60 split + $0.05/$0.08/$50; contract test 20/20); **ERROR-CODES.md** → v0.9.1 full refresh (local-only); **UX-FLOWS** + **ONBOARDING-COPY** reconciled to shipped product (packs 400/80–1200/250–5000/1000 not 10,000/100; real `/account/earnings` field names incl. `unassented_pending`/`payouts_paused`; 503 pause first-class; §7.4-7.5 rewritten to shipped LW-16 seamless+7-day-retraction with pending_review as the exception; no fee-free-first-withdrawal claim); **README** `/learn` example fixed (identity gate). **prompts/** does not exist on this tree — the $0.005 guidance lives only on the retired wip snapshot; nothing to fix. ~~REMAINS: collapse `get_stats`/`get_knowledge_stats` dupes~~ **REMAINDER CLOSED 2026-07-20** (main `c076021`, DEPLOYED): `get_stats` REMOVED (exact dupe of `auxilo_stats`, both GET /stats — README itself called it "alias"); `get_knowledge_stats` KEPT (unique endpoint, no auxilo_* equivalent); tool count 18→17 swept across api.html (title/og/twitter/meta/body/card), for-agents.html, llms.txt, README, .well-known/agent.json; surface-parity + predeploy checks green; rides npm 0.9.4 (unpublished cut — no live client ever saw an 18-tool 0.9.4). 402-first redesign was AUD19-5 (shipped). **Static-path resolution (Wave 1, 2026-07-19):** the deferred §3.3-1 "402-first in static `dualAuth`" is CLOSED BY DELETION on `wave1-money-closures` — grep-verified zero route callers for `dualAuth`, and `x402Gate`'s only caller was `dualAuth` itself, so both deleted (spec §1.4; no tests asserted on either). The live path (`dualAuthDynamic` → `verifyPaymentOrReject`) already carries the AUD19-5 contract; openapi.json `info.version` 0.9.1→0.9.2 reconciled in the same wave (was failing the r01 launch-blocker version test at base). **Wave 2b note (2026-07-19):** new machine-readable codes shipped dark/undocumented and owed to the next ERROR-CODES/openapi reconcile pass: `PAYMENTS_DISABLED` (503 on all five money-movement surfaces incl. a pre-challenge 503 on GET /knowledge/:id when the global switch is off), `IDENTITY_REQUIRED` + `IDENTITY_CAPTURE_UNAVAILABLE` (link-wallet, CP-2 — only reachable when the counsel-gated flag flips), retraction 409 `published_at` now reports the approval-time window basis, and `/health` gained `payments_enabled`. |
| AUD19-14 | **Tracker staleness** — LW-3(a) envelope + AUD-B5 identity guard actually DONE in code; RUNBOOK §2 named a dead branch (fixed locally 2026-07-19: deployable tree = `r01-noncustodial-launch`); RUNBOOK §9 counts stale. | P2 | GOV-1 | ✅ **DONE 2026-07-19** — LW-3(a) row (§20) and AUD-B5 row (§24) reconciled with code evidence on this branch; RUNBOOK §2 fixed earlier; RUNBOOK §9 counts updated (local-only) from a full run on ffc9a0b: **679 tests / 675 pass / 0 fail / 4 skipped** — the audit's 11 "known-stale p2-1a failures" do NOT reproduce on r01 tip (they were a wip-branch artifact; the one cold-start failure seen in a bare worktree is environment-only: the test symlinks repo-root `node_modules`, absent in fresh worktrees). |

| AUD19-15 | **Discovery-premium attribution pre-auth staleness — CONFIRMED** (Gate-A reviewer, 2026-07-19, at 54a66f3 server.js:6777-6784): the discovery-premium `cacheKey`/`searchSourceCache` read uses the pre-auth `callerAccountId`, so on the API-key path `isFromSearch` is always false → contributor share is always 70% and the 60% discovery attribution is DEAD on the primary MCP path. Same bug class as the M-2 wash-guard pre-auth-null defect (fixed in econ branch; this cache read deliberately left for its own fix). Fix: read post-auth identity for the cache key. Note: fixing it makes discovery unlocks accrue at 60% — an economics-visible change; small build + Gate-A. | P1 | BUILD-2 + CAT-1 | ✅ **BUILT 2026-07-19** on `wave1-money-closures` (spec §1.1 — economics-visible, needs its own Gate-A before deploy). The read moved AFTER `dualAuthDynamic`, keyed `${buyerAccountId}:${id}` (write side verified already correct: POST /knowledge runs `optionalAuth()` before `recordSearchSource`). Gated `fundingSource === 'credit_pack'` — structurally credit-path-only; pre-settlement consumers (402 description, router split bps) pinned to `CONTRIBUTOR_SHARE_STANDARD` since x402 payers are anonymous and never qualify (x402 path byte-unchanged in behavior). Composes with AUD19-2: discovery accrual = basis × 60%. WAL/replay verified unchanged (stores derived amounts, replays verbatim — pinned by existing econ tests). Effect: discovery unlocks now accrue at the DOCUMENTED 60/40 (code was overpaying 70% vs published terms — no doc change needed). Stale pre-auth `callerAccountId` capture removed from the handler. Tests: `test/wave1-money-closures.test.js` + econ suite green. NOT deployed. |
| AUD19-16 | **USDC withdrawal timeout (202) path double-pays** — cross-session payout audit, VERIFIED against canon 2026-07-19: (A) `processing_timeout` settlements are reconciled ONLY by `resolveProcessingSettlements()` at STARTUP (hourly `resolveStuckSettlements()` filters `pending|retry|processing_unresolved` only, server.js:1391-1398) → during continuous uptime the timed-out withdrawal never debits `pending_balance`; after the 1h rate-limit the wallet re-withdraws the same balance → second on-chain payout. (B) even at restart, the success branch does `total_withdrawn += amount` WITHOUT `pending_balance -=` (server.js:1280) and the revert branch refunds a never-debited balance (:1285) — the hourly daemon does both (:1428-1429), proving the correct shape. Aggravators: single-slot per-wallet `createReservation` (:7272) under-counts in-flight; no in-flight guard in /withdraw. M-B clamp (:718-727) partially self-heals at next restart only. Extends S20-F3 (which flagged concurrency, not the debit asymmetry or liveness gap). | **HIGH — must-fix before `CUSTODIAL_WITHDRAW_ENABLED` ever flips** (moot if the legacy USDC rail never reopens per SPEC-AUD19-7 DASH/ROUTE + Tyler's rail decision) | BUILD-1 → BUILD-2 + GOV-3/SPEC-1 | ✅ **BUILT 2026-07-19** (branch `wave5a-money-closures`, spec BUILD-SPEC-WAVE5A §3; the rail STAYS kill-switched — reopening remains counsel-gated per Tyler): (1) timeout model inverted — debit at broadcast (same `debitWithdrawableBalance` shape as the confirmed path; broadcast disposition + tx_hash recorded in the WAL payload BEFORE the debit via new `updateWalPayload`; settlement stamped `debited_at_broadcast`), refund only on definitive on-chain failure via marker-guarded `refundDebitedSettlement` (`<id>:refund` marker — pending += / total_withdrawn -= / count -= exactly once); resolver asymmetry fixed BOTH directions (legacy-undebited success now debits the correct dual shape — the lone `total_withdrawn +=` is gone; legacy revert/never-broadcast no longer refunds a never-debited balance); (2) hourly liveness — `resolveStuckSettlements` tail-calls `resolveProcessingSettlements` every tick (reentrancy-guarded), and the S9-4 processing_unresolved branches are debited-aware; (3) 409 `WITHDRAWAL_IN_FLIGHT` guard in /withdraw (pre-nonce + re-checked under the wallet lock); (4) reservations keyed by settlement id (`wd_*` ids cannot collide with `0x*` legacy keys; wallet-key fallback preserved); WAL withdraw-recovery extended to the new model (both-steps WAL leak closed, disposition-aware replay, corrected legacy debit shape, marker-guarded — no double-debit under replay); settlement rows now carry `created_at` (unblocks the 48h force-fail age check that a missing created_at deferred forever). Documented residual (pre-existing, unchanged, kill-switched rail): crash in the window between broadcast return and the disposition write. Tests: `test/wave5a-money-closures.test.js` W-series; openapi /withdraw 409 documented. NOT merged/deployed. Source: cross-session payout audit 2026-07-19. |

> **2026-07-19 Wave 3.4 (internal-phases plan, last queued build):** `wave34-scoped-keys` (from b59579b) — D2 scoped-keys tightening + NF-3 `auxilo init` BUILT, awaiting Gate-A + merge (no push, no deploy): scope taxonomy read < earnings-read < contribute < admin single-sourced in lib/accounts.js (server.js duplicate deleted); earnings gated earnings-read; admin never issuable (both creation paths + device flow); grandfather via `effective_scope` (legacy read ⇒ earnings-read, enforcement-only — stored scope/display untouched; every enforcement site audited to use it, forget-fails-closed); dualAuthDynamic exact-match unlock bug fixed (contribute keys were 403'd on API-key/credit unlocks); rotation endpoint + self-only key rotate/revoke + 10-key cap on the (previously uncapped) device-authorize path; `auxilo init` zero-prompt scoped CI onboarding (client bits POST-0.9.3 → npm 0.9.4, NO version bumped). Spec `~/.auxilo/handoffs/BUILD-SPEC-WAVE34-2026-07-19.md` (route→scope audit table §3; tests before implementation); tests: new `test/wave34-scoped-keys.test.js` (52); **full suite 992 tests / 988 pass / 0 fail / 4 skip (baseline b59579b 940/936/0/4 — nothing regressed, +52)**. Doc impact owed at merge: AGENT-LEARNING-GUIDE.md + docs/UX-FLOWS.md (scope model + init flow; docs/ untracked in build worktree), RUNBOOK §5 no change (no new env).
> **2026-07-19 Wave 2b (internal-phases plan §2.3–2.5 + carry-ins):** `wave2b-ops-hardening` (from 6e90b2a) — server-side ops hardening BUILT, awaiting Gate-A + merge (no deploy, no push): **H-3** OFAC-refresh failure alerting (§19 row); **RUNBOOK §9** learnings.json store-level write mutex (`lib/learnings-lock.js`, earnings-lock idiom; 14 runtime writer sites wrapped incl. unlock post-settle + retraction + cron + self-review/admin/bulk + adoptions + pipeline; **lock-order rule: account → earnings → learnings, learnings innermost/leaf**, test-enforced — no earnings/account acquisition and no `sweepHeldEarnings` under a held learnings lock; boot-time writers exempt by design; cross-process risk already gone — retraction-sunset LaunchAgent retired per P1-13a); **RUNBOOK §5** `PAYMENTS_ENABLED` global kill switch (`lib/payments-switch.js`, default ON/absent=enabled, off-values false/0/off/no; 503 `PAYMENTS_DISABLED` at dualAuthDynamic top [credit deduct + x402 settle + cold 402 mint], /checkout/session, /webhook/stripe [503-so-Stripe-retries = self-healing], both withdraw rails LAYERED ABOVE `CUSTODIAL_WITHDRAW_ENABLED`; reads/search untouched, test-pinned; `/health.payments_enabled`); **CP-2 CODE-DARK** (§22 row); **CAT-1 task-#13**: (a) `/pricing/categories` + `/contributor/:wallet/pricing-insights` now filter through the single shared `visibleCatalog()` predicate (closes CAT-1 §7's 940-vs-58 public leak + a pending-TITLE leak in top_earning_learnings; matchLearnings//knowledge/stats/SSR refactored onto the same helper), (b) unlock counters gated `countersCredited = !accrualCapped && !isSelfUnlock` — `quality.unlocks` (feeds computeScore/search/stats/top_learnings) and demand windows bump only when credited; new ops-only `quality.unlocks_total` keeps raw volume observable (CAT-1 §5: self-unlocks can no longer buy ranking points or 1-day demand spikes; residual x402-sybil demand bump accepted/report-only); **Wave-1 reviewer carry-ins**: F1 discovery-cache restore in the refund catch (retry keeps its 60% attribution, original timestamp), F2 `unrecordAccrual` moved BEFORE the `refundCredit` await (capped-race closed; refund-failure mode analyzed — un-armed is correct since the WAL is already aborted), F4 purchase-ledger write mutex (STORE-level, deliberately not account-keyed — one shared file makes account-keyed unsound under async I/O; argued in module header; compaction/sharding noted future; recordPurchase now async, call sites fire-and-forget `.catch`); **SPEC3 slice B4 (live bug)**: DELETE /learn/:id 500 for never-consented accounts fixed (null-guarded `getConsentState` → truthful `'none'` consent stamp; root cause: null deref inside the audit try misreported as `audit_integrity_error`) + retraction window basis = approval time (self_review_action/moderation_action stamps) with created_at fallback for seamless publishes, 409 reports the enforced basis. Spec `~/.auxilo/handoffs/BUILD-SPEC-WAVE2B-2026-07-19.md` (test cases before implementation); tests: new `test/wave2b-ops-hardening.test.js` (49) + sanctioned pin updates (aud19-2-econ demand-gate regex, wave1 ledger awaits, funnel/cp6 link-wallet slice windows, p2-1a retraction-consent pin superseded by B4-1 with CORRECTION-1 spirit preserved); **full suite 895 tests / 891 pass / 0 fail / 4 skip (baseline 846/842/0/4 — nothing regressed, +49)**. RUNBOOK copy-backs owed (local doc): §5 add PAYMENTS_ENABLED row + CP2 env pair; §9 strike the learnings-mutex SHOULD-FIX.
> **2026-07-19 Wave 1 (internal-phases plan):** `wave1-money-closures` (from 18262e6) — money-path code closures BUILT, awaiting Gate-A + deploy: AUD19-15 discovery attribution post-auth (60% share live on the credit path — economics-visible), AUD19-10 compensating credit refund on delivery failure (abortWal + lot-true refundCredit + cap un-arm), LW-7 rating auth + durable purchase ledger (breaking; MCP description rides **npm 0.9.3**), AUD19-13 static `dualAuth`+`x402Gate` deleted (zero callers) + openapi 0.9.2 version reconcile, reviewer-debt batch (`_custodialAccepts` single challenge source + three-way test, X-Payment-Required on all five 402 emitters, eip712 interval `.unref()`, per-category ops-alert rate limits). Spec `~/.auxilo/handoffs/BUILD-SPEC-WAVE1-2026-07-19.md`; suite 823 tests / 819 pass / 0 fail (+31 vs base incl. 30-test `test/wave1-money-closures.test.js`; geo-embargo E-2 re-scoped to live settle sites after the dead-code deletion).
> **2026-07-19 phase-3 (same session):** econ-fix-aud19-2 (AUD19-2 basis+cap+wash-guard fix, Gate-A SHIP) + payment-contract-aud19 (AUD19-5 402-first + AUD19-7 DASH/ROUTE + verify-wallet whitelist, Gate-A SHIP) merged and DEPLOYED **v37** (792 tests/0 fail; live-verified: cold unlock now 402+accepts[]+options). **BRANCH UNIFICATION DONE:** main absorbed r01 at `2eb880d`, tree byte-identical to prod — main is the sole canon (RUNBOOK §2); r01 retired; PR #14 closed as superseded. External uptime+cert monitor ACTIVE (.github/workflows/uptime.yml, both hosts, 30-min cadence). npm **auxilo-mcp@0.9.2 PUBLISHED 2026-07-19** (Tyler; registry tarball shasum verified byte-identical to the reviewed tree, `latest` tag confirmed, wrapper `auxilo` ^0.9.0 resolves to it) — carries quality passthrough, link two-step, DASH/ROUTE withdraw, 402 handling, auxilo.io default (retroactively fixes the dead-host installer for all future cold installs), renderly tools dropped.
> **2026-07-19 phase-2 (same session):** funnel-fixes-aud19 (Gate-A: SHIP-WITH-FIXES → fixed → SHIP) + docs-reconcile-aud19 merged and DEPLOYED v36 (health-verified; openapi 53 paths live). AUD19-4/6/8 server-side LIVE; AUD19-3 live incl. account-bound EIP-712 link challenge + platform-wallet refusal. MCP half rides npm auxilo-mcp@0.9.2 (Tyler-gated).
> **2026-07-19 reconciliation (same session):** prod v34 == `r01-noncustodial-launch` tip `d4eb050` byte-for-byte (144/144 shipped source files). `wip-snapshot-2026-07-04` retired — final snapshot commit `fea3136`, unique PUNCH-LIST prose ported here (CP-6 status, CP-7, LW-5). Working branch for these fixes: `audit-quick-wins-2026-07-19` (from d4eb050).

## 27. Marketing-Copy Launch-Quality Review (2026-07-19)

6-seat copy panel (direct-response, advertising, conversion, positioning, voice, buyer-job lenses) over the 7 public marketing pages. Contested findings were re-judged in a fully-briefed adjudication round (Tyler ruled the panel's assessment governs): **all four contested seats updated toward the 2026-07-18 locked decisions on their own reasoning** (D1 hero + single-conversion E1 stand), contributing new riders, one consensus work item (demo-section conversion, in COPY-2), staged triggers (COPY-8), and a hero-resolve challenger test plan. Build spec: `~/.auxilo/handoffs/BUILD-SPEC-COPY-LAUNCH-2026-07-19.md` (§1 adjudication record).

| ID | Item | Priority | Owner | Status |
|---|---|---|---|---|
| COPY-1 | Mechanical copy bugs: stale "17 Auxilo tools" (for-agents), dead `/#api-integration` anchor (how-it-works close), "$0.08+ avg" mislabel → "starting price", for-builders og/twitter title mismatch, "seamless" ×4 + JSON-LD mirrors | P1 | BUILD-3 | ✅ **BUILT 2026-07-20** (merged `c1b2426`; **DEPLOYED + live-verified 2026-07-20** (gitSha battery green)) — 17→18 + dead anchor done earlier same-day by concurrent session; this wave: seamless purge incl. dashboard + JSON-LD parity, og/twitter aligned, 'Read the API docs' label, how-it-works og/twitter verb metas |
| COPY-2 | Homepage reframe (spec WP-1): add the missing $0 self-unlock proof block (locked memory-first position's core fact, absent site-wide; cross-operator-reuse closing guard + present-tense client-scope rule), replace investor-voice "Competitive Moat / Why Auxilo Wins / flywheel" section with reader-benefit + real-moat copy, present-tense the market headline, demo-section conversion + hinge line (WP-1d, adjudication consensus) | P1 | BUILD-3 + GROWTH-1 gate | ✅ **BUILT 2026-07-20 complete** — WP-1b/1c/1d merged main `c1b2426` (Gate-A SHIP + voice-gate fixes); **WP-1a un-held and shipped same day** after §31 DR-8 option (a) made the $0 claim true in code: `own-learnings-free` section after the hero ("Your own learnings come back free."), spec raw material + closing-turn guard + client-GA rider verified, copy gate SHIP-WITH-FIXES applied ("hits the same wall" → "runs into the same problem", breaking the double echo with the WP-1d hinge), dash-free, no CSS change (no cache-buster bump), render-QA'd desktop+mobile. Advisory for Tyler on file: "starts working the moment you connect" vs "Extraction defaults to off" trust bullet — coherence nuance, gate ruled non-blocking. |
| COPY-3 | CTA layer completion post-E1 (spec WP-2): nav "Get Started" → action-named, how-it-works close, earnings hero/mid CTAs, sign-in expectation setters, pricing dual close | P1 | BUILD-3 | ✅ **BUILT 2026-07-20** (merged `c1b2426`; setter lines rewritten to the verified real flow: CLI or POST /account/api-keys) |
| COPY-4 | Builder-verb accrual realignment while withdrawals paused ("Start getting paid" → "Start banking earnings" etc.) + voice mechanicals + terminology canon (unlock/builder/agent; "purchase"/"contributor" retired from human copy) | P1 | BUILD-3 | ✅ **BUILT 2026-07-20** (merged `c1b2426`; final verb 'Your earnings start now.' — voice gate rejected 'banking' for paused-rail collision) |
| COPY-5 | for-agents H1 promotion + free-search empty-shelf risk-reversal; pricing H1 promotion (spec WP-5/6a). "Request a learning" mechanism deliberately excluded (future product item) | P1 | BUILD-3 | ✅ **BUILT 2026-07-20** (merged `c1b2426`; risk-reversal written count-free per stat-duality rule) |
| COPY-6 | Credit-pack CTA + 70%-split copy qualifier — **gated on AUD19-2 decision**; no funding copy until it lands | P1 | BUILD-3 + CFO-1 | ON HOLD |
| COPY-7 | Fold panel voice laws + verb policy into private-canon BRAND_GUIDELINES.md / GTM.md — **7 laws per Tyler 2026-07-20** (affirmative-contrast/negation-ban law struck as over-correction to the AI-tell; negation framing = case-by-case craft call). Canonical list: spec §4 T3. | P2 | GROWTH-1 | ✅ **DONE 2026-07-20** — search of ~/dev + ~/Documents found NO current private-canon checkout: only a stale pre-move clone (`~/Documents/Custom/auxilo`, BRAND_GUIDELINES.md mtime 07-12, GTM.md mtime 07-03 — both predate the panel; likely casualties of the 2026-07-01 repo move). Standing home written instead: **docs/BRAND-VOICE.md** (untracked=private): 7 laws, the struck 8th law + rationale, verb policy ("Your earnings start now." ratified; "banking" rejected for paused-rail collision), value-prop hierarchy (cross-ref PRODUCT.md §2 as its canonical home), + status-of-related-canon notes giving Tyler the restore-or-keep decision. Tracked INDEX.md deliberately untouched (public-only index). |
| COPY-8 | Staged copy triggers (spec WP-7): Show HN gate → homepage buyer-demand rebalance (catalog >100 + ≥5 real builders; non-self-unlock guard metric) · UC launch (§21) → universal automatic-capture copy sweep · mem0-tell signal in 30-day window → hero-resolve challenger A/B (challengers on file, spec §1) | P1 at trigger | GROWTH-1 + BUILD-3 | DEFERRED (staged) |

> Verification gates in spec §5 (grep gates, SSR `lc-*` anchor integrity, FAQ JSON-LD parity, truth bounds, drift guard, visual QA). Gate flow per D4: Gate A → BUILD-3 + SPEC-2 + GROWTH-1 → Tyler deploys.

## 28. CAT-1 Catalog-Health Pass (2026-07-19)

Catalog-health pass over the live marketplace (health indicators H1–H12, demand-multiplier surface, dedup/category balance). Full findings with evidence: `~/.auxilo/handoffs/CAT1-CATALOG-HEALTH-2026-07-19.md` (PRIVATE — keep out of this repo). The pass's report-only/watch items live in the handoff; tracked build items below.

| ID | Item | Priority | Owner | Status |
|---|---|---|---|---|
| CH-1 | **Public pricing analytics leak raw-catalog visibility** (handoff §7 + H11 FAIL): `GET /pricing/categories` iterated the raw `learnings` array with no status filter → publicly reported 940 learnings (incl. pending_review/rejected/retracted) vs the 58 visible per `/knowledge/stats`, with pending default prices mixed into public avg_price — the exact inflated-stats class that was a 2026-06-12 launch blocker. Same-class audit hit CONFIRMED on `GET /contributor/:wallet/pricing-insights` (unauthenticated), which additionally exposed **titles** of non-approved learnings via `top_earning_learnings`. | P1 | CAT-1 + BUILD-2 | ✅ **FIXED 2026-07-19** (spec note `~/.auxilo/handoffs/SPEC-CAT1-VISIBILITY-2026-07-19.md`). Both endpoints now draw from `visibleLearningsList()` — the SAME predicate as `/knowledge/stats` (LW-QA) / search / `GET /knowledge/:id`; a wallet whose learnings are all non-approved now 404s from pricing-insights (matches `GET /knowledge/:id` posture). Regression: `test/pricing-visibility.test.js` — structural predicate-agreement guards (CI-enforcing) + behavioral boot against a mixed-status fixture asserting `/knowledge/stats`, `/pricing/categories`, and pricing-insights totals agree. **Gate-A adversarial review 2026-07-19: SHIP (clean)** — reviewer reproduced every test leg and swept ALL other raw-`learnings` read sites; one same-class catch fixed in the same wave: `POST /discover` `knowledge_hint` advertised the raw catalog count (server.js:4758, unauthenticated) → now `visibleLearningsList()`, structural guard added. Suite-hygiene riders in the same wave: stale `0.9.1` literal in test/r01-launch-blockers.test.js → 0.9.2 (missed by 3e3587a), and test/cold-start-seed.test.js behavioral leg now resolves the real node_modules dir (was staging a dead symlink in git worktrees → false FAIL; the AUD19-14 "environment-only" note, now cured). Full suite 797/793/0 fail. NOT deployed — rides next Fly deploy. |
| CH-2 | **`GET /contributor/:wallet` `learnings_submitted` counts non-approved submissions** (Gate-A reviewer, 2026-07-19; also noted in the CH-1 spec note): unauthenticated route counts pending/rejected/retracted for ANY wallet (server.js:7382, 7395) — count-only (no titles/prices), but it reveals a wallet has hidden submissions and now disagrees with the fixed sibling `/pricing-insights` (visible-only / 404). Deliberately NOT changed in CH-1: "submitted" is arguably the builder's own-tally semantics. Needs an intended-semantics decision (own-dashboard vs public predicate) before the pair is called consistent. | P2 | SPEC-3 | ✅ **RULED (b) + FIXED 2026-07-19** — public predicate everywhere. SPEC-3 rationale: the route is unauthenticated for any wallet, so "builder's own tally" semantics don't hold without an auth change (breaking, out of class); no consumer renders the field (dashboard.html uses the authed `/account/earnings` path; repo-wide grep — only openapi.json types it), so builder-UX cost is nil; the raw count was the last public per-wallet hidden-submission oracle after CH-1. Both branches (no-earnings + earnings) now `visibleLearningsList().filter(...)`. openapi.json field gains a description pinning visible-only semantics. Regression: `test/pricing-visibility.test.js` — field-anchored structural guard (both branches must use `visibleLearningsList()`, raw `learnings.filter` banned on the field) + behavioral asserts (mixed wallet → 3, pending-only wallet → 0). Known UX-FLOWS.md staleness on this route's response shape predates CH-2 and is queued for the AUD19-13-class docs reconcile. **Gate-A adversarial review 2026-07-19: SHIP (clean)** — reviewer mutation-tested the structural guard (fails closed on one-branch revert, both-branch revert, and helper-extraction refactor; no false fire), re-swept all raw-`learnings` reads (no remaining unauthenticated per-wallet hidden-count surface; settlements route reads ledgers only), verified counts arithmetic both ways, and reproduced the suite (798/794/0, behavioral leg booted). Non-blocking notes: the CH-1-class port-3000 boot contention can loudly skip the behavioral leg under the parallel runner (structural guard remains enforcing), and dead `getCatalogStats()` (server.js:2888, zero callers) computes raw counts — flagged for removal. Rides the CH-1 wave commit + next Fly deploy. |
| CH-3 | **Dead code: `getCatalogStats()`** (CH-2 Gate-A reviewer debt, 2026-07-19): unused function in server.js computed `total`/`categoryCounts` over the RAW `learnings` array — zero callers repo-wide, but a visibility-leak footgun if ever wired to a public route (the exact inflated-stats class CH-1/CH-2 fixed; public surfaces must use `visibleLearningsList()`). | P3 | CAT-1 | ✅ **REMOVED 2026-07-19** — function deleted from server.js; repo-wide grep confirms zero remaining references; full suite green after removal (852/848/0 fail, 4 skipped). Rides the CH-1/CH-2 wave deploy. |
| CH-4 | **Density-gate the uniqueness multiplier** (handoff §1-2, H4/H7 FAIL; **Tyler-ratified 2026-07-19**): at 58 items with zero tag-similar pairs, 100% of the catalog carried the 3.0× "first of its kind" premium (pricing.js) → 52/58 items failed `calculateVerdict` as "expensive" (2.4-3.7× DIY cost), $2.30 = 39.7% of catalog — prices carried zero market signal. **Decision:** the uniqueness premium applies only when the catalog is dense enough for uniqueness to be informative — capped at 1.5× until the catalog reaches 200+ items, full 3.0× only when an item ALSO has real tag-neighbors to be unique against. | P1 | BUILD-2 + CAT-1 | ✅ **BUILT 2026-07-19** (branch `pricing-density-gate`, spec `~/.auxilo/handoffs/BUILD-SPEC-PRICING-DENSITY-2026-07-19.md` — test cases first). Pure function of (visible-catalog size, tag-neighbor count) — no dates/flags, self-relaxing: premium tiers require ≥200 market-visible items AND ≥3 same-category visible items sharing ≥1 tag (neighbor def nests under the tag-Jaccard similarity tooling: similar ⊂ neighbor; ≥3 mirrors the ≥3-ratings corroboration convention). Rollout with ZERO server.js changes: `getCurrentPrice` now re-derives `base_price` through the gated formula when the catalog is supplied — reachable only from the daily cron, whose existing 15%/day cap converges the live shelf in **5 cron runs** ($2.30→$1.15); the 6 undamped no-pricing items keep their `unlock_price` short-circuit ($0.067, no shock). Fixture mirroring the live shelf: verdicts flip 52-expensive/6-strong_buy → 6/46-consider/6 (expensive 10.3% — H4 PASS). Econ basis unaffected (accrual amounts stored at unlock; WAL replays stored amounts). Regression: `test/pricing-density-gate.test.js` (23 tests incl. structural guards pinning the cron cap + catalog pass-through; negative control: 13/23 fail against ungated engine); full suite 1020/1016/0 fail/4 skipped. **Known residual (spec §2.5, deliberate, pinned by test):** at the ratified 1.5 cap a cold item self-scored >~13/20 still lands "expensive" (ratio 1.5×q×0.8412 > 1.5) — as triage drains the 65%-at-18-20 pending queue, watch H4 trend; follow-up knob is verdict/quality rebalance, not the gate. Doc impact: PRICING-STRATEGY-V2.md §1 Component 3 (private canon, absent from this public checkout — flag for canon-side reconcile). **Gate-A adversarial review 2026-07-19: SHIP-WITH-FIXES** — convergence math vindicated (reviewer reproduced the 5-step path and the floor-mid-walk clamp semantics; compounding-decay objection wrong); gate math/visibility/negative-control confirmed. Fixes applied same wave on the branch: **F1** — three loud `test()`-level request-path pins (search server.js:6893, unlock :6977, homepage displayPrice :9067 each short-circuit on `pricing?.current_price ||` BEFORE any engine call; whitespace-normalized exact-chain match), each verified to KILL its engine-first reorder mutation (fail 1 / exit 1) — closes the M5/M6 gap where the only prior guard was a describe-body assert that exits 0 (see CH-7); **F2** — same-contributor (account-id OR wallet, truthiness-guarded) items excluded from `countTagNeighbors`, ONE-DIRECTIONAL (own items still count as similar and keep lowering premiums; they can never open the gate) — own-filler farm mutation-killed (fail 2 / exit 1), genuine 3-neighbor case still opens. **Merge flag for the server.js wave:** the submission synthetic (server.js:5296) carries no contributor identity, so F2 binds from the first cron refresh (~1 day) rather than at submission — one-line hardening (add `contributor_account_id`/`walletLower` to `syntheticForPricing`) closes it at submission too — **CLOSED in Wave-5A 2026-07-19** (synthetic now carries both identity fields; pinned in `test/wave5a-money-closures.test.js`). Suite post-fixes 1028/1024/0 fail/4 skipped. Follow-ups: CH-5 (dated verdict/quality rebalance), CH-6 (rating replace-semantics), CH-7 (describe-body test debt). NOT merged/deployed — merge with the parallel server.js wave. |
| CH-5 | **Verdict/quality rebalance — DATED, blocks bulk triage** (CH-4 Gate-A reviewer + spec §2.5, 2026-07-19): at the ratified 1.5 uniqueness cap, verdict ratio = 1.5 × quality × 0.8412 (cold demand) exceeds the 1.5 "expensive" threshold for any self-score total > ~13.1/20 — and the extraction quality floor is **14**, so **100% of pending-queue survivors land "expensive" the moment they are approved** (the 65%-at-18-20 band lands at ratio 1.78-1.89). Freshness compounds it: the 1.25/1.15/1.05 first-30-day premium makes every new item born-expensive regardless of QA (even no-QA: 1.5×1.14×0.8412×1.25 = 1.80). Reviewer's projection: H4 re-blows within ~2 bulk-triage sessions (~150 items/session per CAT-1 cadence). Follow-up knob is the verdict thresholds / quality-vs-verdict interplay, NOT the density gate. | P1 | CAT-1 + SPEC-2 | ✅ **BUILT 2026-07-19 — DEADLINE MET (pre-bulk-triage)** (branch `wave5a-money-closures`, spec BUILD-SPEC-WAVE5A §1): option ruled = **recenter the DIY comparison** — `calculateVerdict` now compares price to a VALUE benchmark, `estimateValueBenchmark = DIY × quality × freshness` (both factors scale price and delivered value identically, so they cancel out of the overpricing test; the 0.3/0.75/1.5 thresholds are untouched). Formula-priced ratio reduces to uniqueness × demand: at the ratified 1.5 cap, cold = 1.5 × 0.8412 = 1.262 → **every q14-20 item lands "consider" on arrival, in every freshness bracket** (fresh-q19 wall-test case pinned). Anti-inflation constraint held: uniqueness and demand deliberately stay OUT of the benchmark, so the ungated 3.0× shelf still reads expensive — fixture BEFORE unchanged (52-expensive/6-strong_buy, identical to live), fixture AFTER improves from CH-4's 6/46/6 to **0-expensive/52-consider/6-strong_buy**. CH-4 residual pin INVERTED per its own note + anti-whitewash pin added (`test/pricing-density-gate.test.js`); V-series in `test/wave5a-money-closures.test.js` incl. the community-override property (≥3 DISTINCT bad ratings SHRINK the benchmark → verdict correctly degrades). Recorded tradeoff: contributor-PRICED (fixed unlock_price) items gain ≤1.32× benchmark from self-score — policed by the CI-7 wall-test screen, the 14/20 extraction floor, and the CH-6 distinct-rater blend. openapi verdict enum corrected (was stale fair_price/overpriced). PRICING-STRATEGY-V2.md §7B: canon-side reconcile flagged (same handling as CH-4). NOT merged/deployed. |
| CH-6 | **Rating sybil reaches the base price — per-account replace semantics needed** (CH-4 Gate-A reviewer "F4", 2026-07-19; predates the branch): one account with a single cheap unlock can submit repeated ratings that APPEND to `quality.helpfulness_scores`, reaching the `ratings >= 3` activation gates ALONE (~2h under current limits) and flipping BOTH the rating multiplier (0.70-1.10) and the community-quality blend (60% weight) — a ±~20-34% price swing from one sybil. CH-4's base refresh WIDENS the reach: community ratings now flow into `base_price` via the daily cron (previously frozen at submission), so the sybil moves base, not just the current-price multiplier. | P2 | BUILD-2 + GOV-3 | ✅ **BUILT 2026-07-19** (branch `wave5a-money-closures`, spec BUILD-SPEC-WAVE5A §2): per-account REPLACE semantics — `quality.rater_scores` is a one-slot-per-account map; re-rating overwrites (response + JSONL carry `replaced`); BOTH ≥3 activation gates (community-quality blend AND rating multiplier, lib/pricing.js) now count DISTINCT raters via `distinctRaterCount` (map size; `ratings` fallback only for FROZEN pre-fix shapes — any new rating migrates them, so the fallback cannot be sybil-grown); lazy per-learning migration freezes pre-fix aggregates into `legacy_ratings`/`legacy_helpfulness_sum` (still contribute to count/avg, contribute ZERO to the gates); `ratings.jsonl` stays the durable event log — additive `replaced` field, appended BEFORE the catalog write, and folding last-write-wins per (learning, account) reproduces the map exactly; `stripOpsCounters` strips the rater map + legacy buckets from every buyer-facing quality object (rater account ids never reach buyers). LW-7 purchase gate + 1h account cooldown intact. Wash-trade parity verified (AUD19-2 class): reaching ≥3 now requires ≥3 distinct funded accounts EACH with a real unlock of the target — the same per-account cost structure as the unlock-side self-dealing guards. Tests: R-series in `test/wave5a-money-closures.test.js`. NOT merged/deployed. |
| CH-7 | **Describe-body asserts fail silent under the suite runner** (CH-4 Gate-A reviewer debt, 2026-07-19): assertions placed in `describe()` bodies instead of `test()`/`it()` bodies — e.g. test/r01-launch-blockers.test.js:342's search-chain guard — print ✖ on Node v24 under the exact `npm test` flags (`node --test --test-force-exit test/*.test.js`) but report **fail 0 / exit 0**: CI-green silent failure. The M5 search-chain mutation was "caught" only by this class until CH-4 F1 added loud pins. | P2 | BUILD-4 | ✅ **BUILT 2026-07-19 (Wave-5B, `wave5b-review-closures`; spec §6)** — semantics verified empirically on Node v24.13 under the exact npm-test flags: describe-body assert ⇒ fail 0/exit 0 (silent); before() hook ⇒ exit 1; test() ⇒ fail 1/exit 1. Lexer-grade scanner (strings/templates/comments/regex-literals/char-classes; `it('x',{timeout},fn)` options-object braces; auto-detects file-local assert-bearing helpers — catches the r01:342 `sliceHandler` class, not just literal asserts) found **26 true sites in 7 files** (aud19-2-econ ×3, aud19-payment-contract ×2, r01-launch-blockers ×5, wave1-money-closures ×3, wave2b-ops-hardening ×3, wave34-scoped-keys ×4 blocks incl. the T9b literal-assert extraction, x402-router-server ×1); all converted to `let X; before(() => { X = … })` (loud). Meta-guard `test/ch7-describe-body-guard.test.js`: scanner self-tested against known-bad + known-good fixtures, then sweeps test/*.test.js and pins ZERO findings — the class cannot re-enter. All 250 tests in the converted files still pass. |
| task-#19 | **Drive-ID scrub gap (found LIVE, 2026-07-19):** a private Google Drive doc ID survived the scrubber — `lib/sensitivity-filter.js` had zero Google Drive/Docs/Sheets/Slides patterns. A Drive ID is a capability reference (link-shared docs are readable by anyone holding the ID) — credentials-class, not merely PII-class. | P1 | GOV-3 + BUILD-2 | ✅ **BUILT 2026-07-19 (Wave-5B; spec §4)** — filter v0.4.0→**0.5.0**: `google_drive_url` (the /d/<id> shapes across document/spreadsheets/presentation/forms/drawings/file + open?id=/uc?id=/drive/folders/folderview?id= + /u/N/ variants; unconditional) + `google_drive_id` (bare `(1|0B)[A-Za-z0-9_-]{24,63}` with boundaries AND a `validate` hook requiring uppercase/`-`/`_` — argued FP posture: the filter is a hard 422 at /learn, so the guard excludes 40-hex lowercase git SHAs/long words/decimal runs at a computed miss cost ≈1e-6, and the URL rule still catches every linked form). New optional per-pattern `validate` hook in scanLearning + scanText. Ships both sides (lib/ is in the npm files list; server side live at deploy). Tests incl. the real-world leak shape (docs.google.com/document/d/<44-char id>/edit + its bare ID) + negative controls; version pin in p2-1a-sensitivity-filter-v04 updated (sanctioned — §7.6 bump-on-pattern-add convention). **Gate-A F3 (before the 0.9.4 cut):** reviewer probed six real bare-ID FP classes that hard-422'd (issue-numbered branch names, numeric-leading slugs, digit-1-leading UUIDs ~6%, numeric-separator literals, kebab date-ranges, UPPERCASE 40-hex SHAs) — validate tightened to mixed-case+digit (`/[A-Z]/ && /[a-z]/ && /\d/.test(m.slice(1))`); all six shipped as negative tests, both real-ID fixtures still flag, computed miss cost ≈0.4% (dominated by no-digit IDs; URL rule catches every linked form). Mutation-verified (reverting the guard fails the suite). |

| CI-RED-0726 | **Main CI red undetected ~30h (found 2026-07-26 via Codex SPEC3-A3 base-verification STOP — the discipline working):** TWO independent breaks. (1) Security Audit job red since ≥2026-07-25 21:24 on upstream `fast-uri` HIGH advisories (GHSA-4c8g-83qw-93j6 + GHSA-v2hh-gcrm-f6hx; transitive via @modelcontextprotocol/sdk→ajv→fast-uri@3.1.2) — external event, no code change. (2) Test job red since the `d9cb31b` 0.9.6 bump: package.json went 0.9.6 but package-lock/openapi.json/two hardcoded test assertions stayed 0.9.5 → 2 failing tests. Every PUNCH-LIST record push since (predecessor's and acting PM's tonight) went up without watching CI — local Gate-A suite runs were honestly green (local doesn't run npm audit; drift landed post-Gate-A). **FIXED 2026-07-26:** lock regenerated at 0.9.6; openapi.json→0.9.6; both hardcoded version assertions converted to CONSISTENCY assertions (openapi==pkg; lock==pkg both fields — the partial-bump class now fails loudly); fast-uri→3.1.4 (lock-only transitive bump); suite 1396/1391 pass/0 fail/pin-match; `npm audit --audit-level=high` exit 0; repo-wide 0.9.5 sweep clean. **METHOD AMENDMENT: every push watches CI to completion — record-class pushes included.** Residual (non-blocking, tracked): moderate advisories remain (hono ≤4.12.26 path-traversal on Windows serve-static — prod is Linux; @hono/node-server <2.0.5; body-parser DoS) — schedule a dependency-refresh wave with full Gate-A, do NOT fold into an active build. **CORRECTION (same night): the first fix push (`3bc7a39`+`77ec3ed`) was recorded "CI verified green post-push" BEFORE watching — premature, and FALSE: the Predeploy Guard job (check-surface-drift, a third break surface) caught TWO MORE stale version strings at **0.9.4** (mcp-server.js:163 — shipped inside published npm 0.9.6! — and .well-known/agent.json:4) that the 0.9.5-pattern sweep structurally missed. Both → 0.9.6; local pre-push verification EXTENDED to the full CI job set (`predeploy-check.sh` + suite/pin + `npm audit --audit-level=high`), not suite alone — local suite never ran the drift check, which is why two PMs missed it.** | P1 | GOV-1 + BUILD-4 + GOV-3 | ✅ **FIXED 2026-07-26 across ALL four version surfaces; CI green verified by watching the run to completion (this time actually). Moderates follow-up OPEN; mcp-server.js 0.9.4 string rides inside published 0.9.6 artifact (cosmetic, corrected at 0.9.7).** |

## 29. SPEC-3 Builder Review Loop (2026-07-19)

Design spec: `~/.auxilo/handoffs/SPEC3-BUILDER-REVIEW-LOOP-2026-07-19.md` (seven verified breaks B1–B7; three-lane state model; Moves 1–3; D1 consent decision). The shipped loop made the builder a full-time librarian (441 items silently accumulated, "4 clean / 0 approvable"). Slices per spec §8; every slice Gate-A + marked Gate-B.

| ID | Item | Priority | Owner | Status |
|---|---|---|---|---|
| SPEC3-D1 | **Standing-consent decision** — may clean, scored, extraction-channel learnings publish without a per-item human action, for accounts that opt in? Options A/B/C in spec §9. | P1 | Tyler + GOV-2 + GOV-3 | **DIRECTION RATIFIED 2026-07-19 — Option B** (new explicit standing-consent artifact, default off, own dated version, never agent-mediated). Consent-text/ToS wording remains **counsel-gated**; C1 builds dark, activation (flag flip) is Tyler+counsel's. |
| SPEC3-A1 | Score-at-extraction (client): extractor rubric + `submission_channel:'extraction'` passthrough | P1 | BUILD-2 + SPEC-2 | **BUILT-DARK 2026-07-19** (`wave3-client-funnel`, rides npm 0.9.3) — `AUXILO_SCORE_EXTRACTION` env gate, default OFF; channel marker sent unconditionally. MUST NOT arm before SPEC3-B1 is live (activation order: B4 → B1 server → 0.9.3 → env flip). |
| SPEC3-A2 | Truthful digest (lanes/titles/deep-link; published-vs-held honesty) | P1 | BUILD-2 + SPEC-3 | **BUILT 2026-07-19** (`wave3-client-funnel`, rides npm 0.9.3; P1-13b row) |
| SPEC3-A3 | **Client review surfaces still render the pre-B1 model (drift vs live server)**: CLI `review` (`bin/auxilo-cli.js:527`) computes its own `clean` from `screens_passed` and prints "CLEAN (passed every screen)" + quality bands; dashboard "Select clean" (`public/dashboard.html`) filters `screens_passed` only. Neither consumes the live B1 `lane`/`why`/`approvable_count` contract, so the two surfaces disagree with each other AND with the server (observed live 2026-07-25: CLI "clean" = 20 items at q≥14, dashboard "clean" = 26, server `ready_to_publish` = 12 of 14). Includes the silent-skip UX defect: `--approve-clean` skipping unscored items is documented intent (`lib/self-review.js:312` — null quality skipped unless threshold explicitly 0) but the CLI never SAYS how many unscored items it excluded, so an operator reads "clean approved" as "everything safe approved". Fix: port both surfaces to the three lanes (`ready_to_publish`/`needs_score`/`needs_your_eyes` + per-row `why`), replace `--approve-clean`'s local selection with the server's lane verdict, and print excluded-count lines ("N unscored not included — review individually") on every bulk path. Also: `review --help` enters the interactive flow instead of printing flags, and the interactive `v` reads raw keypresses (TTY-only; piped stdin advances one item) — fold both into the same CLI pass. | P1 | BUILD-2 + SPEC-3 + SPEC-2 | **OPEN 2026-07-25** (found during live pending-queue review; B1 server contract live-verified same day, see SPEC3-B1 row). Rides next auxilo-mcp npm rev + a `public/` dashboard wave. **SPEC WRITTEN 2026-07-26** (`~/.auxilo/handoffs/BUILD-SPEC-SPEC3-A3-2026-07-26.md` — private; venue Codex, brief ready for Tyler relay; ships as 0.9.7 + one dashboard deploy). HEAD re-verification corrected two stale claims in this row: approve-clean exclusion counts ALREADY print (bin/auxilo-cli.js:649-651), and interactive [v] is line-buffered readline at HEAD (spec orders reproduce-first, fix only if real). Confirmed remaining: pre-B1 rendering on both surfaces, client-side approve-clean selection, subcommand --help unhandled, hook-checker object-shape false-negative (reproduced live 2026-07-26; fix folded in — shared helper, both call sites). Design rulings: `--approve-ready` primary (`--approve-clean` hidden alias per B1 naming rule), selection = server lane verdict verbatim, `--min-quality <14` prints an explicit beyond-verdict warning, counted-confirmation rail untouched. **THE ACTIVE BUILD** (G1 queued behind). **Base rulings 2026-07-26: (1) STOP #2 (base not green) CONFIRMED bigger than reported — see CI-RED-0726; authorized base moved to `096c8da` post-fix. (2) STOP #3 (origin advanced past authorized base mid-ritual): base stays PINNED at `096c8da`, proof form = containment (`git merge-base --is-ancestor 096c8da origin/main`), branch created exactly at the pinned sha; record-class commits landing on main during a build are NORMAL and not a stop condition. The tip-equality requirement was a PM relay defect — AGENTS.md's own `--contains` ritual was already race-tolerant. (3) Build chat DIED before ruling #2 was relayed: the CBX-5 clone retirement removed the chat worktree's cwd — the un-gate protected git contents (zero build commits) but not session continuity; RESTARTED clean from the canonical project with all rulings folded into spec REV 2 (env proof via `git rev-parse --git-common-dir`, pinned-base containment ritual, four-surface 0.9.7 bump, no-push handoff via shared `.git`).** ✅ **BUILT 2026-07-26 on `codex/spec3-a3` (0.9.7):** CLI + dashboard now consume the server's three lanes and per-row `why`; `--approve-ready` is primary with a hidden `--approve-clean` alias; thresholds above 14 narrow the ready lane and thresholds below 14 explicitly warn before including `needs_score`; exclusion arithmetic and counted confirmation are pinned. All subcommand help exits before network access. Reproduced the piped `v`/`s`/`q` loss before fixing the readline queue. Shared hook-status helper recognizes structured and legacy SessionEnd shapes in installer and runner status, which now prints the inspected settings path. Missing-lane servers fail soft with a version-skew notice. Verification: focused client suite 104/104; full suite 1410 discovered / 1405 pass / 5 skip / 0 fail; mobile-nav 28/28; test-count pin, surface drift, and predeploy guards pass; dashboard visually exercised at desktop with ready-only bulk selection. **Not pushed, published, or deployed; PM Gate-A is next.** **GATE-A SHIP + MERGED (`8b6906a`) + DEPLOYED + LIVE-VERIFIED 2026-07-26:** PM re-ran suite (1410 discovered/1405 pass/5 skip/0 fail, pin match) + predeploy + audit-high independently; behavioral fires: hook checker "yes" against real object-shape settings, three-lane render on the live queue (lane names lead, per-row why, vocab/sens codes), `--approve-ready` abort-fire selected exactly the server's 2 ready_to_publish with the exclusion line printed, subcommand help exit-0 with zero network; selection diff inspected (lane-authoritative, version-skew fallback, beyond-verdict tracking). **CORRECTION on record: the piped-[v] defect was REAL** (builder reproduced it pre-fix) — the PM's REV-2 "likely stale" call on that claim was wrong. Deploy: single boot (init=1, no crash-loop), `/stats` 0.9.7 (version-string drift closed), agent.json 0.9.7, dashboard-review.js 200 + lane markup live. Merge conflict on this row resolved as union (PM ruling chain + builder completion). REMAINING: npm publish 0.9.7 = Tyler (tarball prepped, shasum on record in PM report). **PUBLISHED + VENDORED 2026-07-26: auxilo-mcp@0.9.7 (Tyler; registry dist.shasum == reviewed tarball `06f0969`, latest tag confirmed); vendored via installRunner() from the PUBLISHED package (key files byte-identical incl. new lib/hook-status.js); regenerated hook dropped ONLY the documented env line — restored, hook byte-identical to pre-vendor; live `runner --status` prints "Hook installed: yes" + inspected-settings path. SPEC3-A3 CLOSED.** |
| SPEC3-F2 | **Client-side extraction dedup (the zero-cost paraphrase answer)**: Tyler 2026-07-26 REJECTED the priced embeddings option — zero-platform-inference is STANDING POLICY ("no cost solution in order to scale"). The paraphrase flood is generated by the contributor's own extraction inference re-reading transcripts with no memory; fix it at the source: local extracted-lessons index (`~/.auxilo/extracted-index.jsonl`, append at submit + free GET hydration of own account), extraction prompt gains a PREVIOUSLY-CAPTURED-LESSONS section (drop re-statements, bar = behavioral difference not wording), post-extraction lexical filter reusing the F1 detector (one implementation), fail-open everywhere (RUNBOOK 07-26 rule). Server screens stay as backstops — client dedup is optimization, never trusted (channel-trust: brake not gas). Cost rides the contributor's existing extraction call; scales per-client. Acceptance: fixture-replay harness, ≥90% flood-cluster candidates dropped (worst of ≥3 runs — LLM judge is nondeterministic), 0 genuinely-new lessons dropped, prompt-budget report at index size 10/100/1000. | P1 | BUILD-2→Codex + SPEC-2 + GOV-3 | **SPEC WRITTEN 2026-07-26** (`~/.auxilo/handoffs/BUILD-SPEC-SPEC3-F2-2026-07-26.md` — private). QUEUED behind SPEC3-F1 Phase 1 (in flight at Codex); one build at a time. → F1 Phase 1 landed 2026-07-26; F2 IN FLIGHT. **Pre-build STOP (Codex, correct): rev-1 spec contradiction — hydration needed own approved+rejected but no endpoint existed; REV 2 ruling = new metadata-only `GET /account/learnings` (no bodies — GOV-3 exfil surface), not a status param on /pending. Acceptance run 1 FAILED worst-of-3 (100/100/80 — one paraphrase leak in run 3; lexical post-filter 0 as expected); REV 3 issued: candidate-anchored judge pass (top-K retrieval by similarity to candidate + one batched client-side LLM judge call, fail-open) as third layer; gate unchanged.** **REV 3 BUILT (Codex, `d96a33c`+`8b85e44`) + GATE-A PASSED + MERGED (`d72c966`) + SERVER DEPLOYED + LIVE-VERIFIED 2026-07-26:** acceptance 100/100/100 worst-of-3, 0 must-keep dropped, drop-audit complete, token cap holds at index 1000; PM re-ran suite (1396/0/pin) + live-fired the anchored judge BOTH directions (real claude CLI dropped the exact leaked-paraphrase class, kept a genuine new lesson — closing the report's honest caveat); `GET /account/learnings` live: 401 unauth, metadata-only 6-field projection, totals reconcile to ownership exactly (543=78a+465r). **ACTIVATED 2026-07-26: npm auxilo-mcp@0.9.6 PUBLISHED (Tyler; registry shasum == reviewed tarball `fa8c46e4`); runner stack vendored via installRunner() from the published package (18 files byte-identical incl. extraction-index/similarity/near-duplicate.json); regenerated hook diffed (ONLY the env line dropped — restored); hook syntax OK, 0755, sentinel present, F2 modules load from vendored layout, SessionEnd registration confirmed in settings.json. Two follow-ups: (1) DEFECT `runner --status` 'Hook installed: no' false-negative — checker string-matches SessionEnd entries but settings use the object shape; cosmetic, fix next client rev. (2) DECISION (Tyler): 600-file pending-learnings backlog, all queued in one batch 2026-07-19 10:03 (historical transcript backfill) — draining it = ~600 extraction LLM calls on Tyler's subscription; the July flood already came from this era, so marginal value is low. DECIDED (Tyler 2026-07-26): KEEP as G1 raw material — and PM verification found the queue is architecturally frozen anyway (flush path POSTs to the deprecated server /extract which 410s since client-side migration; a drain requires a local-F2-pipeline pass, a G1-era build). Drain AFTER G1 lands: paced, F2-deduped, keeps land private directly. G1 spec open question flagged: should the private tier accept non-technical learnings (backlog includes personal/non-code sessions; CI-5 currently drops them) — Tyler scope call.** |
| SPEC3-F1 | **Cross-session near-duplicate detection**: extraction hook re-extracts the same lessons daily — queue flood 14→73 with ~30 near-identical items across 4 categories and `by_screen.near_duplicate`=0; separately a re-extraction of a PUBLISHED learning reached ready_to_publish. Root-cause hypotheses (verify, don't assume): current screen skips rejected-status corpus (queue refills after cleanup), compares same-category only, and 3-token shingles miss paraphrase-class dupes. Phase 0 offline dry-run gates everything: 5 candidate zero-inference detectors (incl. pending-set clustering), ground-truth fixture clusters from the 2026-07-26 flood + published-dupe pair, must-NOT-catch negatives incl. same-domain-different-lesson pairs; gate = ≥90% cluster recall, ≤2% false-pair rate. Phase 1: submission-time scope fix (pending+approved+REJECTED, cross-category; dup-of-rejected holds with why, never auto-rejects) + review-time cluster collapse + RIDER: fix E1 residual (reject-by-signal selector never gets accountVocab derivation) with behavioral test. Never-block, zero inference, Gate-A must exercise every extended surface behaviorally (E1 lesson). | P1 | BUILD-1→Codex + GOV-3 + CAT-1 + SPEC-2 | **Phase 0 run 1 (Codex, `0c1db90`): FAILED THE GATE as designed** — composite recall 0.58% vs ≥90%, FP 0.00%; H1/H2/H3 all confirmed (H3 dominant: max raw score 0.106 vs 0.60 threshold). PM verified independently (focused 14/14 + count-guard 1355/0, which hung in the builder's env). PM probes killed two rev-2 candidates on the same fixtures (rare-term Jaccard 57% max; anchor-term components blob 49-68/80 clean contamination). **REV 2 issued: paraphrase class is SEMANTIC — zero-inference cannot reach it; the ≥90% pairwise gate was a spec defect (PM's).** Restructured: Phase 1 GO on the zero-inference subset (H1+H2 scope fixes incl. dup-of-REJECTED holds + D3-composite for the near-verbatim class = the class that reaches ready_to_publish + reject-by-signal rider); paraphrase dedup ESCALATED to Tyler as priced embeddings decision (~$0.00002/item, $0.05 backfill, pennies/mo; first platform inference since the zero-inference principle — his call, no urgency while account_vocab quarantines the flood). **UPDATE: embeddings REJECTED by Tyler 2026-07-26 — zero-inference is STANDING POLICY; paraphrase answer moved to SPEC3-F2 (client-side). Phase 1 BUILT (Codex `6cbccbb`) + GATE-A PASSED + MERGED (`94bb930`) + DEPLOYED + LIVE-VERIFIED 2026-07-26: PM re-ran full suite (1365/0/pin-match) + behavioral suites (server-boot /learn holds, rider route 200 processed 2/rejected 2); clean single-boot deploy (config/ tree, fail-open honored); live-fire proof: near-verbatim POST /learn → pending_review + near_duplicate + needs_your_eyes + why naming the published predecessor id; test artifact rejected. Perf: submission scan 7.18→69.76ms at 1×1039 — acceptable, no index. Dup-of-REJECTED + cross-category + D3 composite (published pair 0.8807) all live.** **RESIDUAL DEFECT found 2026-07-26 (PM G1 pre-probe, GOV-3 class, low exposure today): the comparison set is ACCOUNT-BLIND for non-public predecessors — `findNearDuplicate` (lib/similarity.js:216) receives the full corpus at every call site, and `nearDuplicateHold` (server.js:5523) exposes predecessor_id/status/category cross-account while both `why` phrasings assert same-account facts ("your published learning" / "you previously rejected") the code never checks. A cross-account near-match of another account's REJECTED item = false statement to the submitter + existence leak of a never-published item (id opaque, GET /knowledge/:id 404s non-approved per S21-2 — so low severity at current single-real-account scale, but it becomes a private-content leak under SPEC3-G1). Fix ruled into G1 Phase 1 (comparison-set scoping via opts.contributorAccountId, one implementation all call sites, pinned cross-account + same-account tests — G1 spec §2.6a); ships STANDALONE next server deploy if G1 slips.** |
| SPEC3-E1 | **Corpus-relative vocabulary signal (`account_vocab`)**: per-item screens cannot see account-private system vocabulary (observed live 2026-07-25: 9 pending items in one account's internal system nouns passed every screen; 6 landed lane ready_to_publish — the auto-publish lane). New zero-inference signal computed at review-surface time: unknown-proper-noun tokens (fail `isKnownWord()`) recurring in ≥2 of the SAME account's learnings re-lane the item to `needs_your_eyes` with `why` + evidence row + sanitize hint (contributor's client does the rewrite — no platform inference cost, per operator constraint). Cross-account contrast designed but dark until ≥10 accounts. NEVER a hard block; submission screening chain untouched (preserves the 2026-07-02 recalibration safety model). Phase 0 offline dry-run is the acceptance gate: recall ≥8/9 on pinned fixture items, hold-rate ≤10% on historically-clean corpus (the 2026-07-02 failure was 64%). **Extends the A1 activation chain: B4 → B1 server → 0.9.3 → E1 → env flip — do not arm `AUXILO_SCORE_EXTRACTION` before E1 Phases 0–1 land.** | P1 | BUILD-1→Antigravity + GOV-3 + CAT-1 + SPEC-2 | **SPEC WRITTEN 2026-07-25** (`~/.auxilo/handoffs/BUILD-SPEC-SPEC3-E1-2026-07-25.md` — private; pins fixtures, test cases, doc impact, gates). Venue: Antigravity (Tyler 2026-07-25). **Phase 0 run 1 (Antigravity, branch `codex/spec3-e1-phase0` @ `9c5816b`): FAILED THE GATE as designed** — recall 2/9, hold-rate 25% (threshold-3 retry no better); root cause was a SPEC defect: candidate rule reused the title-case person-name tokenizer, structurally blind to kebab/camel/all-caps/filename identifiers. **Spec REV 2 issued 2026-07-25** (six identifier shape classes + zero-inference exclusion baseline: clean-corpus document-frequency + static common-dev wordlist; acceptance gate unchanged, per-shape-class reporting added). **Run 2 (Codex, `a8eb583`): mechanical PASS (recall 8/9, hold 3.75%) — but PM verification found one fixture carried only by the public acronym `PR` (its real internal noun occurs once = pre-stated single-occurrence tail) and one fixture body verified vocabulary-generic (spec-authoring error). REV 2.1: `PR` allowlisted, ground truth corrected to 7 must-flag + 1 documented known-miss + 1 reclassified; run 3 confirmation gate = 7/7 recall, ≤10% hold. Corrections make the gate harder, not easier — no luck credit.** **Run 3 (Codex, `06b85e1`): PASS — recall 7/7, hold 3.75%; known-miss + reclassification behaved as ruled. PHASE 0 CLOSED, PHASE 1 GO issued 2026-07-25** with amendments: S6 ships dark (0 recall both runs, noise-only; title-case already covered by `unknown_proper_noun` at screening), shipped signal = S1–S5; S1+baseline alone = 7/7 @ 1.25% noise (minimum viable fallback); single-occurrence tail documented as design limit; wordlist = the tuning valve. Phase 1 = `lib/account-vocab.js` + lane wiring per spec §3, branch `codex/spec3-e1-phase1`. **Phase 1 BUILT (Codex, `2972e7e`) + GATE-A PASSED 2026-07-26 (PM review: suite re-run independently 1339/0-fail/pin-match; never-block verified structurally AND behaviorally — detector opts-gated, summary-route-only; buyer-strip extended; no earnings/settlement/pricing/OFAC diffs; fixture corrections verified legitimate). Analyzer→runtime drift (holds 3.75%→0.00%) explained: S6-dark + same-account-approved exclusion; recall pinned by ratified-fixture test. **MERGED (`a4761c4`) + DEPLOYED 2026-07-26 with one incident: v56 crash-looped ~6min (wordlist read from `data/` = prod volume mountpoint, shadowed the image copy — RUNBOOK §12 entry; the Gate-A miss was approving boot tests that hand-placed the file, masking exactly this); rolled back to v55, fixed forward `ac470c4` (wordlist → `config/`, fail-open loader, true empty-volume boot tests, guard-fires test, pin 1339→1341), redeployed clean. LIVE-VERIFIED: queue had grown 14→73 (extraction flood, ~30 AgentWork/WPR near-dupes); `account_vocab` flagged 68/73 → `needs_your_eyes`, leaving 4 genuinely-generic in `ready_to_publish`, `by_signal.account_vocab=68`. Signal working as designed on live data. E1 link of the activation chain COMPLETE — `AUXILO_SCORE_EXTRACTION` flip now armable (Tyler+counsel per D1/C1).** **RESIDUAL DEFECT found 2026-07-26 during queue cleanup (fail-closed, no data harm): `reject-by-signal` accepts `signal:'account_vocab'` (validation list extended in Phase 1) but `selectPendingIdsBySignal` reads only PERSISTED signals and never receives the review-time vocab derivation → summary counts 68, selector counts 0, 409 SIGNAL_COUNT_MISMATCH. Counted gate caught it exactly as designed; cleanup done via per-id rejects (68/68). Fix: inject the accountVocab opts into the reject-by-signal selector path (same derivation as summary) + a behavioral test that flags an item via vocab then bulk-rejects it — the ci5 pin only checked the validation list, which is how Gate-A missed it. Queue after cleanup: 73→2 (3 approved incl. one that surfaced the near-dup gap live: a dupe of already-published lrn_e3f59c88 sat in ready_to_publish — dupes of APPROVED items carry no vocab flag; reinforces near-dup build as next).** |
| SPEC3-B1 | **Taxonomy + compact surfaces (server)**: summary gains per-row `lane` (ready_to_publish / needs_score / needs_your_eyes) + top-level `approvable_count`/`needs_score_count` + `counts.by_lane`/`by_signal` + filters (`lane`/`flag`/`signal`/`category`/`ids`) + pagination (`limit`/`offset`, absent = pre-B1 shape) + bounded `full=1` bodies (≤25 rows or explicit ids — kills the 174KB payload class); `/learn` accepts + persists `submission_channel` and **holds clean+scored extraction-channel items** (`standing_consent_off`, lane ready_to_publish) unless the account's clean lane is active; assessor provenance (`quality_self_assessment.assessor` server-derived: operator-agent / extractor-local/<client> / server-import). Channel-trust note: marker is a brake, never a gas pedal (spec §3.2). | P1 | BUILD-1→BUILD-2 + SPEC-2 + GOV-3 | **BUILT 2026-07-19** (`spec3-b1-server` from b8d34aa; spec `~/.auxilo/handoffs/BUILD-SPEC-SPEC3-B1-2026-07-19.md`). New `lib/clean-lane.js`; `test/spec3-b1-server.test.js` (51); openapi: summary contract + submission_channel documented. Full suite 1048/1044/0 fail/4 skip (baseline 997/993/0/4 — nothing regressed; sanctioned pin updates: review-seamless summary-callsite window, p2-1a-audit-chain retraction window). MCP compact list rides npm 0.9.4. ✅ **GATE-A PASSED + MERGED + DEPLOYED** — Gate-A verdict SHIP-WITH-FIXES with F1–F3 applied on `fcf606b` (`99005a4`: F1 behavioral guardrail coverage, F2 un-fingerprintable dark 404, F3 no affirmation echo); merged to main at `dfe8f11` (D1 direction ratified, activation counsel-gated). **LIVE-VERIFIED on prod 0.9.4, 2026-07-25:** `GET /account/pending/summary` returns the B1 contract — per-row `lane` (all three observed: ready_to_publish / needs_score / needs_your_eyes), `why`, `sensitivity_signals`, top-level `approvable_count`/`needs_score_count`, `counts.by_lane`/`by_category`/`by_screen`/`by_quality_band`/`by_signal`, plus `returned_count`/`truncated`/`near_dup_clusters`. (Filters, `limit`/`offset` pagination and bounded `full=1` were NOT re-exercised in that check — only the summary shape above was observed live.) Prior status line read "Awaiting Gate-A + merge; NOT pushed, NOT deployed" and was stale from 2026-07-19 until corrected 2026-07-25. |
| SPEC3-B2 | Evidence + flag-class actions: `sensitivity_evidence` capture (+persist LLM reason), injection span, projection-stripping tests, `ack_flags` server invariant, dashboard chips/evidence/ack UI, score-backfill route + CLI (D2) | P1 | BUILD-2 + GOV-3 + SPEC-3 | ✅ **BUILT (server core) 2026-07-19 (Wave-5B, `wave5b-review-closures`; spec `~/.auxilo/handoffs/BUILD-SPEC-WAVE5B-2026-07-19.md` §1)** — `classifySensitivity` returns `evidence` rows (`{signal, excerpt≤120, hint}`, span≤60, neutral §10.3 templates; person_name bigram captured); `combineSensitivity` PERSISTS the LLM layer's reason as an `llm_semantic` evidence row (appendix defect 3 closed; fail-closed verdicts carry their why too); `/learn` + `/extract` persist `sensitivity_evidence`; injection span already persisted (`injection_flags[].excerpt` — no change needed). Projection discipline (GOV-3): evidence + lineage on contributor projections only; triage rows gain one-sentence `why` (evidence hint, per-flag fallbacks for pre-B2 records); admin queue includes evidence; all 4 buyer projections strip `sensitivity_evidence` + `sanitized_from/to` (count-pinned, ci5 strip-pin extended). Bulk-by-class: `POST /account/pending/reject-by-signal` (counted gate `expected_count` == live selection else 409 SIGNAL_COUNT_MISMATCH nothing-mutated; REJECT-ONLY per §5.2 asymmetric rule; shared `selectPendingIdsBySignal` selector = summary filter semantics) + MCP `auxilo_review {action:'reject_by_signal'}` (0.9.4 rider). Lane derivation unchanged (test-pinned). **Still open from this row:** `ack_flags` server invariant (bulk-approve-flagged hardening), dashboard chips/evidence/ack UI (public/ rides its own wave), score-backfill route + CLI (= SPEC3-D2, still OPEN). |
| SPEC3-B3 | Sanitize-and-resubmit: full-pipeline resubmission route with lineage (`sanitized_from/to`), dedup-predecessor exclusion, depth guard; CLI/MCP propose flows; dashboard diff UI | P1 | BUILD-2 + GOV-3 + SPEC-3 | ✅ **BUILT (server + MCP) 2026-07-19 (Wave-5B; spec §2)** — `POST /account/pending/:id/sanitize` (contribute scope, ownership-before-status M-6, source pending_review OR rejected): ATOMIC one-lock/one-write retire+resubmit (no lost-item window — the reason it is a route, not reject+POST /learn); FULL screen pipeline zero-bypass (body sanitizer → exact+near dup with the retiring predecessor EXCLUDED → credentials filter → injection → two-layer sensitivity w/ evidence → system-fact screen → quality floor, score carried over); replacement ALWAYS held (`review_reason` +`sanitized_resubmission`, never seamless/clean-lane, any channel — clean+scored lands lane ready_to_publish, one counted approve away); lineage BOTH directions (`sanitized_from`/`sanitized_to`, reviewer-projected, buyer-stripped); guards 409 ALREADY_SANITIZED + SANITIZE_DEPTH_EXCEEDED (2 hops = 3 strikes) + retired-category refusal; original rejected reason `sanitize-resubmit`. MCP `auxilo_review {action:'sanitize'}` (0.9.4 rider — direct corrected-content submission; the client-side propose/diff flow is the residual). openapi documents both new routes + enum. **Still open from this row:** CLI `--sanitize` propose flow (bin/), dashboard diff UI (public/). **Gate-A on 541bdab: SHIP-WITH-FIXES — applied same wave:** F1 409 `ADMIN_REJECTED_FINAL` (admin `moderation_action.action==='rejected'` is contributor-irreversible — without it the predecessor exclusion let a near-verbatim resubmit launder admin-rejected content in two calls; guarded in phase 1 AND re-checked under the lock; a later admin approve overwrites the stamp and lifts the guard); F2 screens moved OUTSIDE the learnings lock (the 6s LLM timeout was queuing every catalog writer — /learn posture restored: async screens on a snapshot, then a fully-SYNCHRONOUS locked phase re-validating the mutable guards [status/lineage/admin → 409 `SANITIZE_CONFLICT`, cheap sync exact-dup re-check] + mutation-pair + one write; no-await-under-lock walker test); F4 the ALWAYS-held pin now targets the PERSISTED replacement literal (`sanitized_from`/`status` adjacency) — the old includes() matched the response literal and survived flipping the stored status. All three mutation-verified (each kills fail≥1/exit 1). |
| SPEC3-B4 | Retraction fixes: null-consent-state 500; window basis = approval time | P1 | BUILD-2 + GOV-3 | ✅ **DONE 2026-07-19** (Wave 2b, §26 record) |
| SPEC3-C1 | **Clean-lane standing consent (per D1 Option B)**: versioned consent artifact `data/clean-lane-consent.jsonl` (own file — extraction-consent.jsonl is state-keyed; shared hash chain per P0-B) with grant/revoke/status routes; grant = strengthened L-2 clickwrap (exact dated `consent_version` 2026-07-19-clean-lane-a1 + `agree:true` + VERBATIM affirmation sentence on the wire + `tos_version_at_grant` recorded + account lock; contribute scope minimum); **never agent-enrollable** (no MCP tool, test-pinned); when armed + granted: clean+scored+extraction items auto-publish stamped `published_via`/`standing_consent_version`/`retractable_until` (+7d) with a per-publish notice line in the response, threshold default 16 (per-grant tunable 14–20), and the **retraction-rate auto-freeze guardrail** (>5% over 30d ⇒ freeze row + ops alert, category clean-lane; checked before every auto-publish AND after every clean-lane retraction; reactivation = explicit human re-grant only). | P1 | BUILD-2 + GOV-2 + GOV-3 + Tyler | **BUILT-DARK 2026-07-19** (`spec3-b1-server`) — flag `EXTRACTION_AUTOPUBLISH_CONSENT_ENABLED` default OFF/absent OFF; while dark all three routes 404 **before auth** and every extraction-channel item holds (zero behavior change, test-pinned). Still owed at activation: notice rollup email + failure-freeze (`notice_undeliverable`), dashboard/CLI enrollment UI, ToS §5.9.3 sentence + privacy-policy notice purpose (GOV-2), openapi publication of the routes, consent-copy wordsmith (spec §10.1 is the recorded affirmation text; rewording bumps the consent version). Activation gate: Tyler + GOV-2 counsel pass + GOV-3 controls sign-off (spec §4.5). **GOV-3 determination (Gate-A, 2026-07-19):** the verbatim-affirmation layer is EVIDENTIARY not preventive — it prevents accidental enrollment and creates a hash-chained record of a deliberate act by the credential holder; it does not stop an agent holding the operator's key, whose enrollment risk is bounded (the same key already publishes via seamless; freeze + notice cap blast radius). Activation review must confirm dashboard/CLI remain the only advertised enrollment paths and docs describe the affirmation layer as evidentiary. |
| SPEC3-D2 | Operator-triggered score-backfill of existing unscored pending items (`--score-backfill` + `POST /account/pending/:id/score`) | P2 | Tyler (approve) + BUILD-2 | OPEN — bundled into B2 per spec §9 D2 (no consent implication: backfill only moves items into a lane where the human still acts) |

| GOV2-DEL | **Automated account-data deletion workflow** (pre-existing gap surfaced by G1 pin-17 verification; elevated by the private tier storing personal-class content): verified-request intake → both-axes enumeration (account_id + verified linked wallet) → learnings removal incl. private + account/key removal with policy §4 retention exceptions → live verification; SLA 30 days per served policy. Until built, RUNBOOK §13 manual path (retract-learnings.js) is the compliance mechanism; the pin-17 canary in test/spec3-g1-private-visibility.test.js breaks when a route lands (revisit the pin then). Scope includes the counsel nod on the private-tier lesser-included-use ToS reading. | P1 | GOV-2 + BUILD-2 | **OPEN 2026-07-27** (G1 Gate-A finding). **SPEC WRITTEN 2026-07-28** (`~/.auxilo/handoffs/BUILD-SPEC-GOV2-DEL-2026-07-28.md` — private; venue Codex, pinned base `14dd835`, branch codex/gov2-del, two-phase with Phase-0 store-audit STOP gate per the G1 pattern; server-side, NO version bump; pin-17 replacement in scope §3.6). GOV-2 rider draft (3 Privacy Policy insertions) annexed to the spec — **rider APPROVED by Tyler 2026-07-28** (builder applies annex verbatim; counsel-driven amendment, if any, returns as a spec rev via Tyler). Counsel brief addendum authored same day (`AUXILO-COUNSEL-BRIEF-GOV2-DEL-ADDENDUM-2026-07-28.md`: Q1 lesser-included-use §5.2 duration-clause wrinkle; Q2 deletion-workflow conformity incl. grace-window question) — Tyler relays to counsel; nod gates nothing in the build, informs the rider's final form. **Base re-pinned `185a589` at relay (record-only diff from drafting base; CI green verified on the exact sha); handed for relay 2026-07-28. **STATUS CORRECTED 2026-08-27 — NOT BUILT, NOT STARTED. The relay was never picked up: no `codex/gov2-del` branch exists, no deletion routes exist in server.js, and the pin-17 absence-canary still passes. This row read "THE ACTIVE BUILD" for 30 days while nothing was in flight, and two other rows (FLAKE-0728 remediation, EXT-0806(b)) were queued behind a phantom — a record-integrity defect, surfaced by the GOV-2 red-team review of the 08-27 sequencing call. Base `185a589` is now 9 commits stale and MUST be re-pinned at the next relay. SEQUENCING RULED 2026-08-27: R13-CLOSE goes first (GOV-2 objection conditionally withdrawn — the served §8.3 promise is currently KEPT via the RUNBOOK §13 manual path within its own 30-day SLA, while ToS §9.2's automated-filter claim is the weaker representation and R13-CLOSE carries the heavier legal dimension); **GOV2-DEL is the NEXT build after it, merging before whichever comes first: the campaign's public-launch date, or the 10th non-operator account** (GOV-2 condition C7). Open design question to chase during the R13-CLOSE cycle: counsel addendum §C Question C(c) (hard-deletion takeover-protection grace window) has no recorded answer — GOV2-DEL should not be built against it. Phase-0 store audit must also enumerate R13-CLOSE's new report-threshold counter as a store class, or a deleted account leaves orphaned report state.** |
| SPEC3-G1 | **Private learnings tier** (Tyler idea, ratified direction; natural F2 successor): third disposition `keep_private` — `visibility:'private'` on the existing catalog (status enum untouched; rejected stays recoverable by flip); owner-only search inclusion + $0 owner recall (extends shipped DR-8; non-owner GET → 404 existence-hiding); exclusion via the single `visibleCatalog()` predicate (server.js:3211) with a Phase-0 count-pinned raw-iteration audit; promote-to-public ONLY via sanitize (B3) → full screening chain incl. CI-5; private lane accepts `category:'non-technical'` (private-only — public submission 400s naming CI-5); GOV-3 safety screen + local scrub run on ALL visibilities; no price/earnings/ranking; auto-publish (D1/C1) can never touch private items (pinned test). Unlocks the 600-file pending-learnings drain (Phase 3 runbook: local F2 pipeline, paced, ~600 extraction calls on Tyler's own subscription, lands private directly). | P1 | BUILD-1→Codex + GOV-3 + GOV-2 + CAT-1 + SPEC-2/SPEC-3 | **SPEC WRITTEN 2026-07-26** (`~/.auxilo/handoffs/BUILD-SPEC-SPEC3-G1-2026-07-26.md` — private). **Tyler rulings 2026-07-26: GO (spec authored parallel to the A3 build) + SCOPE = non-technical ACCEPTED, private tier only** — CI-5 re-scoped as a marketplace-quality rule enforced at the promote boundary (§30 partially superseded, recorded there; public-catalog hard scope UNCHANGED). Build queued behind SPEC3-A3 (one in flight). **STAMPED + HANDED FOR RELAY 2026-07-26 (post-A3-close): pinned base `270fd9f`, containment ritual, scope = Phases 0-1 only (server-side, NO version bump), Phase 0 = hard STOP gate for PM ruling, no-push handoff on `codex/spec3-g1`.** **PHASE 0 PASSED (PM-verified) 2026-07-27, commit `d4d3ae6` (one audit file on the pinned base):** 32 raw-read paths / 47 call sites — verified SUPERSET of the PM's independent 21-site baseline (1 PM false-positive resolved: server.js:11422 is `pipeline.learnings`, not the store); findings F1-F7 include three real classes beyond spec §2.6a (private pricing influence via `isMarketVisible`, tier activation via unscoped `learnings.some`, rate/report existence oracles) — all spot-checked in source by PM. **PM RULINGS ISSUED: F5 AUTHORIZED** (sanitize accepts owner's approved-private source + replacement category restricted to the technical taxonomy, private-source-only; result public-destined pending_review via full chain; original stays private/recoverable), **F6 YES** (exact-dup gates get §2.6a scoping too, all three implementations: public-for-all, non-public same-account-only), **F7 YES** (omission shape: private results/recall omit unlock_price_usd/current_price/value_signal/_revenue price entirely; DR-8 truth-fields retained; count-pinned both directions). Row-5 question ruled: private-destined pending rows DO count in operator ops-alert totals (internal metric). F1 two-layer fix, F2-F4, KEEP-RAW allowlist + G1_RAW_READ_ALLOW markers, and the 17-pin set APPROVED as Phase 1 acceptance frame. **PHASE 1 GO.** **PHASE 1 BUILT (Codex `1845366`) + GATE-A SHIP + MERGED (`cbe8dcc`) 2026-07-27:** PM re-ran the full battery (suite at pin 1427/0 fail, predeploy, audit-high) + inspected every ruling-critical hunk (unconditional visibleCatalog exclusion; keep_private with ownership-before-status; sanitize replacement-category restricted to the technical taxonomy + G1_RAW_READ_ALLOW markers; similarity comparison-set doctrine — public predecessors now also require approved status, closing cross-account rejected comparison by construction; F7 owner-proof refactor runs before price resolution). dr8/ci5 structural reconciliations verified non-weakening (sanctioned window/anchor class); the first-regression 8 failures were stale structural expectations, honestly reconciled. **Delivery ❌ on pin 17 RESOLVED BY PM RULING:** the served Privacy Policy promises REQUEST-driven deletion (30-day window), not an automated route — the manual path is real (retract-learnings.js any-status removal + new RUNBOOK §13 verified-request procedure); the pin-17 canary (asserts route absence; breaks loudly when one lands) is KEPT; automated workflow → new row GOV2-DEL. **GOV-2 rider APPROVED by Tyler 2026-07-27** (three additive Privacy Policy insertions: §1.2 collection ¶, §3.1 never-shared ¶, §4 retention row) — ships in this push. ToS: no amendment (owner-only storage is a lesser-included use of the existing grant); counsel nod queued on GOV2-DEL. **DEPLOYED + LIVE-VERIFIED 2026-07-27:** single boot (init=1); /stats 0.9.7, catalog 123 UNCHANGED (stats exclusion holds); /privacy serves all three rider insertions; missing-id 404 shape confirmed as the private-non-owner baseline; /account/learnings projection carries `visibility` (7 metadata fields, no body). **SPEC3-G1 Phases 0-1 CLOSED.** Phase 3 (backlog drain runbook) queued. **PHASE 2 SPEC WRITTEN + STAMPED 2026-07-27** (`~/.auxilo/handoffs/BUILD-SPEC-SPEC3-G1-P2-2026-07-27.md` — private; pinned base `9b0013a`, containment ritual, branch codex/spec3-g1p2, no-push handoff; scope: CLI rapid `p` + bulk `--keep-private` (safe-direction `--yes` parity with all-reject), dashboard keep-private actions, MCP `keep_private` with shared-selection dry-run parity, runner opt-in `AUXILO_CAPTURE_VISIBILITY=private` (default unchanged, scrub mandatory, CI-5 relax private-only), 0.9.8 four-surface bump, §3.5 verify-first summary-projection rider). **REV 1.1 issued minutes after relay (PM spec defect, caught by PM pre-probe): rev-1 claimed approve-on-private-destined "finalizes as private" — server truth is 409 `private_requires_sanitize` (self-review.js:168); amended to up-front exclusion from approve selections + 409-surface handling; §3.5 rider CONFIRMED required (projectTriageRow lacks visibility).** **Builder STOP #2 (docs/* conflict) RULED 2026-07-27: `docs/AGENT-LEARNING-GUIDE.md` authorized (source of truth per its own banner + F1/F2 precedent; UC6-R1 ruling class). STOP unearthed a real doc-governance defect: builds split-brained the two tracked guide copies — A3 + CI-5 edited the SUPERSEDED root copy while F1/F2 edited docs/ (600 diff lines drift); reconciliation folded into the P2 build (port A3's 0dc1f8e hunk into docs/ first; root copy frozen behind its banner). AGENTS.md:50 corrected on main same day — "docs/* is deliberately untracked" was factually wrong (docs/ is a mix; git ls-files is the authority) and had generated three builder STOPs of this class.** **P2 BUILT (Codex `882d1f1`, isolated worktree — CBX-4 model working as intended) + GATE-A SHIP + MERGED (`2ad5197`) + DEPLOYED + LIVE-VERIFIED 2026-07-27/28:** PM battery x2 clean (pin 1445/0 fail; run-1 exit-1 = transient loopback class, builder-disclosed, unreproduced); live fires: lane list, `--keep-private` counted abort (exact 8-item lane + exclusion line), and **the catalog's FIRST real private learning: `lrn_a03d1248` kept private live (Tyler-approved)** with all five guarantees verified on prod (owner-listed private, anon-search miss on its own vocabulary, non-owner 404, owner free recall with zero price fields, catalog stats unchanged at 123). Docs split-brain reconciled into docs/ source of truth (root guide frozen behind banner). Deploy healthy (v63, 1/1 checks); /stats + agent.json 0.9.8; dashboard keep-private UI served == source. **PUBLISHED + VENDORED 2026-07-28: auxilo-mcp@0.9.8 (Tyler; registry dist.shasum == reviewed tarball `730aaea`, latest confirmed; publish attempt #1 failed E404 = masked E401, dead npm token — Tyler re-authed via npm login, attempt #2 clean, same byte-identical tarball). Vendored via installRunner() from the PUBLISHED package; hook byte-identical after documented env-line restore; runner-stack files byte-match published (review.js correctly absent — CLI/MCP lib, not runner stack); private-lane plumbing vendored (runner resolveCaptureVisibility → captureVisibility opt → extractor). SPEC3-G1 COMPLETE (Phases 0-2 shipped; Phase 3 drain runbook = the remaining deliverable, now fully unlocked).** **THE ACTIVE BUILD — sequencing ruled by Tyler 2026-07-27: Phase 2 before UC-6** (disposition surface is upstream of UC-6 capture volume; review throughput gates supply, not capture). UC-6 remains queued behind. Gates: GOV-3 count-pinned exclusion set; GOV-2 disclosure + deletion rider (pre-deploy); CAT-1 stats exclusion live-verified. |
| SPEC3-E1b | **`account_vocab` generic-term false positives — the signal was suppressing its own queue.** Live 2026-08-06: pending 30, `by_signal.account_vocab`=21 (70% hold) against E1's own Phase-0 design gate of <=10%; `ready_to_publish` starved to 5 while approvable sat flat and the catalog held at 123 for 6 days. Every sampled flagged term was generic: ordinary English compounds (`already-vetted`, `race-condition`, `auto-generated`, `7-day`, `home-dir`, `image-processing`), ALL-CAPS emphasis (`VALUE`, `LOUDLY`), and public product names (`ChatGPT`, `LinkedIn`, `mcp-server`, `codex-desktop`). Root cause is STRUCTURAL, not a stale list: S1/S2 match any `-`/`_` token so ordinary compounds are indistinguishable from identifiers, and all three precision suppressors are inert in this corpus — cross-account contrast needs >=10 accounts (3 exist), approved-DF needs the term in >=3 approved learnings, and the wordlist holds only single words while compounds grow combinatorially. Fix makes the wordlist multiplicative: `isCompoundOfKnownTerms()` excludes a candidate when EVERY segment is known-or-numeric (one unknown segment keeps the flag — preserves `vandelay-brain`, `initech-pilot`, `acme_widget`; synthetic fixtures per GOV-3 identity ruling), new exclusion reason `static_common_dev_compound` keeps the two mechanisms separately auditable, wordlist +65 terms scoped to what the live queue actually flagged plus public product nouns. **Process note worth keeping:** a first pass added 186 speculative words including `private` and BROKE the ratified E1 fixture `private_queue` — internal identifiers are often two ordinary words, so generic identifier modifiers (private/public/shared/core/lane/tier/state/mode) silently disarm the signal. Trimmed to evidence-only; the wordlist header now carries that as a DO-NOT-ADD rule. Known accepted trade, documented at the call site: an all-ordinary-words compound whose MEANING is account-internal (`ready-to-publish`) now passes — the signal targets unknown proper nouns, never hard-blocks, and per-item screens still run. | P1 | BUILD-1 + CAT-1 + SPEC-3 | **BUILT 2026-08-06; GATE A COMPLETE 2026-08-27 — four reviews (BUILD-1, BUILD-4, GOV-3, GOV-1) all PASS-with-notes, zero blockers; reviewer-mandated remediation applied pre-merge and commits rebuilt (branch unpushed — history rewrite free): `shared`/`core`/`queue` removed from the base wordlist + `shared_queue` must-flag regression pin (convergent MED: the compound rule weaponized pre-existing DO-NOT-ADD-class entries), synthetic fixture swap (GOV-3 identity ruling), trade-example comment corrected to `auto-publish` (BUILD-1 — `ready-to-publish` still flags and was a false example), AGENT-LEARNING-GUIDE compound clause (GOV-1 merge-gating; CLOSES "docs/ E1 surface not yet reviewed" — all other E1 doc surfaces affirmed clean). Rebuilt code commit `57a4c08` (base a377ddb).** Verified post-remediation: 13/13 live false positives excluded, 0/9 real identifiers lost (7 pinned in the new file incl. `shared_queue`, plus `private_queue`, `ops-consumer` in the ratified E1 suite); full suite 1460 discovered / 0 fail; pin 1455->1460. **MERGED + DEPLOYED 2026-08-27: merge `2e0da6d`, CI green on the exact sha, Fly machine v65, boot clean. Live re-lane VERIFIED same hour (operator queue of 44, before→after): `account_vocab` holds 39→33, `needs_your_eyes` 42→36, `ready_to_publish` 1→4, `needs_score` 1→4; all 13 sampled generic terms cleared, every must-flag class intact. HONEST RESIDUAL: hold-rate still ~75% vs the <=10% design gate — with an item's top term excluded, the signal surfaces its NEXT recurring generic term. Live tail: `re-*` hyphen prefixes (`re-fire`/`re-creates`/`re-query`/`re-check`), compounds with one missing generic segment (`per-project`, `client-rendered`, `api-limitations`, `background-tab`, `deep-link`, `browser-based`, `on-demand`, `b2b-advertising`, `campaign-manager`, `data-leakage`, `already-running`, `url-parsing`, `legal-compliance`), universal acronyms (`CMS`/`SHA`/`GA`/`SOP`/`FDA`/`SKIP`), public product nouns (`ImageMagick`). SPEC3-E1c is therefore REQUIRED, scope live-evidence-defined: ACRONYM_ALLOWLIST extension, evidence-scoped segment adds incl. `re`, whole-token product nouns, SEGMENT_DENYLIST + executable DO-NOT-ADD invariant, all-numeric-compound ruling — while `project-internal`/`always-loaded`-class modifier compounds stay flagged by design.** Prior fast-follow note (BUILD-1/BUILD-4 remainder) is folded into that E1c scope. |
| EXT-0806 | **Extraction pipeline produced ZERO learnings for 3+ days and reported success the whole time.** Discovered by the 2026-08-06 supply cadence. Since `2026-08-03T21:52Z`, 30 consecutive runs log `local model not authenticated in this context (run \`claude\` and /login once); skipping deterministic extraction` and then return a normal result object — the caller prints `[runner] ✓ published=0 held=0 rejected=0 (extraction: client-skip)`. **A checkmark on a total no-op.** Capture/scrub/dedup/upload are all healthy; only the extraction step is skipped, which is why the catalog sat flat at 123 while the sweeper looked green. Two distinct problems. (a) AUTH — reproduced directly 2026-08-06: `claude -p` returns `Not logged in · Please run /login`, exit 0, BOTH inside and outside the tool sandbox, so it is a genuine credential failure and not an environment artifact. A `Claude Code-credentials` keychain item exists for the account but the CLI will not use it (expired/revoked; last real extraction `2026-08-02T14:51Z`). Critically, the Claude Code app session authenticates separately — so daily interactive use gives NO signal that the CLI login is dead, which is exactly why this ran silent for 3 days. Fix is Tyler-only (interactive `claude` + `/login`); an agent cannot authenticate. (b) SILENT-FAILURE DEFECT (ours, fixable) — `scripts/runner.js:392-394` returns a success-shaped `{published:0, held:0, rejected:0, extraction_id:'client-skip'}` on the skip path and the three caller log lines (`:923`, `:949`, `:1088`) stamp it with `✓`. Nothing counts consecutive skips, nothing alerts, and `~/.auxilo/extract.log` greps clean for `Upload failed`/`✗` — the cadence check's own error probe was blind to it. Proposed: drop the `✓` for skipped runs and emit a distinct token, count consecutive client-skips, and alert via `lib/ops-alert.js` past a threshold; mirror the threshold check into the supply-cadence task so the monitor cannot be fooled by a green log again. | P1 | BUILD-1 + BUILD-4 | **(a) REGRESSED 2026-08-28 — REOPENED. The 08-27 resolution held for about nine hours and did not survive the machine migration.** Restored that morning (real extraction `client-ead5d628…`, held=2, queue drained). Last REAL extraction `2026-08-27T19:08Z`. The new MacBook's setup completed `2026-08-27T20:18` and **Claude Code CLI credentials live in the Keychain, which the transition bundle explicitly did NOT carry** (RESTORE.md §1: "Keychain is NOT in this bundle... a clean install does not"). Since the migration: **2 client-skips, 0 real extractions.** Direct probe 2026-08-28: `claude -p` → `Failed to authenticate: OAuth session expired and could not be refreshed`, **exit 0** — the silent-success shape all over again. Surfaced not by the extraction monitor but by `com.slam.ops-consumer` logging `Invalid API key · Please run /login` on an unrelated job. **This class has now run silent TWICE in three weeks (2026-08-03→08-06, and 2026-08-27→now), and both times the `✓` in extract.log actively concealed it.** Tyler action: run `claude` interactively and `/login`, then confirm the next sweep logs a non-`client-skip` extraction id. **SEQUENCING RECOMMENDATION (PM, 2026-08-28): promote (b) to NEXT, ahead of GOV2-DEL.** It is a small logging/counter/alert build, it has now failed twice with multi-day silent windows, and it is the only thing that makes a third occurrence visible without an unrelated job happening to leak the symptom. R13-CLOSE is mid-build and is NOT to be interrupted (one build in flight); this takes the slot after it.** (b) OPEN — BUILD-SPEC authored 2026-08-27 (`~/.auxilo/handoffs/BUILD-SPEC-EXT-0806b-SILENT-SKIP-2026-08-27.md`, base pinned at handoff): distinct `⊘ extraction SKIPPED` token replacing the lying `✓`, persistent consecutive-skip counter, ops-alert at threshold 2 with daily dedup, `--status` extraction line, supply-cadence probe mirror (PM-owned post-merge). Client-side build → five-surface bump + npm publish (Tyler 2FA) + re-vendor. **PUBLISH BLOCKER FOUND 2026-08-28 (machine migration): `npm whoami` returns E401 — the npm token is dead.** `~/.npmrc` is present, mode 600, and byte-identical to the pre-migration drive copy, so it migrated correctly; the token itself is expired/revoked at the registry. Per the documented class, a publish 404 on PUT is a MASKED auth failure — always `npm whoami` first. **Tyler must `npm login` before this build's publish step; the build itself can proceed and land unpublished.** Queue-point corrected 2026-08-27 (was "behind the GOV2-DEL relay" — that build never started): sequenced after R13-CLOSE and GOV2-DEL, and it is the one queued item needing an npm publish (Tyler 2FA). A learning already sitting in the operator's private pending queue independently documents this same benign-looking-skip failure class (GOV-3 practice note: its title is deliberately not quoted here — pending content passes review before it appears in public surfaces, this tracked file included). |
| R13-CLOSE | **Malicious-content defense closeout** — SEC-CLOSEOUT audit 2026-08-27 (run the day after the 2026-08-26 Hugging Face agent-swarm incident reports made "marketplace learnings as an attack channel" the first buyer question): delivery-side envelope + confidentiality screens verified LIVE; three submission/discovery-side mitigations the R-13 register row claims remain unbuilt, and closing them is launch-gating for the governed-channel positioning. Build = three server-side moves + a one-clause ToS rider (GOV-2, Tyler-approved text; GOV2-DEL rider pattern); full scope in the private BUILD-SPEC (`~/.auxilo/handoffs/BUILD-SPEC-R13-CLOSE-2026-08-27.md`, Phase-0 coverage STOP then Phase-1). No npm bump. Second-tier backlog (reputation holds, ops scheduling, FN telemetry) recorded in spec §3.5. | P1 | GOV-3 + BUILD-2 + GOV-2 | **OPEN — SEQUENCED FIRST (2026-08-27 ruling). Spec at rev 2: a red team REFUTED the PM's proposed interim control and, in doing so, the assumption the spec rested on.** The PM had advised that `FORCE_ALL_REVIEW=true` (server.js:348) would close the campaign-window risk as a config flip and decouple GTM timing from this build. FALSE, and verified false in code: holding an item to `pending_review` does not keep it off the public catalog, because the CONTRIBUTOR self-approves it — `POST /account/pending/:id/approve` → `applySelfDecision` (lib/self-review.js:193-194) sets `status='approved'` + `visibility='public'`, checks ownership/status/visibility/category only, and `FORCE_ALL_REVIEW` has ZERO occurrences in lib/self-review.js. Accounts are self-service (magic-link, any address; new-account limit is in-memory, clears on the restart a secret-flip causes), so the adversary chain is ~4 calls. The flag is also NET-NEGATIVE: a forced hold sets none of the fields `screenFlags()` (lib/self-review.js:245) reads, so those items land flagless in `ready_to_publish` — the lane `selectForBulkApprove('clean')` and the `approve_clean` MCP action present as "passed ALL platform screens." **Rev 2 therefore adds §3.0 publication-authority gate as the load-bearing item** (trust-gate self-approve for untrusted accounts on platform-flagged items; persist the platform hold reason so no held item can present as clean; de-fang the self-publish instruction at server.js:6240 for untrusted accounts; bring scripts/reclassify-pending.js:91 behind the same check — it bulk-approves on the sensitivity classifier alone and would undo this build). Without §3.0 the §3.1 semantic verdict is toothless against its own adversary: it changes the lane, the attacker changes it back. Live-probe acceptance tightened — the probe must run the FULL chain from a fresh untrusted account and ATTEMPT self-approve; stopping at the hold does not satisfy the gate. **The self-approve lane is CORRECT for the case it was built for (the builder's own consent gate, protecting a builder from publishing their own secrets); it simply has no adversary model, which was fine at 3 trusted accounts and is not fine under a public campaign.** GOV-2 conditions on this sequencing: C1 (GOV2-DEL record corrected — DONE this commit), C3 (exercise RUNBOOK §13 deletion drill on prod once, before campaign traffic — OPEN, Tyler/PM, ~1h), C4 (privacy-request mail routing so the §8.7 clock is visible — OPEN), C5 (no self-serve-data-control claims in campaign copy — binding on GTM-HF), C6 (fix ToS §9.2's automated-filter claim in this build's rider), C7 (GOV2-DEL merges before the campaign launch date or the 10th non-operator account). Gate A: BUILD-1+BUILD-4+GOV-3+GOV-1; Gate B: GOV-2. **TYLER RULED 2026-08-27: venue = Codex Desktop relay** (strict build/review separation kept; PM offered to build in-session and was declined). **STAMPED + RELAY-READY: base PINNED `36c59c7`, CI green on that exact sha, expected at base 1460 discovered / 1455 pass / 0 fail / pin 1460 / 0 `not ok` / skip 5-6 (FLAKE-0728 class, not a stop).** PM-owned relay-integrity check, added because GOV2-DEL went 30 days dark unnoticed: **the PM verifies a `codex/r13-close` branch actually exists within 72h of handoff and reports if it does not** — a relay is not started until a branch exists. **PHASE 0 DELIVERED + ACCEPTED 2026-08-28** (`codex/r13-close` @ `bcd0435`, unpushed, baseline reproduced exactly as stamped: 1460/1455/0/5, pin 1460, DR-1 28/28). Report `R13-PHASE0-COVERAGE.md`. It found a publish-capable path the spec MISSED and corrected two PM premises. **Five rulings issued, spec now rev 3, Phase 1 GO.** **R1 → R1-B, declining the builder's recommended R1-A:** `POST /pipeline/:id/approve` publishes to the catalog after only a credential/PII regex — no injection screen, no semantic screen, and no `SERVER_SIDE_EXTRACTION_ENABLED` guard (only `requireSession`). Rather than extend the safety classifier there, gate the route behind the same 410 that already guards `/pipeline/upload`. PM verified on prod: upload 410s and `/app/data/pipelines.json` DOES NOT EXIST, so `pipelineEntries` is `[]` and the route can only 404 — R1-B is free, closes it harder, and **spends zero inference, so §3.1's "NO new LLM calls anywhere else" stands unamended.** The standing zero-inference exception is not spent on a dormant feature. Rider: safety-screen coverage is a precondition of ever re-enabling the pipeline. **R2 → accepted:** verified wallet does NOT grant trust (fresh wallets are cheap; wallet-alone converts the 4-call attack into a slightly longer self-service one). **Corrects the spec — §3.0's criterion (b) verified-wallet is WITHDRAWN, a PM error.** Trust = explicit operator grant, or prior public learning with durable admin/platform approval provenance excluding `moderation:'auto'` and `self_review_action` (else trust bootstraps circularly from self-approval). **R3 → B, REVERSING the PM's own recorded recommendation of A.** Every untrusted account's first public learning queues for operator approval. A was recommended on 2026-08-27 assuming wallet could anchor trust; R2 removed that anchor, and under A a brand-new account can iterate against the semantic screen until a false negative publishes with no human ever in the loop — the exact original red-team failure. B costs one operator review per new contributor, once, not per item; trusted accounts keep today's flow byte-for-byte. **Tyler may override to A; residual to record if he does: semantic-screen false negatives remain directly publishable by brand-new accounts.** **R4 → accepted:** `/discover` carries NO learning-authored text (static skills.json) — **the spec named it wrongly, PM error from the audit gap list.** Unlock envelope stays byte-identical; a field-neutral preview advisory covers the real stragglers; and of five successful body-bearing raw-HTTP unlock branches only two carry the advisory today, all five must. **R5 → accepted:** title + full body are already injection-screened before the snippet is derived, so §3.3(b) needs a regression (lure in the first 120 chars is held), not a duplicated regex call. |
| GTM-HF | **Incident-hook GTM package** — "the governed channel for what agents learn," riding the 2026-08-26 METR/OpenAI Hugging Face reports; full draft (narrative memo, X thread, LinkedIn post, long-form with sources, platform-outreach paragraph, proposed docs/GTM.md addendum, risks/don'ts) at `~/.auxilo/handoffs/GTM-DRAFT-HF-INCIDENT-2026-08-27.md`. Honesty rails enforced in the draft: zero incident involvement stated in every long format, no defense-completeness claims (R-13 status disclosed in the long-form itself), product-last ordering. Campaign side-effect pre-flagged: expect adversarial submissions; GOV-3/CAT-1 notified before anything ships. | P1 | GROWTH-1 + BUILD-3 | **OPEN — AWAITING TYLER, and now HARD-GATED ON R13-CLOSE §3.0 (2026-08-27 red-team finding).** The campaign copy claims "human review lanes before anything held publishes" and "a human approves anything held." Both are literally true and materially misleading in the security frame this campaign creates: the human is the CONTRIBUTOR, not the platform, and any visitor can be their own reviewer in ~4 calls (see the R13-CLOSE row). Publishing a governed-channel claim against an open self-approve surface invites exactly the reader who will test it, and the resulting writeup would be the campaign. **The PM's earlier proposal to decouple the two with an env flag is withdrawn — it does not work.** There is no config-only interim; the only interim control is not publishing yet. Additional pre-publish conditions: GOV-2 C5 (no self-serve-data-control claims while deletion is email-only) and a copy re-review against what the screens actually do, replacing platform-review implicature with the true mechanism. Secondary exposure to fix in the same window (A1, verified live on prod 08-27): `POST /knowledge` search results carry title/snippet/task_context/tags with NO `content_advisory` — free, unauthenticated, pre-purchase untrusted text into a buyer's model, fenced only after payment (R13-CLOSE §3.3 closes it). Timing cost accepted and stated: the ~72h long-form window will likely be missed; per GROWTH-1's own draft the incident converts to an evergreen category proof point, which is the fallback. **TYLER RULED 2026-08-27: HOLD EVERYTHING** — nothing publishes until R13-CLOSE §3.0 lands. The ~72h long-form window is knowingly forfeited; the incident converts to the evergreen category proof point per GROWTH-1's own fallback. The narrower "analysis-only, claims stripped" option was offered and declined. Humanizer + claims pass runs when §3.0 is merged, not before. ALL publishing Tyler-gated. |

## 30. Catalog Identity — TECHNICAL-ONLY SCOPE (Tyler, 2026-07-19; SUPERSEDED same day: curation → hard scope)

**Decision (final form, Tyler 2026-07-19 evening):** non-technical learnings are OUT OF SCOPE for Auxilo entirely —
**not collected, not considered, not published, by any user.** Rationale: buyers are agents searching for technical
solutions to token-heavy challenges mid-task; non-tech search demand is unlikely. The earlier same-day tech-first
CURATION rule (approval priority + 20% share cap) is superseded by this hard scope rule.

**AMENDED 2026-07-26 (Tyler, SPEC3-G1 scope ruling): PARTIAL SUPERSESSION.** The hard scope stands UNCHANGED for the
public catalog and every buyer-facing surface (the rationale above is buyer-demand-based and untouched). The SPEC3-G1
**private** tier — owner-only, never visible to any other account, zero buyer surface — ACCEPTS non-technical learnings:
CI-5 is re-scoped as a marketplace-quality rule enforced at the promote-to-public boundary (sanitize → full screening
chain, where non-technical still 400s). "Not collected" is relaxed ONLY for private-destined, owner-opt-in capture
(backlog drain + private-lane runner mode); nothing non-technical can reach any public or buyer surface. Record:
`~/.auxilo/handoffs/BUILD-SPEC-SPEC3-G1-2026-07-26.md`.

**Executed 2026-07-19:** 138 pending non-tech items rejected (communication 32, content-generation 106, incl. the
sensitive-comms family); 9 live non-tech items retracted (6 via API within window, 3 via operator box-edit past
window + restart); live non-tech = 1 (lrn_resend01, a TECHNICAL email-API learning mislabeled `communication` —
kept, recategorization owed to the CI-5 enforcement build). Catalog: 68 live, effectively 100% technical.

| # | Rule | Owner | Status |
|---|------|-------|--------|
| CI-1..4 | (superseded by CI-5 hard scope — priority/share-cap curation no longer applicable; tech triage pass CI-4 continues for TECH items) | — | SUPERSEDED 2026-07-19 |
| CI-5 | **HARD SCOPE: technical learnings only.** Non-technical content (interpersonal/comms strategy, copywriting/content insights, business strategy, personal matters) is not collected (extractor must not generate candidates), not accepted (/learn + MCP contribute must refuse or hold-reject), not published — for ALL users, not just curation. Category labels `communication`/`content-generation` to be retired or redefined as technical-only (e.g. email-API/content-pipeline TECH) by the enforcement build. | CAT-1 + BUILD-1 + GOV-3 | **BUILT 2026-07-19** (branch `ci5-scope-enforcement` from e1d9c90; spec `~/.auxilo/handoffs/BUILD-SPEC-CI5-SCOPE-2026-07-19.md` — taxonomy ruling: **option (a), labels RETIRED**; 6-category tech taxonomy). Enforcement: extractor hard-scope prompt + post-parse category drop (both score-gate states, rides npm 0.9.4 with the MCP enum/description trim); `/learn` 400 `CATEGORY_OUT_OF_SCOPE` (self-healing, machine-readable) + `/extract` remnant reject + approve-path guard (self/bulk/admin — a retired-label item can never re-enter publication); idempotent boot migration recategorized lrn_resend01 → web-interaction (provenance-stamped) and demotes any unexpected visible retired-label item; pricing base rows kept frozen for legacy stored items; openapi split `Category` (skills, 8) vs `LearningCategory` (6). Docs: AGENT-LEARNING-GUIDE (root+docs), README, CLAUDE.md, ONBOARDING-COPY, seed. Copy-back owed (untracked canon): MARKETPLACE.md ruling record, ERROR-CODES.md `CATEGORY_OUT_OF_SCOPE` row, UX-FLOWS.md example payloads. Tests: `test/ci5-scope-enforcement.test.js`. **Gate-A 2026-07-19: SHIP-WITH-FIXES — applied same wave on the branch:** F1 chat-pipeline pair pulled inside the net (/pipeline/upload prompt gains scope paragraph + enumerated 6 + CI-7 litmus; /pipeline/:id/approve loop skips candidates outside VALID_CATEGORIES with distinct `category_out_of_scope` for the retired two, `'general'` fallback removed; mutation-verified), F2 for-agents.html static fallbacks (lc-categories 8→6, stale lc-learnings 58 de-hardcoded — SSR fills live values), F3 reclassify-pending.js retired-label guard before the sensitivity gate, F5 parseLearnings stderr drop count. NOT pushed, NOT deployed. |
| CI-6 | **GTM threshold on the (now all-tech) catalog: 200 live.** Current: 68 live + tech-queue survivors in triage. | GROWTH-1 + CAT-1 | ACTIVE |
| CI-7 | **SYSTEM-FACT TEST (Tyler, 2026-07-19): a learning must be a reusable, verifiable fact about an external system or reproducible technical behavior, anchored to a symptom/error/blocker an agent would SEARCH mid-task.** Workflow preferences, process/methodology advice, project decision logs, agent-design opinions, and best-practice essays are NOT learnings — not collected (extractor litmus: "no error message, system limitation, or reproducible behavior at the core → do not extract"), not accepted (server content-type screen), not uploaded. Tyler's canonical formulation, verbatim: "would an agent hit a wall then want to search for these learnings to solve their issue? that's how you know." Canonical examples: Odesli-Tidal API gap = learning; two-phase mastering consultation contract = not a learning (both scored q19 — the rubric cannot see this dimension, which is why it's a hard rule, not a score). | CAT-1 + BUILD-1 + GOV-3 | ✅ **BUILT 2026-07-19 (Gate-A SHIP)** — litmus in all 3 collection prompts (extract-local both gate states, lib/extractor, pipeline upload); server screen: `learning_type` second verdict from the SAME LLM sensitivity call (zero added calls/latency), process_advice → HOLD `pending_review` reason `process_advice_screen` → lane `needs_your_eyes` even at q19; approve stays allowed (human appeal path); `LEARNING_TYPE_SCREEN_ENABLED` default ON; rubric anchor in score addendum + MCP description. Honest limit: client post-parse is category-mechanical — prompt is the collection gate, screen + human review are acceptance gates. Retro-applied by hand 2026-07-19: 75 pending advice items rejected, 8 live retracted (wall-test), 3 borderlines kept with argued symptom anchors. |

> **Wave-5 residual annotations (2026-07-19 night, recorded on closed rows — NOT open items):**
> - **Rail-reopening checklist (attaches to the AUD19-16/RT-5 counsel-gated rail decision — LEGAL bucket):** F-1 reservations are write-only (checkBalance never sums in-flight; wire reserved-sum + a stale-slot janitor at reopening); F-2 build a behavioral WAL-replay harness (W-series is source-pins); F-6a the unprotected crash window is the whole sendUSDC await, both paths.
> - **Accepted residuals (documented properties of shipped rows):** 5A F-4 ratings map can lag JSONL by one on crash (no startup fold — inverse of the old loss mode); F-5 TOCTOU 409 burns nonce+rate-slot (UX only); F-8 unlock-events.jsonl unrotated + full-scan replay guard (note before the GTM-9 render ships); 5B F7a const-arrow assert helpers invisible to the CH-7 scanner (function-decl regex only); ~~F7c full-suite count nondeterminism under --test-force-exit~~ **F7c FIXED 2026-07-20** (main `c076021`): root cause = `--test-force-exit` process-exiting the runner while the slowest child's final IPC batch was in flight (tail of exactly one file dropped uncounted, file varied run-to-run; wander 1242-1267 reproduced). Flag REMOVED from `npm test` (the eip712 un-unref'd interval that originally justified it was already `.unref()`-fixed; 15+ un-flagged runs prove no hang) + CI drift guard `scripts/check-test-count.sh` pins the discovered total (currently 1313, TAP reporter pinned, mutation-proven loud both directions) as the blocking "Run tests" step in ci.yml; F8 selectPendingIdsBySignal is a fixture-pinned semantic copy of the summary filter; 5C TOCTOU stat-then-read (OOM guard, not a security boundary).
> - **Applied post-review micro-fixes (this merge train, reviewer-prescribed):** 5A F-6b legacy-refund marker guard; 5A F-6c /withdraw 202 openapi doc; 5C F1 fail-closed unknown-size cap; 5C F2 single-file oversize behavioral test.

## 31. Live-Site Creative + Render Review (2026-07-19)

Art-direction seat (desktop + mobile, all 9 pages, judged against styles.css tokens + the five 07-18 visual moves) + mechanical render-QA (Chromium + WebKit, computed-style evidence; screenshots in session scratchpad). Verdicts: desktop clean and system sound (4 of 5 visual moves LANDED; hero scale system landed desktop / failed mobile); mobile blocked by one defect. The `<title>`/footer "Discovery Layer" straggler (OG + hero already memory-first) routes to COPY spec decision T1, not here.

| ID | Item | Priority | Owner | Status |
|---|---|---|---|---|
| DR-1 | **Mobile hamburger nav broken site-wide** — `backdrop-filter` on `#main-nav` (styles.css:96) creates a containing block for the `position:fixed` `.nav-links` overlay → "full-screen" menu renders as an 84px strip; For Builders / For Agents / How It Works land off-screen and are untappable; remaining links overlap page content. All 8 shared-nav pages; Chromium + WebKit confirmed via getBoundingClientRect + elementFromPoint. NEW regression, distinct from AUD19-11's class-toggle bug (toggle itself works). Fix: un-nest `.nav-links` from the filtered element (sibling of `#main-nav` or portal to body on open) + BUILD-4 regression assertion (`nav-open` → `.nav-links` height ≈ viewport height). | **P0 — mobile launch-blocker** | BUILD-1 + BUILD-4 | ✅ **BUILT + DEPLOYED 2026-07-20, live-verified** (overlay 812px viewport-exact on prod, all 8 links hit-testable; deploy lesson: the wave shipped changed CSS under the stale `?v=8` cache-buster — returning visitors held the broken copy until the v=9 bump `6b867a6`; RULE: any styles.css change bumps the `?v=` string in the same commit, add to Gate-A checklist) — pure-CSS: glass moved to `#main-nav::before`; zero HTML/JS; overlay viewport-exact, all 8 links hit-testable, desktop pixel-diff 0; regression test tests/test-mobile-nav-overlay.js (Tier-1 always-on, adversarially proven; Tier-2 playwright **DONE 2026-07-20** — BUILD-4 follow-up shipped: `playwright` devDependency pinned 1.61.1, wired into the existing `test` job in `.github/workflows/ci.yml` (`npx playwright install chromium --with-deps` before `npm test`, `CI_REQUIRE_TIER2=1` so a broken install fails the build loudly instead of silently downgrading to Tier-1-only), all 8 shared-nav pages now driven — not just the original 3 — which caught and fixed a real test bug (hardcoded 8-link assertion failed dashboard.html's legitimate 5-link authenticated nav), ~16s local runtime; local `npm test` unaffected on machines without the devDependency installed (Tier-2 quiet-skips, exit 0); doc impact: tests/README.md's "nothing in tests/ is CI-wired" claim amended with this file's documented exception, this row updated — takes effect once pushed) |
| DR-2 | Emoji icons (📚📈💰⭐, `.compound-factor-icon` styles.css ~1958) on /earnings + /for-builders break the monochrome/gold line-art icon language — the site's most off-brand element. Replace with the existing `.moat-icon`/`.step-icon` line-art set, gold ~0.85 opacity. | P1 | BUILD-3 | ✅ **DONE 2026-07-20 — DEPLOYED, live-verified** (final-sweep wave, merged main `c076021`): all four emoji (HTML entities) replaced with monoline 24×24 `currentColor` SVGs in the `.moat-icon`/`.step-icon` idiom (book-open, trending-up; coin + star reuse existing step-icon paths); `.compound-factor-icon` CSS moved font-size → width/height + aurum + 0.85 opacity, for-builders inline copy matched. Chromium-verified rendered SVG at both breakpoints; live curl shows SVG markup on /earnings. Cache-buster v=9→v=10 same commit. |
| DR-3 | Mobile type ramp collapse: hero hard-set 36px (styles.css:1595) vs 28-32px section H2s → dominance ~1.1× vs 2.25× desktop; the one-large-headline law vanishes at 375px. Rebuild spread: display heads ~40-44px, section H2s ~24-26px, ratio ≥1.5× at every breakpoint. | P1 | BUILD-3 | ✅ **DONE 2026-07-20 — DEPLOYED** (main `c076021`): inside `@media (max-width:600px)` ONLY — display heads 40px, section H2s 25px (`!important` defeats per-page inline sizes) → **1.60×** at 375px, measured via headless-Chromium computed styles on index/earnings/pricing (was ~1.29-1.36×). Desktop untouched (Gate-A verified the only type-size changes in the diff live inside that query); 601-800px tablet band deliberately unchanged. Gate-A INFO-2 recorded: closing-CTA H2 shares the 25px sub-head size on mobile per the section-H2 rule — confirmed intended (page display head = hero H1). |
| DR-4 | Reveal fragility: 20 of 26 `.reveal` elements start `opacity:0` and depend on IntersectionObserver adding `.visible`; observer failure renders whole sections blank obsidian (observed once live during review). Add no-JS/observer-failure failsafe. | P1 | BUILD-1 | ✅ **DONE 2026-07-20 — DEPLOYED** (main `c076021`, two iterations): hidden state now gated on `html.js-reveal` (added only when IntersectionObserver exists → no-JS/no-IO = fully visible, Chromium-proven 30/30) + a per-page 1.4s-tick WATCHDOG that force-reveals any `.reveal` sitting unrevealed INSIDE the viewport for 2 consecutive ticks. The watchdog superseded the first-build one-shot 3s backstop after verification REPRODUCED the live failure class: the observer can construct fine yet deliver initial entries all `isIntersecting:false` (deterministic in headless Chromium, identical on unmodified main — 0/30 revealed after 5s), so any construction/delivery-time stand-down under-covers. Healthy observers win the race (no animation loss; below-fold never prematurely revealed — index desktop has 0 above-fold `.reveal`); silent observer cured ≤2.8s; forced reveals still animate (transition lives on `.reveal`); reduced-motion `!important` unaffected. Measured post-fix in the broken env: earnings 3/3 in-view @3.2s, 8/8 after scroll; index 0 forced at load (correct), 5/5 after scroll. Gate-A: XSS-free, display:none skipped, dual-reveal idempotent, dynamic nodes picked up; LOW-2 recorded (interval never clears — steady-state cost one empty selector pass/1.4s, accepted). |
| DR-5 | Live catalog-count contradiction: `/health` `catalog_size:27` vs `/knowledge/stats` `learnings_count:102` (the figure actually injected into on-page ledgers); /status publicly shows 27 beside marketing ledgers showing 102. Reconcile field semantics + which is canonical for public surfaces. | P1 | CAT-1 + BUILD-2 | ✅ **DONE 2026-07-20 — DEPLOYED, live-verified** (main `c076021`): root cause — `catalog_size` on /health, /api/info, AND /stats read `skills.length` (the separate static 27-entry skills.json capability catalog), not the learnings marketplace; two different arrays sharing one ambiguous field name. All three now report `visibleLearningsList().length` (the CH-1/CH-2 shared public predicate, same as /knowledge/stats); old metric preserved honestly as `skills_catalog_size`; openapi updated; /status self-heals (renders /health verbatim). Structural predicate-agreement guards + mixed-fixture behavioral test extended in test/pricing-visibility.test.js (Gate-A mutation-verified the guard catches a `skills.length` revert). Live: /health 103 == /knowledge/stats 103, skills_catalog_size 27. Same-class sweep: /categories `total` deliberately left (self-consistent skill-category endpoint, never claimed marketplace semantics — flagged for symmetry if wanted). |
| DR-6 | /status drift: footer "v0.9.1" vs openapi 0.9.4 / npm 0.9.3 (missed in the 0.9.4 sweep); /status also runs an independent `:root` token set (near-miss obsidian/ivory values, no Geist fallback). Fix version string; confirm token exception is intentional or fold into shared stylesheet. | P2 | GOV-1 + BUILD-3 | ✅ **DONE 2026-07-20 — DEPLOYED** (main `c076021`): version now SELF-HEALS from `GET /version` (same-origin fetch, textContent, 0.9.4 fallback — can't silently drift again); token ruling = near-miss values with no rationale are DRIFT, not intention (the page's own theme-color was already #0A0A0A): local `:root` aligned to canonical tokens (`--obsidian` #0A0A0A, `--ivory` #FAFAF8, border rgba) + Geist sans/mono fallbacks added; status-only palette (state colors) kept; shared stylesheet deliberately NOT linked (page renders standalone during incidents). |
| DR-7 | Art refinements (bundled): /for-agents discovery-band gold over-spend (demote `.discovery-cat` to slate; price = the sole gold column) · raised/ground tint too subtle (hairlines, not tone, carry the rhythm; homepage ends on double-ground) · /pricing six identical section skeletons (vary 1-2 beats) · unify ledger container form (strip vs cards) · codify centered-vs-left headline rule · thin chapter heroes on /pricing /earnings /how-it-works (add one-gold inline ledger where a stat exists). | P2 | BUILD-3 | ✅ **DONE 2026-07-20 — DEPLOYED** (main `c076021`): (a) `.discovery-cat` aurum→slate, price = sole gold column; (b) homepage `#faq`→raised (fixes ending double-ground) + tint .05→.065 / hairline .10→.13; (c) pricing `#faq`→raised + hero inline ledger = 2 varied beats; (d) **ledger-form ruling: hairline STRIP is canonical** — earnings `.math-stat` flattened from boxed cards to strip cells (Chromium-verified transparent bg); (e) headline rule codified as "head matches its section-label alignment" — audit found zero true outliers, deliverable is the documented rule; (f) earnings/pricing/how-it-works heroes gain ONE gold ledger stat, truth-bounded: client-side `/knowledge/stats` fetch, `display:none` until a real number arrives (Gate-A verified never hardcoded/placeholder). Known seam (recorded): raised-adjacent pairs at odd section counts placed mid/late where least jarring. |
| DR-8 | **Self-unlock is CHARGED but advertised as $0** — caught by the copy-wave builder holding WP-1a on truth grounds, code-verified twice: `GET /knowledge/:id` charges via `dualAuthDynamic`→`deductCredit` BEFORE any owner check; the M-2 branch zeroes only *earnings* (wash-trade guard; its own comment: content returns "at the price of the unlock, with zero earnings"); no free owner-recall path exists. Meanwhile README.md:7,100,122 publicly claim "own learnings back free / your own learnings $0 / Self-unlocks are $0" (live on GitHub + riding npm 0.9.4), and the LOCKED memory-first positioning + STRAT-positioning-2026-07-18 treat the $0 self-unlock as the shipped, load-bearing single-player mechanic — it is not shipped. **Decision fork (Tyler + SPEC-1/CFO-1): (a) build the owner short-circuit so self-unlock is genuinely $0 (aligns product with position + README; recommended), or (b) retract the claims (README + WP-1a premise), which guts the single-player story.** Either way the false claim must not survive the next npm publish/promotion push. WP-1a held in the copy wave pending this. | **P0 — false public pricing claim + positioning load-bearing** | Tyler + SPEC-1 + CFO-1 + BUILD-2 | ✅ **BUILT 2026-07-20 — option (a) per Tyler.** Owner short-circuit on `GET /knowledge/:id` (commit `1180736`): PROVABLE owners (valid API key → `contributor_account_id` match, or account whose AUD19-3 EIP-712-linked `account.wallet` == `contributor_wallet`) served FREE before `dualAuthDynamic` AND before the `CONTRIBUTOR_NOT_ONBOARDED` gate (owner recall receives no third-party funds). Free path is a PURE READ — no counter (not even ops-only `unlocks_total`), no demand/ranking, no earnings, no WAL, no unlock event, no purchase-ledger (LW-7 held), no file writes, exempt from the payments kill switch (moves no money). `X-Wallet-Address` stays an unverified claim → paid path + M-2 backstop (still pays, still zeroes). Contract surfaces: MCP `auxilo_unlock` desc ("YOUR OWN learnings are $0", rides next npm cut), `/prices`, openapi (`_revenue` +`amount_paid_usd`/`self_unlock`/`owner_recall_free`/`accrual_capped`), ERROR-CODES.md + PRODUCT.md §2 canon. Tests: `test/dr8-self-unlock-free.test.js` (31) + honest pin updates (ci5 strips 4→5; r01 window 4200→9500 + call-site pin); suite 1297/0 fail; live smoke proved owner-free-with-zero-credits, linked-wallet arm on an un-onboarded learning, non-owner 402 w/ accepts[], header-claimant refused, learnings/earnings byte-identical after free recalls. **Gates: Gate A SHIP** (0 gating; LOWs recorded: wallet-arm earnings-destination-as-identity semantic confirmed intended per M-2 model; owner path omits content_advisory matching M-2 precedent; structural-test convention noted; pre-existing no per-route rate limit on unlock — accepted residual, both reviewers ruled no change), **SPEC-1 SHIP** (6 abuse angles cleared: no scrape path w/o wallet private key, linkWallet one-time+unique blocks transfer walking, no AUD19-2/discovery distortion, wash-trade backstop intact, DoS bounded to existing free-read class, zero custodial receipt), **CFO-1 SHIP** (forgone revenue ~$0 at current scale — prior self-unlock "revenue" was seed accounts paying our own Stripe fees, net-negative cash; pack burn forecasts shift down and pack purchases now cleanly signal REAL cross-buyer demand; $0 recall books nothing = correct non-transaction posture; watch metric: `unlocks_total − quality.unlocks` isolates paid-backstop volume). **README:7/100/122 claims now TRUE in code** (live only after deploy). WP-1a un-held + shipped same wave. NOT deployed, NOT npm-published — runbook `~/.auxilo/handoffs/DR8-DEPLOY-RUNBOOK-2026-07-20.md`. **DEPLOYED 2026-07-20 (gitSha `cdac367` live-verified: WP-1a block on prod homepage, /ready 200, withdraw 503 intact)** |

## 32. Codex Builder Environment Remediation (2026-07-26)

RCA of the SPEC3-A3 stale-base incident (builder reported clean worktree at `feb96e7`, a sha absent from canonical history) + the remediation set. Root cause: the builder's desktop app opened its Auxilo project from the retired pre-move clone at `~/Documents/Custom/auxilo` — frozen 2026-06-12 at `feb96e7`, origin remote deleted, entire lineage dead since the 2026-07-01 history reconciliation — because the app's project-trust list still pointed there and had no entry for `~/dev/auxilo`. Nothing non-canonical was ever pushed (the dead clone's missing remote was the only guard — luck, not design: the machine's owner-scoped token could push `main` freely; `main` had zero protection). Mid-incident reset succeeded: the build worktree now sits on `codex/spec3-a3` at canonical `f387b18`.

| ID | Item | Priority | Owner | Status |
|---|---|---|---|---|
| CBX-1 | AGENTS.md rewritten as standing-rules-only: canonical-base verification + mandatory session-start ritual (fetch, base proof via `git branch -r --contains HEAD` / merge-base for resumes, status, `npm test`, `check-test-count.sh` — all pasted into the build thread), STOP-on-non-canonical-base, `codex/<spec-id>` branch + push rules, docs/-untracked, no `stash -u`, same-commit test-count pin, one-build-in-flight, standing product locks (earnings lock, zero-inference, promise verification), retained source-discipline + delivery-report contract. Replaces the stale P2.1a-pinned version (which told every new session its "primary task" was a build that shipped in April). | P1 | GOV-1 | ✅ this commit |
| CBX-2 | GitHub ruleset `protect-main` (id 19787699): force-push (`non_fast_forward`) and deletion of `main` blocked, **zero bypass actors** — applies to the owner token too. Deliberate friction: if history surgery is ever truly needed, the ruleset must be consciously deleted first. | P1 | GOV-3 | ✅ 2026-07-26 |
| CBX-3 | Machine-side pre-push guard installed in the canonical checkout's local hooks: direct pushes of `main` are PM-only (GitHub cannot distinguish PM from builder — both use the single owner token — so the discriminator lives on the machine). Un-versioned by design; builders hand off on `codex/*` branches. | P1 | GOV-3 | ✅ 2026-07-26 |
| CBX-4 | Re-point the builder app at `~/dev/auxilo` (open project + Trust) so its worktree isolation inherits canonical `.git` and every build starts on the right base by construction. | P1 | Tyler | ✅ 2026-07-26 — trust entry verified in app config |
| CBX-5 | Retire `~/Documents/Custom/auxilo` (rename/archive) + its builder-app worktree children. **GATED on SPEC3-A3 merge** — the in-flight A3 worktree is a child of that clone's `.git`; deleting early destroys the build. A3 handoff needs no push from the dead clone: PM fetches `codex/spec3-a3` directly from the worktree path. 2026-07-26 update: the A3 worktree has ZERO build commits and a clean tree — cheaper path is restarting the A3 build from the re-pointed canonical project, which re-gates retirement on "old builder session closed" instead of "A3 merged". | P1 | PM (on Tyler's word) | ✅ 2026-07-26 — Tyler confirmed old builder session closed; clone renamed in place with RETIRED marker (nothing deleted), remaining app worktree bundled into it (the other was already app-pruned), stale project-trust entry removed from the builder app config. Canonical checkout verified untouched. |
| CBX-6 | Credential hygiene: dead personal-account keyring token removed ✅ (named in the private record only — this file is public; the username published here 2026-07-26 was scrubbed at tip same night, remains in commit `dc4e646` history); token scope trim (drop `delete_repo`) ✅ DONE 2026-07-26 — earlier attempts stalled on a browser-session account mismatch during the device flow; completed via driven-browser flow + Tyler authorization, scopes live-verified (`gist, read:org, repo, workflow`). | P2 | Tyler | ✅ 2026-07-26 |

## 33. Conway Decommission Sweep (2026-08-24)

Tyler confirmed the pivot off Conway is total and the pre-LLC Conway-era wallet (`0x1BE9…6Ca6`) is retired; contributor payout policy set the same day — **contributors choose Stripe or USDC**. Full sweep run across the live repo, the retired pre-move clone, stray working copies, operator memory trees, scheduled jobs/skills, and the live catalog. Verdicts: live code was already Conway-clean — the retirement is implemented (`server.js` `LEGACY_PLATFORM_WALLETS`: identity-only recognition so the seed catalog doesn't 409; LLC wallet is the sole payTo; legacy wallet force-deverified); `facilitator.openx402.ai` verified as an independent permissionless facilitator, not Conway infrastructure — stays as current rail; all x402/Stripe legs untouched per policy. Full private record: `docs/CONWAY-DECOMMISSION-2026-08-24.md` (local canon).

| ID | Item | Priority | Owner | Status |
|---|---|---|---|---|
| CDX-1 | Private-canon salvage: the retired pre-move clone still held the ONLY copies of 22 internal canon files (17 docs/ + 4 root + the full private doc index) that never migrated at the 2026-07-01 repo move — restored verbatim into the live tree under the locally-only convention (`docs/*` ignore covers docs; machine-side exclude + the CDX-3 .gitignore lines cover root). Every dangling CLAUDE.md routing-table doc reference except `SECURITY-AUDIT.md` and `prompts/` (both pre-existing gaps, chipped in CDX-8a) resolves again. | P0 | PM | ✅ 2026-08-24 |
| CDX-2 | Doc corrections: Conway-as-current infra lines fixed (FINANCIAL-PLAN cost basis → Fly.io + pivot/payout-policy banner; HOSTING-SWOT status → decommission COMPLETE; OPS-READINESS → SUPERSEDED banner), stale 402 `payTo` examples → LLC wallet (UX-FLOWS/ERROR-CODES/DEVELOPER-GUIDE), retired wallet swapped out of all doc examples for a placeholder, PRODUCT `/discover` example matched to the live catalog, money-transmission analysis re-stamped with current rail state. Regression green post-edit: `check-test-count` 1455 discovered / 0 fail, pin matched. | P1 | PM + GOV-1 | ✅ 2026-08-24 |
| CDX-3 | Tracked-file diff — CLAUDE.md SPEC-1 row (+`contracts/README.md`), BUILD-SPEC.md:600 historical-wallet note, .gitignore root-canon guard block — staged locally on the PM branch, Gate-A reviewed; pushes on Tyler's approval. | P1 | Tyler | ⏳ surfaced |
| CDX-4 | Disposition: full archive tarball of the retired dir written to the private tree; deletion of the source dir + `conway-topup.mjs` (embedded presumed-dead Conway API key — revoke-if-possible, then delete; hygiene) + the obsolete pending-learnings draft + one verified-fully-landed stale builder clone teed up as ONE Tyler pass. | P1 | Tyler | ⏳ teed up |
| CDX-5 | The retired wallet still carries a residual USDC balance on Base (and no gas); sweep to the LLC wallet per the Slam-side Assignment papering, then the key-custody decision. Operator-side item — details in the private brief. | P1 | Tyler | ⏳ open |
| CDX-6 | Catalog: 5 live Conway-subject learnings (`lrn_conway01..04` old-build seed data still in the prod DB + 1 organic) — keep as valid third-party knowledge (recommended) vs retract/reprice. | P2 | CAT-1 + Tyler | ⏳ open |
| CDX-7 | The 26 retired-wallet-attributed learnings: re-attribution ruled UNNECESSARY (identity-only by design, $0 earnings, wallet deverified, never payTo) and UNSUPPORTED in code (no admin path touches `contributor_wallet`; `linkWallet` one-time/one-way) — no action. | P2 | SPEC-1 | ✅ ruled 2026-08-24 |
| CDX-8 | Follow-ups chipped: (a) GOV-3 canonical `SECURITY-AUDIT.md` and SPEC-2's `prompts/` exist in neither tree — locate or repoint; (b) payment copy-vs-code divergence (four MCP tool descriptions + dashboard copy + PRODUCT §99 describe the inert router / paused withdrawals as live; rides npm 0.9.9); (c) GOV-1 refresh of the 22 salvaged June-era docs. | P2 | GOV-1/2/3 + SPEC-2 | ⏳ chips |

## 34. Public-Site Copy Panel Review (2026-08-27)

Source: `~/.auxilo/handoffs/COPY-PANEL-SITE-REVIEW.md` (FINAL, 52KB). Six seats (positioning, advertising, conversion, JTBD, fresh-eyes human, fresh-eyes AI-agent) over the 8 live pages plus `llms.txt`, reviewed against `docs/GTM.md`, `docs/BRAND-VOICE.md`, README, `docs/MARKETPLACE.md`, `docs/SUPPORTED-CLIENTS.md`. Live pages diffed vs repo at `7ba42ad`; differences are SSR working as designed, so live == authored copy. 29 findings (F1-F29), 3 BLOCKERs. Replacement copy for every top item is drafted in the report under the hard rules. **PM note on provenance:** F1 and F24 were re-verified independently from source by the PM before this section was written; everything else is carried from the panel report and is marked panel-reported, not PM-verified. **No public copy was edited** — `public/` is surfaced-first per standing rule, so every row below is a proposal awaiting Tyler.

| ID | Item | Priority | Owner | Status |
|----|------|----------|-------|--------|
| COPY-9 | **BLOCKER F1 — the site misstates its own consent model.** Three surfaces state an absolute that is false: `public/for-builders.html:870` "Approve what goes live: nothing publishes until you do", `public/index.html:325` "nothing you extract goes public until you approve it", `public/dashboard.html:658` "Nothing is published until you approve it." **DIRECTION CORRECTED 2026-08-27 by PM behavioral verification — the panel had it backwards, and so did this row as first filed.** The contradiction is REAL; the identification of which side is false was not. Verified against live prod, not just source strings: `EXTRACTION_AUTOPUBLISH_CONSENT_ENABLED` is set NOWHERE (absent from `flyctl secrets list -a auxilo` and from `fly.toml [env]`), so `cleanLaneFlagEnabled()` returns false and every clean EXTRACTION-channel item is held with `standing_consent_off` (server.js:6087-6093). The clean lane is flag-dark and counsel-gated (D1/C1); it has never been on. **Therefore the three flagged surfaces are ACCURATE today, and how-it-works Step 5 is the FALSE one** — its "publishes to the catalog right away", "Every extraction publish carries a 7-day retraction window", and "Prefer approve-first? Switch to manual mode in your account settings" all describe the flag-dark future state as if live. **Propagating Step 5 as the panel recommended would have shipped a false claim to three surfaces including the signed-in dashboard, telling builders their agents' captures auto-publish when they do not — the exact trust break F1 exists to prevent, inverted.** Corrected remediation: leave the three surfaces alone; rewrite Step 5 to today's behavior. Channel nuance neither the panel nor this row's first draft surfaced: a DIRECT `POST /learn` clean submission DOES auto-publish (`seamlessEligible = seamlessScreensAndScore`; the standing-consent block runs only `if (submissionChannel === 'extraction')`). Site copy describes the extraction flow, so it is correct for what it describes. **Method lesson worth keeping: the original row was stamped PM-VERIFIED on the strength of reading the STRINGS at HEAD. String-presence verification cannot establish which of two contradictory claims is true — that needs a behavioral/runtime check against the deployed env. A copy claim about product behavior is verified against the running product, never against the copy.** Consent representation, so GOV-2 rides. | P1 | BUILD-3 + GOV-2 | **OPEN 2026-08-27 — direction corrected by PM behavioral verification against prod env; the panel's canonical sentence is REJECTED, do not apply it.** Hard precondition on any governed-channel campaign (both the panel §6 and the R13-CLOSE red team reached this gate independently). |
| COPY-10 | **BLOCKER F24 — `public/for-agents.html` documents a machine contract that does not exist.** Line 756 tells agents the verdict field returns `unlock`, `skip`, or `low_confidence` and to "Build your unlock logic on this". The real enum in `openapi.json:259-266` is `strong_buy` / `recommended` / `consider` / `expensive`, and its semantics are price-vs-value-benchmark, not an act/skip instruction. `low_confidence` appears nowhere in `openapi.json`, `server.js`, `lib/`, or `mcp-server.js`. An agent following the page branches on values that never arrive and every comparison falls through. Panel also reports example JSON shapes do not match real responses, and that the page instructs agents to drop unlocked bodies straight into the context window while the schema itself marks them UNTRUSTED do-not-execute (`UNTRUSTED_CONTENT_ADVISORY`, `server.js`). Agents are the audience most likely to act on the error and least able to notice it. | P1 | SPEC-2 + BUILD-3 | **OPEN 2026-08-27 — PM-VERIFIED from source** (enum read at `openapi.json:259`; fabricated values grep-confirmed absent repo-wide). |
| COPY-11 | **BLOCKER F2 — four paused-rail verb violations** against the COPY-4 / `docs/BRAND-VOICE.md` verb policy: earnings hero and its `og:description` ("you're paid on every unlock" — travels into every link preview), index "Agents pay, you collect.", pricing label "Flexible Withdrawals", dashboard "Your share (withdrawable)". Replacements drafted. | P2 | BUILD-3 | **OPEN 2026-08-27 — panel-reported** (individual strings not re-verified by PM). |
| COPY-12 | **Positioning wave (report §5 items 4-9).** Pick one category noun and ship it to the machines first (F3+F16: JSON-LD on 7 pages plus the `llms.txt` tagline all carry the commodity line "the discovery layer for the agent economy"; 5 category nouns live site-wide). Put $0 self-unlock back in the story (F4 — appears exactly once on the whole property) and the honest ledger where the sell happens (F7). Make the install command the primary action and retire the dishonest CTAs (F5 — no primary button site-wide performs the install, and pricing's "Search the catalog free" points at a page with no search); surface the notify capture (F11). Fix the extraction client list in 5 spots (F6 — "GA on Claude Code and OpenClaw" contradicts `/legal/supported-clients`, which omits Codex and grades OpenClaw Best-effort, not GA). De-hype `/earnings` and the how-it-works earnings FAQ (F7 — "hundreds of agents", "Year 5", "average prices climb" against a 6-unlock ledger) and gloss direct-vs-discovery at first use (F26 — undefined until page five). Credits-first payment ordering on for-agents and api (F8); technical-only catalog scope stated nowhere on-site or in `llms.txt` (F10). | P2 | BUILD-3 + GROWTH-1 | **OPEN 2026-08-27 — panel-reported.** |
| COPY-13 | **Spec-side fixes (F25) routed to SPEC-2** with normal Gate A: `agent.json` documents only the x402 rail so a walletless orchestrator concludes paid access is closed; `openapi.json` security arrays on `/knowledge` and `/learn` self-contradict on search auth; endpoint-table completeness; rate-limit and 409 prose; internal ticket-id scrub. | P2 | SPEC-2 | **OPEN 2026-08-27 — panel-reported.** |
| COPY-14 | **Medium and low sweep (F12-F23, F27, F28)** at next page revision. Includes the double-priced running example, the reprice-bounds math gap, and the wallet-only `/learn` example shown against "no wallet required" (all caught by the fresh-eyes human seat). | P3 | BUILD-3 | **OPEN 2026-08-27 — panel-reported.** |
| COPY-15 | **Three product calls wearing copy clothes — Tyler decisions, deliberately not drafted as copy.** (a) A public search box: the strongest answer to the prove-it-before-install job, which the JTBD seat graded F as an orphan, and the change that would make pricing's existing search CTA honest. (b) A governance/trust page: the incident campaign targets agent-platform decision-makers, an audience with no door in the current nav, and the draft's claim list exists as no single page. (c) Render the live ledger on for-builders, converting the small ledger from liability into context. | P2 | Tyler | **OPEN 2026-08-27 — awaiting Tyler.** |
| COPY-16 | **Dashboard non-paused render branch contradicts the marketing story (F22).** The branch offers a live Stripe withdrawal flow with "1 to 2 business days" while every marketing surface runs the paused-rail verb policy. Reconcile BEFORE `payouts_paused` ever flips, or the site ships two payment stories on the same day. Related but distinct from the GTM-10 projection-reinstatement trigger. | P1 | SPEC-3 + CFO-1 | **OPEN 2026-08-27 — panel-reported; gated on the rail flip.** |
| COPY-17 | **`docs/BRAND-VOICE.md` canon-status block was stale and actively misleading.** Its stand-in banner and "Status of related canon files" section both asserted `BRAND_GUIDELINES.md` and `docs/GTM.md` were absent from this checkout. Both are present — restored 2026-08-24 by the Conway-decommission private-canon salvage. **But the panel's proposed one-line "they exist now" fix would have made this worse:** the PM verified both restored files carry NONE of the ratified canon (no Seven Voice Laws, no paused-rail verb policy, no struggle-elimination hierarchy, no 2026-07-20 marker) and that both retain their pre-move mtimes (07-12 and 07-03), i.e. they were copied verbatim, never merged. Present-but-stale is a higher risk than absent, because `CLAUDE.md`'s routing table now resolves to a real file an agent will trust. Corrected to say the fold gap is still open and only changed shape. Note `COPY-7`'s own DONE rationale ("search found NO current private-canon checkout") is now outdated history for the same reason; row left closed, artifact fixed instead. | P3 | GOV-1 | ✅ **DONE 2026-08-27 (PM)** — `docs/BRAND-VOICE.md` banner, §value-prop cross-ref, and both canon-status bullets rewritten with a dated update stamp; file is untracked/private so nothing pushed. Tyler's (a) fold-into-canon vs (b) keep-standing-home decision remains unmade and is now the blocking item. |

| COPY-18 | **NEW DEPENDENCY ON RECORD (surfaced by the COPY-9 correction, previously written down nowhere): site copy is a dependency of the consent flip.** The moment `EXTRACTION_AUTOPUBLISH_CONSENT_ENABLED` is flipped (D1/C1, counsel-gated), the three COPY-9 surfaces plus the for-builders code comment become FALSE on landing, and how-it-works Step 5 becomes true. The flip and the copy change must ship in the same wave. **Add to the D1/C1 flip checklist — a silent flip breaks the site's most trust-sensitive claim in three places including the signed-in dashboard.** | P1 | GOV-1 + BUILD-3 | OPEN — checklist item, no build. |
| COPY-19 | **F6 (HIGH) PM-VERIFIED against the served matrix: the extraction-client claim contradicts our own legal page.** Five spots across how-it-works and for-builders say extraction is "GA on Claude Code and OpenClaw". The served `docs/SUPPORTED-CLIENTS.md` (2026-07-28) has Claude Code ✅ Supported, **Codex (CLI + Desktop) ✅ Supported**, Antigravity ⚠️ Supported (early), OpenClaw ✅ **Best-effort**. So the marketing line omits Codex entirely (the client UC-6 shipped capture for) and promotes OpenClaw a full tier, while the for-builders FAQ states the wrong summary in the same breath as linking the page that corrects it. Panel's replacement, verified accurate: "Background extraction runs on Claude Code and Codex today. More clients are on the supported clients page. Claude Desktop and Cursor connect as MCP-only clients." | P1 | BUILD-3 | OPEN — verified, copy drafted, Tyler-approved before deploy. |

## 35. Public-Site Art-Director Visual Review (2026-08-28)

Source: `~/.auxilo/handoffs/AD-VISUAL-SITE-REVIEW.md` (FINAL, 44KB). Sequenced AFTER §34 and gated on it — the copy panel's settled messaging direction is the standard the visuals were judged against, and every row below names the copy finding it blocks or completes. Lead seat brand-art-director; supporting seats responsive-css-engineer and visual-brand-qa (source-level), whose load-bearing claims the lead then re-verified against the live DOM. Scope: the 8 live pages at 375 / 768 / 1440px. 34 findings (V1-V34). 38 real screenshots captured with headless Chrome at `~/.auxilo/handoffs/ad-visual-shots/`.

**Method note.** Numbers are computed styles, bounding rects and scroll extents read from the running site, not estimated from images; contrast ratios were computed from the token values. Provenance is marked per row: *lead-measured* (live), *both* (source-derived by a supporting seat, then live-verified by the lead), or *source-only* (not reproduced live, labelled latent).

**PM note on two self-corrections.** The lead's first pass made two wrong calls that the supporting seats caught and that were re-verified before filing, because both change the fix: (1) the breakpoint count is **7 across 35 media blocks**, not the 5 visible in `styles.css`; (2) the heading-size chaos is **not** "the site mixes fluid and fixed type" — the fluid ramp is consistent and well built, and the real cause is ancestor selectors carrying different clamp formulas over identical markup. The withdrawn hypothesis is recorded here so nobody re-derives it.

**What the review found working, so nothing here gets "fixed":** contrast passes AA/AAA at every pairing actually used (ivory/obsidian 18.94:1, slate/obsidian 6.29:1, obsidian-on-gold buttons 8.66:1; the one failing palette pair, ivory-on-gold at 2.19:1, is used nowhere); **zero horizontal-scroll violations** on any page at any width (`overflow-x: clip` at `styles.css:29` verified `scrollWidth === clientWidth` on all 8); the fluid type ramp; the `--section-pad` 120/80/64 step; container discipline on 7 of 8 pages; `aria-expanded` correct on every hamburger with a 46x44 target and 56px menu links; and every internal link and `#anchor` resolving (zero 404s, zero dead anchors).

**No file in `public/` was edited and nothing was pushed** — `public/` is surfaced-first per standing rule, so every row below is a proposal awaiting Tyler.

| ID | Item | Priority | Owner | Status |
|----|------|----------|-------|--------|
| AD-1 | **BLOCKER V19 — trust-critical copy is horizontally clipped on phones, and this gates COPY-9.** At 375px code blocks render a **293px box against up to 744px of content** (for-builders) and 514px (api); `overflow-x:auto` on `<pre>` means the page never scrolls sideways, so the defect is invisible to any whole-page check. Clipped strings include the **exact line COPY-9/F1 is about** (`# Approve what goes live: nothing publishes until you do`), plus `# installs the extraction runner, then asks consent (default: No)` and the withdrawals-accrual disclosure. On api, **6 of 8** code blocks clip. **Consequence for sequencing: reconciling the consent sentence inside a code comment does not reach a phone reader** — roughly the first 40% of the line is all that shows without scrolling inside the block. Consent and payment-state disclosures must move out of code comments into prose beneath the block, in the SAME wave as COPY-9, and that is a layout decision, not a copy edit. | P1 | BUILD-3 + BUILD-1 | **OPEN 2026-08-28 — lead-measured live** (box vs content widths and clipped strings read from the running DOM at 375px). Hard precondition on COPY-9 landing correctly. |
| AD-2 | **BLOCKER V10 — the button hierarchy is inverted and the primary action is styled as the weakest control on the page.** Computed: `btn-primary` gold fill / obsidian text / 15px / **52px** tall; `btn-secondary` transparent / ash border 0.2 alpha / 15px / **54px** tall; `copy-btn` transparent / slate border / **11px** type / `2px 8px` padding / **36px** tall. The secondary is **2px taller than the primary**, and the control that copies `npx auxilo setup` — the whole conversion thesis per COPY-12/F5 — is the smallest, lowest-contrast, lowest-type-size interactive element in the system. This is the visual half of F5 and **no copy rewrite fixes it**. Proposal: real primary treatment on the install block (gold fill, 52px, full-width at 375px), demote today's `btn-primary` destinations to secondary, pin `btn-secondary` to 52px so it can never out-measure primary. | P1 | BUILD-3 | **OPEN 2026-08-28 — lead-measured live.** Pairs with COPY-12. |
| AD-3 | **Click-destination map: dead, dishonest, duplicate and ejecting CTAs (V11/V12/V13/V17).** Full map of every control on every page is in the report §4. Structural result: **six of fourteen marketing primaries/secondaries land on `/for-builders`**, and the operator story the ledger CAN back has no primary anywhere. Specifics: `index` has **zero** `btn-primary` and, at 375px, **zero buttons in the fold at all**; `/earnings` carries **three** gold primaries (y=436, y=1412, y=5528) all pointing at `/for-builders`, **two with the identical label** "Start building your catalog", and zero secondaries, so the page has no visual gradient; `pricing.html:847-869` renders `$10/$25/$100` `.pack-card`s containing **three `<p>` elements and no `<a>` or `<button>`** (the visual expression of F5's unbuyable packs); the nav's gold "Connect your agent" points at `/#install` from every page, so from the seven non-index pages it **ejects the reader to the homepage** and on index it is a no-op onto a block already in the fold. Terminal destinations: the packs, `openapi.json` (raw file, no return path), and the dashboard login for a visitor with no account. **No page routes to `/pricing` from a button**, though it carries the hero §34 rates best on the site. | P1 | BUILD-3 + SPEC-3 | **OPEN 2026-08-28 — lead-measured live** (every href and computed button style extracted from the running DOM; routes and anchors verified to resolve). Completes COPY-12's F5 half. |
| AD-4 | **V30 — the site's only statement of the $0 self-unlock is visually demoted to an aside, which compounds COPY-12/F4.** `index.html:261` carries an inline `max-width:720px; text-align:center`. Measured live at 1440px: that container is **720px wide at left edge 353px, centered**, between siblings **1100px wide at left edge 163px** — inset 190px and switched to centered while its neighbours are left-aligned, so it reads as a pull-quote rather than a section. F4 found this promise appears exactly once on the whole property; the one appearance is also styled as the least structurally important block on the page. One inline style to fix, and the cheapest high-value change on this list. | P2 | BUILD-3 | **OPEN 2026-08-28 — both** (flagged from source by the supporting seat, geometry then confirmed live). |
| AD-5 | **V27 + V33 — every page ships its own stylesheet, which is the root cause of most rows below and changes the fix order.** All 8 pages carry an embedded `<style>` block loading AFTER `/styles.css`: index 82 lines, how-it-works 540, for-agents 426, for-builders 452, pricing 446, earnings 261, api 320, dashboard 529 — roughly **3,000 additional lines**, where equal-specificity rules silently win. Downstream consequences already filed as their own rows: two FAQ components (AD-12), per-page nav behaviour (AD-10), per-page hero padding (AD-8), and `.compound-factors` / `.value-callout` rendering differently on the two pages that use each. Also ~**300 lines of dead CSS** in `styles.css` reachable by no page (`.earnings-table*`, `.snapshot-table*`, `.discovery-row`, `.auth-card`, `.catalog-stats`, `.hiw-page-header`, `.section-h2`) plus ~160 more fully shadowed. **A fix applied only to `styles.css` may not land — check the page's own block first.** Recommend consolidating BEFORE the other visual rows, since AD-8, AD-10 and AD-12 otherwise each become eight edits instead of one. | P2 | BUILD-1 + BUILD-3 | **OPEN 2026-08-28 — both** (discovered independently by both supporting seats; line ranges spot-verified by the lead). Sequencing row. |
| AD-6 | **V21 — one heading level renders five sizes and the markup gives no clue which.** Measured live on index at 1440px: `h2` at **24 / 32 / 42 / 42 / 42 / 24 / 44 / 60px** across weights 500 and 700, while **all eight carry the identical class (`reveal`) inside the identical parent (`container`)** — size is assigned entirely by ancestor section selectors. The 42-vs-44px pair is a distinction with no visible difference. Across pages the recurring closing-CTA `h2` — same job, same position, last thing a visitor sees — ranges **44px to 60px** (index/for-builders `clamp(32,4.5vw,60)`; for-agents/pricing `,56`; api/earnings `,52`; how-it-works `,44`), a 36% spread with no grouping logic. Checked and NOT found: any h2/h3 pair within 2px on one page, so within-page hierarchy is not flattened; the defect is cross-section and cross-page. | P2 | BUILD-3 | **OPEN 2026-08-28 — lead-measured live; root cause corrected by supporting seat** (see the PM self-correction note above). |
| AD-7 | **V6/V7/V8/V9 — the pages do not line up with each other.** Hero alignment splits **3 left (index, for-agents, for-builders) / 3 centered (earnings, pricing, how-it-works)** with no rule, so the primary heading jumps sides as you navigate. The content left edge moves between **163px and 187px** at 1425px (pricing and api are the outliers) — a 24px shift between pages sharing a fixed nav, visible on load. `how-it-works` and `api` hero blocks compute `max-width:none` against the 1100px token, so the how-it-works `h1` spans **1377px of a 1425px viewport** while its own body below stays constrained. The footer is not one component: **seven different link sets across eight pages** (pricing and earnings carry only 2 links each and are the two pages that most need onward routing; earnings' `/api` is an absolute `https://auxilo.io/api` where every peer uses a relative path; dashboard has no footer at all). | P2 | BUILD-3 | **OPEN 2026-08-28 — lead-measured live.** |
| AD-8 | **V1/V2/V3/V4/V5 — spacing rhythm.** The hero band is hardcoded per page and does not use the spacing token, producing **six different mobile results** for one nominal component: for-builders / pricing / earnings **228px (28.1% of a 375x812 viewport, with no mobile override at all)**, for-agents 202px, how-it-works 200px, api 148px. On for-builders at 375px **roughly 39% of the first phone screen is empty background** before the eyebrow appears. `earnings.html:352` hardcodes `padding:80px 0 60px`, giving earnings three section paddings on one page (the site's most visible stutter); `api.html:369` hardcodes 96px against every peer's 120px. Underneath: **558 spacing declarations, 38 distinct values**, 14 of 36 pixel magnitudes off the 4px grid, concentrated in a reused 6/10/14/18px quarter-step family (`14px` alone appears 37 times) — a coherent system shows 6-10. Separately, mobile page lengths are extreme: for-agents **15,522px (19.1 phone screens)**, for-builders 14,855, api 12,687, pricing 12,496, how-it-works 12,110, index 11,001, earnings 7,764 — section count and card stacking, not padding, so this wants a content-density pass rather than a spacing change. | P2 | BUILD-3 | **OPEN 2026-08-28 — both** (page-height and hero geometry lead-measured; the declaration census is the supporting seat's, source-cited). |
| AD-9 | **V20/V29/V22 — breakpoints and reflow.** **Seven distinct breakpoints across 35 media blocks** (480, 540, 600, 640, 700, 768, 900); 540 and 640 exist only in dashboard, and 768 appears in both `styles.css` and independently in `earnings.html`. At exactly 768px the 768 and 900 queries match while 700/640/600/540/480 do not, so tablet is a half-adapted state by construction. Confirmed instance: `.pricing-packs` computes `322.5px 322.5px` at 768px with **`$100` orphaned alone in row 2 beside an empty 323px gap**, and the grid steps 3-col → 2-col at 900 → 1-col at 600, so every width from 600 to 900 shows it. Same-named components diverge for real: `.compound-factors` renders 2-col at 480px on earnings and 1-col on for-builders because for-builders redefines it with its own breakpoints. Also the for-builders stat strip reflows **1-then-2** at 375px (`70%` alone, `$0.08` and `<1 min` paired) rather than stacking cleanly. | P2 | BUILD-3 + BUILD-1 | **OPEN 2026-08-28 — both** (breakpoint census and the pack-orphan prediction from the supporting seat; grid columns and card coordinates then confirmed live at 768px by the lead). |
| AD-10 | **V23 — mobile-nav close behaviour differs page to page and no page locks background scroll.** Live-verified Escape: index **closes**, earnings **closes**, how-it-works **does not**, pricing **does not**, dashboard **does not**; source shows how-it-works and dashboard define `toggleNav()` with zero other listeners, so they also lack link-click close. On **every** page: `body` computes `overflow: clip visible` unchanged while the full-viewport 812px overlay is open, so **the page scrolls behind the menu**; focus is never moved into the menu (`activeElement` stays `BODY`, no trap); outside-click never closes; the toggle has no `aria-controls`. Not a trap — the hamburger sits at `z-index:1001` above the overlay so re-tapping always works — but a degraded and inconsistent experience. Note this is a DIFFERENT defect class from DR-1 (§31), which was a containing-block break; the DR-1 fix and its regression test both hold, and `tests/test-mobile-nav-overlay.js` covers only the DR-1 geometry, not close/focus/scroll behaviour. | P2 | BUILD-1 + BUILD-4 | **OPEN 2026-08-28 — both** (per-page matrix from source by the supporting seat; Escape behaviour re-tested live on 5 pages by the lead, which corrected an earlier lead claim that Escape worked everywhere). |
| AD-11 | **V24/V25/V26 — colour hierarchy, not colour contrast, is the problem.** Contrast passes everywhere (see the intro). Gold is the only accent and carries no rank: it is spent on the nav CTA, three same-destination primaries on earnings, a "Notify me" buried at 75% page depth, section eyebrows, stat numerals, code keywords, list glyphs and the logo — while `npx auxilo setup` renders in gold monospace **inside a passive code box**, reading as syntax highlighting rather than as the action. Proposal: reserve gold FILL for one control per screen (the install primary), keep gold for stat numerals and the logo, move eyebrows / list glyphs / code keywords to ash or the already-defined-but-unused-for-this `--aurum-dim`. Also `--aurum-border` at 0.35 alpha makes the nav CTA read as LESS clickable than the `btn-secondary` outlines below it despite being higher-intent. Housekeeping: `growth-flywheel.svg`, `section-divider.svg`, `geometric-pattern.svg`, `og-image.svg` and `logo-square.svg` are referenced by **no HTML page** — check `growth-flywheel.svg` against AD-14 before commissioning new work, then adopt or delete. | P2 | BUILD-3 | **OPEN 2026-08-28 — lead-measured live** (asset non-reference grep-confirmed). |
| AD-12 | **V28 — the FAQ is two different components, plus cross-page component drift.** Live-verified: pricing renders **4 static `.faq-item`s, 0 accordion buttons, answers permanently visible**; how-it-works renders **8 `.faq-question` buttons with `aria-expanded="false"` and a collapsed first answer (height 0)**. Six pages use the static form; how-it-works alone uses a JS accordion with a rotating plus icon. Same section, same class family, different visual AND interactive behaviour — visit any two in sequence and the FAQ belongs to a different site. Related drift, source-confirmed: `.value-callout` differs in seven properties between earnings and for-builders (visibly fainter gold on one); `.compound-factor` is a bordered mini-card on for-builders and bare text on earnings; the numbered-step card is `40px 32px` on index/for-builders but `36px 28px` on for-agents/api. Pick one FAQ implementation and one card spec. Downstream of AD-5. | P2 | BUILD-3 | **OPEN 2026-08-28 — both** (drift catalogued from source by the supporting seat; the two FAQ implementations confirmed live by the lead). |
| AD-13 | **V14/V15/V16/V18/V31/V32/V34 — targets, forms and semantic residuals.** The site's only email capture (`#payout-waitlist`, the single form on all seven marketing pages) sits at **y=11,200 of a 14,855px page — 75% down for-builders, 13.8 phone-screens** — inside FAQ answer #2, spending a gold `btn-primary` there; this is the visual half of COPY-12/F11 and wants a standing block above the footer on for-builders, earnings and pricing. Tap targets: every `copy-btn` is **45x36px** sitewide (6 on for-agents alone) — above the WCAG 2.2 AA 24px floor, below the 44px comfortable minimum, and it must be REBUILT not restyled if F5 promotes copy-to-clipboard to the primary action; dashboard `.btn` computes ~42.4px (the login CTA) and `.btn-sm` ~32.8px. Footer links compute **16px tall** at 375px (`api` at 18x16) — standalone list links, so the 2.5.8 inline exception does NOT apply; inline links inside sentences (e.g. "withdrawals open soon" at 164x19) ARE exempt and are deliberately not counted. Semantics: `pricing.html:936` places `<h4>` directly under `<h2>` with no intervening `h3` (every other page is clean, all 8 have exactly one `h1`); the signed-in dashboard view renders its six section titles as `<div class="dash-card-title">`, so in the state a real user occupies longest the page has **zero heading elements**. No focus ring distinct from hover on `btn-secondary`; no `aria-current` on the active nav item. Latent, NOT reproduced: `.endpoint-table-wrap` (`api.html:201`) uses `overflow:hidden` as its unconditional base and only gains `overflow-x:auto` below 600px, leaving 601-900px with no scroll affordance — box-model math suggests it does not visibly clip today, so it is filed as the one table using clip-not-scroll rather than a confirmed break. | P3 | BUILD-3 + BUILD-4 | **OPEN 2026-08-28 — mixed provenance, marked per clause** (waitlist depth and tap-target geometry lead-measured live; heading semantics and the api-table risk source-only from the supporting seats, the latter explicitly unreproduced). |
| AD-14 | **Visual value proposition — proposals, Tyler's call.** There is **not one explanatory diagram on any of the eight pages**, and the only illustrative asset in `public/` is referenced nowhere. Top recommendation: **"One learning, three moments"** in the empty right half of the index hero — a thin-line, single-gold-accent diagram showing the same learning at three times (discovered Tuesday for ~45 min / ~$2.40 → returns to your own agent at **$0.00** → unlocks for another agent at **$0.08**, 70% back to you). It solves three filed problems at once: fills the dead right half (AD-3/V12), gives the fold the category-defining image F9 says is missing, and is the only placement that puts the $0 self-unlock in front of every visitor (F4, AD-4). Structural sketch plus a validated inline SVG drawn to the real tokens is in report §5; it states no volume, no projection and no calendar, so it survives GTM §1 and the F7 de-hyping, and every number in it is already ratified and live. Two supporting treatments: a **cost strip** for pricing and for-agents (F19's "promote the numbers" as an image), and a **loop ring for how-it-works whose whole point is the consent fork** — after `screen`, clean → publishes retractable 7 days, flagged → your private queue → you approve; a picture holds the two-branch rule that one sentence has now failed to hold on four surfaces, so it should ship WITH COPY-9, not after. Explicitly NOT recommended: an animated hero flow, which would fight the honest-numbers posture that is the site's best trust asset. | P2 | Tyler + BUILD-3 | **OPEN 2026-08-28 — proposal only, nothing built.** Ties to COPY-12 (F4/F9/F19) and COPY-9 (F1). |

## Counts

| Priority | Open | On Hold | Deferred | Done/Verified | Total |
|----------|------|---------|----------|---------------|-------|
| P0 (blocks launch) | **1** | 0 | 0 | **34** | 35 |
| P1 (blocks real money / production) | **15** | 6 | 2 | 80 | 103 |
| P2 (blocks scale) | **20** | 1 | 0 | 27 | 48 |
| P3 (polish) | **2** | 0 | 0 | 5 | 7 |
| **Total** | **38** | **7** | **2** | **146** | **193** |

> **Counts caveat (PM, 2026-08-28).** The table above now includes the nine §34 rows and the fourteen §35 rows (AD-1..AD-14: 3 P1, 10 P2, 1 P3). It still does NOT include `R13-CLOSE` and `GTM-HF` (§29) — their statuses have not been re-derived, so the totals remain understated by at least those two rows and want a full recount by whoever owns them.

> **§34 / §35 pairing (PM, 2026-08-28).** The copy panel (§34) and the art-director review (§35) were run in sequence against the same 8 live pages, and three pairs must ship together rather than in separate waves: **AD-1 gates COPY-9** (the consent sentence F1 corrects lives in a code comment that is horizontally clipped at 375px, so fixing the words alone ships the correction where phone readers cannot see it); **AD-2 + AD-3 complete COPY-12's F5 half** (F5 found no primary button performs the install; §35 found the install control is the weakest-styled element on the page and mapped every dead, dishonest, duplicate and ejecting CTA); **AD-4 compounds COPY-12's F4** (the $0 self-unlock appears once on the property, and that one appearance is also the most visually demoted block on its page). AD-5 is a sequencing row: per-page embedded stylesheets mean several §35 fixes are eight edits each until consolidated.

> **2026-07-20 npm cut SHIPPED:** auxilo-mcp@0.9.4 PUBLISHED by Tyler (registry `dist.shasum` byte-identical to the Gate-A-reviewed tarball `cc23329…8ee`, `latest` tag confirmed, wrapper `auxilo` ^0.9.0 resolves it; published artifact spot-verified: $0 self-unlock `auxilo_unlock` description present, get_stats absent / 17 tools, re-led README). This propagates every "rides npm 0.9.4" rider: wave-3.4 `auxilo init` CLI, wave-5B sanitize/reject_by_signal MCP actions + Drive-ID scrub 0.5.0, wave-5C size-capped adapters, CI-5 extractor prompts, DR-8 $0 description, AUD19-13 tool collapse.
> **2026-07-20 FINAL SWEEP (Tyler-directed):** DR-2/3/4/5/6/7 (all §31 design rows), CP-1, LW-1 (secrets verified live), LW-4 (backup freshness green), LW-5 (all fragments), LW-9(c), COPY-7, AUD19-13 remainder (get_stats collapse), F7c test-count root-cause + CI drift guard — all closed, Gate-A SHIP (0 gating; LOW-1 CP-1 re-verify stamp reset + LOW-2 watchdog interval recorded as accepted), deployed to prod, live-verified. The sole remaining open P0 is **AUD19-9** (router finality vs proxy timeout — bound to the counsel-gated router flag-enable, banking bucket). Every other remaining open/partial row is legal-gated, banking/rail-gated, a Tyler product hold (GTM/B-10/COPY-6/COPY-8), Tyler-approval-gated (SPEC3-D2), or roadmap-scale feature work (UC-2/3/4, UC-5 R1-R5, GTM-9 render, LW-18b review page, SPEC3-B2/B3 UI residuals, AUD-F1/F2/TESTS mainnet gates).

> **2026-06-10 Launch Wave**: LW-1–LW-10 added (§20, spec `specs/BUILD-SPEC-LAUNCH-WAVE.md`). 4 P0 + 6 P1 OPEN. P0 launch gate re-opened until LW-1–LW-4 land.

> **P2.1a Autonomous Extraction**: DONE (2026-04-15). Server-side extraction pipeline, admin CLI, transcript sources, runner, cron jobs, legal docs updated.
> PD-6: DEFERRED — Stripe withdrawals work, on-chain needs 0.01 ETH when ready.
> PD-1–PD-5: All DONE (2026-03-30). BUILD-2a–2d: DONE (5-agent audit, 2026-03-30). H-3 (OFAC monitoring): OPEN.

> **Spec files**: `auxilo/specs/SPEC-A3.md` (Opus), `SPEC-A0.md` (Opus), `SPEC-A2.md` (Sonnet), `SPEC-A1.md` (Sonnet), `SPEC-A4.md` (Sonnet)
> **P0 LAUNCH GATE: CLEARED** — All 26 P0 items DONE/VERIFIED. Sprint 07 closed T-4 (26/26), T-5 (30/30), T-7 (58/58). Total regression: 157/157 green.
> **Sprint 08 DONE**: Production Hardening — P1 Infra (I-4, I-5) + P2 Security (M-A through M-G). 28/28 S8 tests + 49/49 P03 regression + 11/11 live checks. See `specs/SPRINT-08.md`.
> **Sprint 09 DONE**: Audit Remediation — 5/5 audit findings fixed (AU-1 through AU-5). 10/10 regression checks green. 8 findings deferred. See `specs/SPRINT-09.md` + `specs/AUDIT-POST-S8-RESULTS.md`.
> **Sprint 11 DONE**: MCP Completeness + Security Polish — AU-10 (8 MCP tools), AU-11 (CORS docs), AU-12 (err.message stripped), AU-13 (startup .catch replaced). 4/4 items closed.
> **Sprint 12 DONE**: Deploy (server already current, S11 live-confirmed), auxilo-mcp v0.7.0 packaged (BLOCKED npm auth — `npm login` required), live 9/9 PASS. AU-12 confirmed no `details` field; 32 routes; the web-reader health route, /stats, /knowledge/stats all 200.
> **Sprint 13 DONE**: Brand & Narrative — BRAND_GUIDELINES.md (B-1, 159 lines) + landing-page-copy.md (B-8, 194 lines). Zero code changes. Opus model.
> **Sprint 14 DONE**: Landing Page Build & Deploy — index.html (34KB), styles.css (22KB), server.js route changes (Approach A: / → landing page, /api → API discovery). PM2 restart. 7/7 live checks 200. Sonnet model. No new punch list items closed (build/deploy bridges B-1 + B-8 into production).
> **Sprint 15 DONE**: Brand Assets & Governance — B-6 (OG image, 1200×630 PNG deployed to VM, serves 200), B-7 (favicon, inline SVG data URI in index.html), G-1 (Stripe terms review, 15.5KB compliance doc). 3 items closed. Opus model. Key insight from G-1: x402 and Stripe must be separate rails — no fiat↔crypto conversion.
> **Sprint 16 DONE**: Content & Assets — B-5 (8 SVG icons, 24×24, monoline stroke, currentColor), B-9 (onboarding copy, 6 sections covering full builder flow). 2 items closed. Opus model. No deployment needed (assets not yet integrated into UI).
> **Sprint 17 DONE**: Brand Assets Final — B-2 (geometric-pattern.svg, tile-ready, 8-12% Aurum opacity), B-3 (growth-flywheel.svg, 4-stage circular loop, "learnings" not "skills"), B-4 (section-divider.svg, centered A mark + gradient fade lines). 3 items closed. Opus model. All P1 brand items now DONE.
> **Sprint 18 DONE**: Governance Sweep — G-2 (MSB analysis, MEDIUM risk, marketplace exemption likely applies), G-3 (tax reporting, 1099-K at $600 threshold), G-4 (ToS draft, Wyoming recommended), G-5 (privacy policy draft, GDPR/CCPA covered), G-6 (risk register, 20 risks, 9 HIGH/CRITICAL). 5 items closed. Opus model. **All P0 + P1 items now DONE.** Only 3 deferred P2 audit items + 1 blocked remain.
> **Sprint 19 DONE**: Integration & Deploy — Icons integrated into landing page (5 feature icons + 3 step icons, inline SVG, currentColor). Growth flywheel section added between #how-it-works and #for-builders. OG image deployed (31552 bytes, serves 200). `auxilo-mcp@0.7.0` published to npm (npm auth blocker cleared via granular access token). 5/5 live checks pass. Sonnet model for integration, manual deploy via ttyd.
> **Sprint 20 DONE**: P2 Scale Optimizations — AU-6 (startup parallelization, WAL sequential then Promise.all for settlements), AU-7 (in-memory TTL cache for accounts 60s + settlements 30s), AU-8 (per-API-key sliding window rate limiter, x402 bypass). Audited by Opus (SHIP WITH FIXES → 4 fixes applied → re-audit SHIP). Deployed to VM, 6/6 live checks pass. **All open items CLOSED. Only B-10 (LinkedIn launch content) remains BLOCKED on stealth mode lift.**
> **Sprints 21-25 DONE**: Production Readiness — 4 parallel review agents (Legal, Security, Infra, Ops) identified 18 unique blockers. All addressed across 5 sprints: S21 (x402 payment dedup, content moderation hold, reporting endpoint, SESSION_SECRET validation, health-check.js), S22 (OFAC SDN screening from treasury.gov + alt.csv, backup.sh + setup-cron.sh), S23 (legal doc hardening: contact info, indemnification, GENIUS Act, payment characterization normalization, payout mechanism language, DMCA registration guide), S24 (version normalization 0.7.0, OpenAPI 36→45 paths, security.txt, status page, error code reference), S25 (audit remediation: backup path fix, PII hash in reports, OFAC alt.csv, Bitcoin case sensitivity). Consolidated audit: SHIP WITH FIXES → 4 HIGHs fixed → deployed. 8/8 live checks pass.
> **Unblocked**: B-9 (onboarding copy) — Phase 0 + 1 now complete
> **Orchestrator**: Run via ORCHESTRATOR.md — dispatches scoped agents with fresh context per sprint item
> **Docs**: DEPLOY-GUIDE.md (legacy VM deployment), ORCHESTRATOR.md (sprint execution pattern)
> **P2.1a — Autonomous Learning Extraction**:
> - A6 Option B: `scheduled` extraction mode removed from validModes (server.js) and openapi.json enum. Review surface deferred to P2.1b.
> - B16: No-op — Option B selected, no `/extract/review/*` routes needed. Confirmed no stale routes exist.
> - Legacy 4-learning flush: DONE 2026-04-18 (3 published via `/learn` POST: mailersend, cloudflare-email-routing, cloudflare-pages. 4th (private-email-stack) correctly rejected by sensitivity-filter v0.4 for real PII — moved to ~/.auxilo/rejected-sensitive-20260418/).
> - FOUNDATION.md prerequisite: P2.1a launch depends on the governance framework in FOUNDATION.md (not yet created). Cross-reference the P0 governance track.

> **P2.1a — Bugs discovered during T-107/T-108 runs (2026-04-18), queued as P1 fast-follows**:
> - P1-1: `claude-code source.discoverSessions` looked in wrong path (`conversations/` subdir) — ✅ FIXED `2732ac6`.
> - P1-2: `claude-code source.readSession` extracted `msg.content` (wrong level) — ✅ FIXED `2732ac6`. Real shape is `msg.message.content` array of typed blocks.
> - P1-3: `runner.js --transcript <path>` handler missing — ✅ FIXED `ece8072`. Single-file mode now works via source adapter + scrub + POST pipeline; plain-text fallback when readSession fails.
> - P1-4: `ip_redacted` IPv4-only regex — ✅ FIXED `799c972`. New `redactIp()` helper handles IPv4 (mask last octet) + IPv6 (truncate at /64 prefix). Unit-tested in-place.
> - P1-5: `/extract` Idempotency-Key undocumented — ✅ FIXED `560adec`. AGENT-LEARNING-GUIDE §4 now has full request example + 8-row error-code cheat sheet.
> - P1-6: `/extract/consent` POST route missing — ✅ FIXED `799c972`. Thin wrapper over appendConsent with action→mode mapping (grant→automatic, revoke→off).
> - P1-7: `/account/settings` Bearer-JWT-only, rejects X-API-Key — ⏸ DEFERRED. Runner can't flip modes programmatically. Needs auth-surface review before changing; post-soak task.
> - P1-8: `runner.js` ignored `~/.auxilo/credentials.json` — ✅ FIXED `ece8072`. `loadCredentials()` now reads env → file → localhost default. Matches mcp-server.js.
> - P1-9: Extractor chunk failures silent — ✅ FIXED `dcd3d91`. Server now logs `X/Y chunks failed LLM extraction: <error>` to stderr when `providerResult.stats.chunks_failed > 0`.
> - P1-10: Dockerfile runs as root — ✅ FIXED `5a1cf91`. New `scripts/docker-entrypoint.sh` chowns /app/data then `exec su-exec node`. Applied on next Fly deploy.
> - P1-11: Anthropic dashboard truncated-key display confusion — pending doc note in AGENT-LEARNING-GUIDE (P2 polish).
> - P1-12: npm audit high advisories — ✅ FIXED `6cc3f73`. 4 advisories patched via in-major upgrades (hono, path-to-regexp, express-rate-limit, @hono/node-server). 0 vulnerabilities remaining.
> - P1-13: LaunchAgent `auxilo-sweeper` TCC-blocked on `~/Documents/` — ✅ DONE (2026-06-10). Root cause fixed in repo: new `node scripts/runner.js --install-sweeper` copies the full executable surface (wrapper + runner + sources + sensitivity-filter) to `~/.auxilo/bin/` and rewrites the plist to point there (`scripts/runner.js` installSweeper(), `scripts/auxilo-sweeper-wrapper.sh` now resolves runner relative to its own location). Also fixed a second latent bug found during the fix: wrapper + plist exported `AUXILO_EXTRACTING=1`, which tripped the runner's own recursion guard (runner.js main) — every launchd run silently no-opped, so the 2026-04-18 "soak" never actually swept. Applied locally: installer run, agent bootout/bootstrap'd, `launchctl kickstart` cycle executed — 28 sessions discovered, learnings published to auxilo.io, `sweeper.err.log` clean (0 bytes).
> - P1-13a: Same TCC block on the two remaining `~/Documents` LaunchAgents — ✅ DONE (2026-06-10). `io.auxilo.digest` (label since purged of its legacy prefix): daily-digest is a purely local job (summarizes `~/.auxilo/extract.log`), so it got the P1-13 treatment — new `node scripts/runner.js --install-digest` copies `jobs/daily-digest.js` (self-contained, no repo-local requires) to `~/.auxilo/bin/jobs/` and rewrites the plist; agent reloaded, `launchctl kickstart` verified (stderr clean, digest rendered, exit 0). the retraction-sweeper LaunchAgent: **retired as obsolete** — `jobs/retraction-sunset.js` mutates the repo-local `data/learnings.json`, but the production catalog has lived on the Fly volume (`fly.toml` `[[mounts]]`) since cutover, so the local sweep operated on a dead copy. Booted out, plist deleted. No production gap: server.js enforces the retraction window by timestamp at retract-time (DELETE /learn/:id), and nothing reads `retraction_window_active` for behavior — the stale-true flag on Fly is cosmetic. If flag hygiene matters later, port the sunset sweep into server.js's in-process cron. Bloated logs cleaned (~1MB `retraction-sunset.stderr.log` removed with the job; digest logs truncated).
> - P1-13b: daily-digest always reports zero activity — parser/format mismatch — ✅ **FIXED 2026-07-19** (`wave3-client-funnel`, SPEC3 slice A2). Runner now emits `published=`/`held=`/`rejected=`/`account=` tokens on the per-run log lines ONLY (the inner postExtract line lost its tokens — it double-counted), and classifies /learn responses truthfully (2xx `pending_review` = held, not published — every hook item pends today, so old `published=` counts were lies). Digest parses `held=`, aggregates per-builder, and (with credentials.json) renders queue truth as the three SPEC3 lanes: Ready to publish / Needs a score / Needs your eyes + oldest-age + dashboard link; the word "clean" never renders. Operator action after 0.9.3 install: re-run `--install-sweeper` and `--install-digest` to refresh the ~/.auxilo/bin copies.
> - P1-14: `deploy.js` pkill+nohup legacy code — SUPERSEDED (current deploy uses git-pull path via `a7eb2cf`). Delete post-legacy-host-decom.
> - P1-15: legacy sandbox `dc034f2b...` 48h standby — decommission deadline 2026-04-21. Requires Tyler's go (destructive prod action).
> - P1-16: Cloudflare proxy re-enable with Full (strict) SSL — post-soak enhancement, Tyler's call.
>
> **P1 fix burn-down (2026-04-18 session)**: 9 fixes shipped, 3 deferred (P1-7, P1-13 need Tyler review; P1-16 is an enhancement), 1 pending non-urgent doc polish (P1-11). Commits: `2732ac6`, `560adec`, `799c972`, `ece8072`, `5a1cf91`, `6cc3f73`, `82a7a21`, `dcd3d91`. All in `origin/main`, will land on next `flyctl deploy` (post-soak).

> **P2.1a — T-109 execution log (2026-04-18)**:
> - T-105 ✅ (skipped — doc checks against pre-migration infra; post-migration docs pending separate T-105 re-run)
> - T-106 ✅ consent grant recorded: audit chain genesis at 2026-04-18T17:03:41Z
> - T-107 ✅ server-side pipeline exercised end-to-end (extract_attempt audit row, idempotency row, chain continuity verified)
> - T-108 ✅ real publish: 2 learnings, cost $0.02376, retraction_window_ends 2026-04-25
> - T-109 steps 1-8 ✅ (retraction, recursion guard, kill switch, server revocation all verified 2026-04-18)
> - T-109 step 9: **IN PROGRESS (restarted)** — previous soak (started 2026-04-18T17:54Z) never ran: P1-13 TCC block + the `AUXILO_EXTRACTING=1` recursion-guard self-trip meant zero sweeper cycles executed. Both fixed 2026-06-10 (see P1-13 entry). New 72h soak started 2026-06-10T20:26Z (first verified launchd cycle: kill-switch sentinel present, sweep ran, learnings published, err log clean). Expected pass at 2026-06-13T20:26Z.
