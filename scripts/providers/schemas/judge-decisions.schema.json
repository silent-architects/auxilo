{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$comment": "CODEX-OUTPUT-SCHEMA-REJECTED — strict output transport for judge mode. parseJudgeDecisions enforces candidate bounds, unique indices, decision count and duplicate ids within the candidate's top-K list. A null matched_index_id on a nonduplicate is equivalent to omission in that parser.",
  "type": "object",
  "properties": {
    "decisions": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "candidate_index": { "type": "integer" },
          "duplicate": { "type": "boolean" },
          "matched_index_id": { "type": ["string", "null"] }
        },
        "required": ["candidate_index", "duplicate", "matched_index_id"],
        "additionalProperties": false
      }
    }
  },
  "required": ["decisions"],
  "additionalProperties": false
}
