# `/public/` — Brand Asset Index

| File | Purpose |
|------|---------|
| `og-image.svg` | OG image source (1200×630) — Angular A mark + wordmark + tagline on Obsidian |
| `og-image.png` | OG image rasterized PNG for `<meta property="og:image">` |
| `geometric-pattern.svg` | Subtle tile-ready background pattern (A-mark triangles + crossbar fragments at ~12% opacity Aurum on dark) |
| `growth-flywheel.svg` | 4-stage circular growth loop diagram (Supply → Demand → Growth → Network Effect) |
| `section-divider.svg` | Decorative horizontal divider — centered A mark with gradient-fading Slate lines |
| `icons/` | UI icon set (stroke-based, 1.5px, `currentColor`) — see `icons/INDEX.md` for full list |
| `favicon` | Referenced in `index.html` `<link>` — uses the Angular A mark (inline SVG data URI or external file) |
